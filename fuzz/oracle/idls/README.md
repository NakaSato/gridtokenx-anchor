# IDL Files

Place your program's IDL JSON file here as `oracle.json`.

## Generating IDL

If you have the legacy (v0.29) IDL format:
```bash
anchor idl convert target/idl/oracle.json -o fuzz/oracle/idls/oracle.json
```

If you have the new IDL format (v0.30+), copy it directly.
