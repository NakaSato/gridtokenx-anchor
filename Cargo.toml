# This directory's workspace is now managed at the root level
# See ../Cargo.toml for workspace configuration
# 
# Legacy Anchor-specific profile settings (preserved for anchor build compatibility)
# Note: Release profile is inherited from root workspace
