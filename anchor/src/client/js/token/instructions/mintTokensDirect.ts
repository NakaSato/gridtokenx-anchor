/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { ENERGY_TOKEN_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const MINT_TOKENS_DIRECT_DISCRIMINATOR = new Uint8Array([
  13, 246, 31, 237, 99, 19, 88, 226,
]);

export function getMintTokensDirectDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MINT_TOKENS_DIRECT_DISCRIMINATOR
  );
}

export type MintTokensDirectInstruction<
  TProgram extends string = typeof ENERGY_TOKEN_PROGRAM_ADDRESS,
  TAccountTokenInfo extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountUserTokenAccount extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends
    | string
    | AccountMeta<string> = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTokenInfo extends string
        ? WritableAccount<TAccountTokenInfo>
        : TAccountTokenInfo,
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountUserTokenAccount extends string
        ? WritableAccount<TAccountUserTokenAccount>
        : TAccountUserTokenAccount,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type MintTokensDirectInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type MintTokensDirectInstructionDataArgs = { amount: number | bigint };

export function getMintTokensDirectInstructionDataEncoder(): FixedSizeEncoder<MintTokensDirectInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: MINT_TOKENS_DIRECT_DISCRIMINATOR })
  );
}

export function getMintTokensDirectInstructionDataDecoder(): FixedSizeDecoder<MintTokensDirectInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
  ]);
}

export function getMintTokensDirectInstructionDataCodec(): FixedSizeCodec<
  MintTokensDirectInstructionDataArgs,
  MintTokensDirectInstructionData
> {
  return combineCodec(
    getMintTokensDirectInstructionDataEncoder(),
    getMintTokensDirectInstructionDataDecoder()
  );
}

export type MintTokensDirectAsyncInput<
  TAccountTokenInfo extends string = string,
  TAccountMint extends string = string,
  TAccountUserTokenAccount extends string = string,
  TAccountAuthority extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  tokenInfo?: Address<TAccountTokenInfo>;
  mint: Address<TAccountMint>;
  userTokenAccount: Address<TAccountUserTokenAccount>;
  authority: TransactionSigner<TAccountAuthority>;
  tokenProgram?: Address<TAccountTokenProgram>;
  amount: MintTokensDirectInstructionDataArgs['amount'];
};

export async function getMintTokensDirectInstructionAsync<
  TAccountTokenInfo extends string,
  TAccountMint extends string,
  TAccountUserTokenAccount extends string,
  TAccountAuthority extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ENERGY_TOKEN_PROGRAM_ADDRESS,
>(
  input: MintTokensDirectAsyncInput<
    TAccountTokenInfo,
    TAccountMint,
    TAccountUserTokenAccount,
    TAccountAuthority,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  MintTokensDirectInstruction<
    TProgramAddress,
    TAccountTokenInfo,
    TAccountMint,
    TAccountUserTokenAccount,
    TAccountAuthority,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ENERGY_TOKEN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    tokenInfo: { value: input.tokenInfo ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    userTokenAccount: {
      value: input.userTokenAccount ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenInfo.value) {
    accounts.tokenInfo.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 111, 107, 101, 110, 95, 105, 110, 102, 111])
        ),
      ],
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.tokenInfo),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.userTokenAccount),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getMintTokensDirectInstructionDataEncoder().encode(
      args as MintTokensDirectInstructionDataArgs
    ),
    programAddress,
  } as MintTokensDirectInstruction<
    TProgramAddress,
    TAccountTokenInfo,
    TAccountMint,
    TAccountUserTokenAccount,
    TAccountAuthority,
    TAccountTokenProgram
  >);
}

export type MintTokensDirectInput<
  TAccountTokenInfo extends string = string,
  TAccountMint extends string = string,
  TAccountUserTokenAccount extends string = string,
  TAccountAuthority extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  tokenInfo: Address<TAccountTokenInfo>;
  mint: Address<TAccountMint>;
  userTokenAccount: Address<TAccountUserTokenAccount>;
  authority: TransactionSigner<TAccountAuthority>;
  tokenProgram?: Address<TAccountTokenProgram>;
  amount: MintTokensDirectInstructionDataArgs['amount'];
};

export function getMintTokensDirectInstruction<
  TAccountTokenInfo extends string,
  TAccountMint extends string,
  TAccountUserTokenAccount extends string,
  TAccountAuthority extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ENERGY_TOKEN_PROGRAM_ADDRESS,
>(
  input: MintTokensDirectInput<
    TAccountTokenInfo,
    TAccountMint,
    TAccountUserTokenAccount,
    TAccountAuthority,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): MintTokensDirectInstruction<
  TProgramAddress,
  TAccountTokenInfo,
  TAccountMint,
  TAccountUserTokenAccount,
  TAccountAuthority,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ENERGY_TOKEN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    tokenInfo: { value: input.tokenInfo ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: true },
    userTokenAccount: {
      value: input.userTokenAccount ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.tokenInfo),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.userTokenAccount),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getMintTokensDirectInstructionDataEncoder().encode(
      args as MintTokensDirectInstructionDataArgs
    ),
    programAddress,
  } as MintTokensDirectInstruction<
    TProgramAddress,
    TAccountTokenInfo,
    TAccountMint,
    TAccountUserTokenAccount,
    TAccountAuthority,
    TAccountTokenProgram
  >);
}

export type ParsedMintTokensDirectInstruction<
  TProgram extends string = typeof ENERGY_TOKEN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    tokenInfo: TAccountMetas[0];
    mint: TAccountMetas[1];
    userTokenAccount: TAccountMetas[2];
    authority: TAccountMetas[3];
    tokenProgram: TAccountMetas[4];
  };
  data: MintTokensDirectInstructionData;
};

export function parseMintTokensDirectInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedMintTokensDirectInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      tokenInfo: getNextAccount(),
      mint: getNextAccount(),
      userTokenAccount: getNextAccount(),
      authority: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getMintTokensDirectInstructionDataDecoder().decode(instruction.data),
  };
}
