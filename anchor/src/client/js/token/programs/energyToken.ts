/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  type Address,
  type ReadonlyUint8Array,
} from '@solana/kit';
import {
  type ParsedAddRecValidatorInstruction,
  type ParsedBurnTokensInstruction,
  type ParsedCreateTokenMintInstruction,
  type ParsedInitializeInstruction,
  type ParsedInitializeTokenInstruction,
  type ParsedMintGridTokensInstruction,
  type ParsedMintTokensDirectInstruction,
  type ParsedMintToWalletInstruction,
  type ParsedTransferTokensInstruction,
} from '../instructions';

export const ENERGY_TOKEN_PROGRAM_ADDRESS =
  '94G1r674LmRDmLN2UPjDFD8Eh7zT8JaSaxv9v68GyEur' as Address<'94G1r674LmRDmLN2UPjDFD8Eh7zT8JaSaxv9v68GyEur'>;

export enum EnergyTokenAccount {
  TokenInfo,
}

export function identifyEnergyTokenAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array
): EnergyTokenAccount {
  const data = 'data' in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([109, 162, 52, 125, 77, 166, 37, 202])
      ),
      0
    )
  ) {
    return EnergyTokenAccount.TokenInfo;
  }
  throw new Error(
    'The provided account could not be identified as a energyToken account.'
  );
}

export enum EnergyTokenInstruction {
  AddRecValidator,
  BurnTokens,
  CreateTokenMint,
  Initialize,
  InitializeToken,
  MintGridTokens,
  MintToWallet,
  MintTokensDirect,
  TransferTokens,
}

export function identifyEnergyTokenInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array
): EnergyTokenInstruction {
  const data = 'data' in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([73, 235, 183, 190, 157, 85, 246, 2])
      ),
      0
    )
  ) {
    return EnergyTokenInstruction.AddRecValidator;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([76, 15, 51, 254, 229, 215, 121, 66])
      ),
      0
    )
  ) {
    return EnergyTokenInstruction.BurnTokens;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([35, 109, 237, 196, 54, 218, 33, 119])
      ),
      0
    )
  ) {
    return EnergyTokenInstruction.CreateTokenMint;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([175, 175, 109, 31, 13, 152, 155, 237])
      ),
      0
    )
  ) {
    return EnergyTokenInstruction.Initialize;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([38, 209, 150, 50, 190, 117, 16, 54])
      ),
      0
    )
  ) {
    return EnergyTokenInstruction.InitializeToken;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([216, 110, 76, 206, 212, 49, 238, 59])
      ),
      0
    )
  ) {
    return EnergyTokenInstruction.MintGridTokens;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([17, 40, 71, 107, 142, 232, 163, 100])
      ),
      0
    )
  ) {
    return EnergyTokenInstruction.MintToWallet;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([13, 246, 31, 237, 99, 19, 88, 226])
      ),
      0
    )
  ) {
    return EnergyTokenInstruction.MintTokensDirect;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([54, 180, 238, 175, 74, 85, 126, 188])
      ),
      0
    )
  ) {
    return EnergyTokenInstruction.TransferTokens;
  }
  throw new Error(
    'The provided instruction could not be identified as a energyToken instruction.'
  );
}

export type ParsedEnergyTokenInstruction<
  TProgram extends string = '94G1r674LmRDmLN2UPjDFD8Eh7zT8JaSaxv9v68GyEur',
> =
  | ({
      instructionType: EnergyTokenInstruction.AddRecValidator;
    } & ParsedAddRecValidatorInstruction<TProgram>)
  | ({
      instructionType: EnergyTokenInstruction.BurnTokens;
    } & ParsedBurnTokensInstruction<TProgram>)
  | ({
      instructionType: EnergyTokenInstruction.CreateTokenMint;
    } & ParsedCreateTokenMintInstruction<TProgram>)
  | ({
      instructionType: EnergyTokenInstruction.Initialize;
    } & ParsedInitializeInstruction<TProgram>)
  | ({
      instructionType: EnergyTokenInstruction.InitializeToken;
    } & ParsedInitializeTokenInstruction<TProgram>)
  | ({
      instructionType: EnergyTokenInstruction.MintGridTokens;
    } & ParsedMintGridTokensInstruction<TProgram>)
  | ({
      instructionType: EnergyTokenInstruction.MintToWallet;
    } & ParsedMintToWalletInstruction<TProgram>)
  | ({
      instructionType: EnergyTokenInstruction.MintTokensDirect;
    } & ParsedMintTokensDirectInstruction<TProgram>)
  | ({
      instructionType: EnergyTokenInstruction.TransferTokens;
    } & ParsedTransferTokensInstruction<TProgram>);
