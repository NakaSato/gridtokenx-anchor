/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { TRADING_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CREATE_SELL_ORDER_DISCRIMINATOR = new Uint8Array([
  53, 52, 255, 44, 191, 74, 171, 225,
]);

export function getCreateSellOrderDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_SELL_ORDER_DISCRIMINATOR
  );
}

export type CreateSellOrderInstruction<
  TProgram extends string = typeof TRADING_PROGRAM_ADDRESS,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountOrder extends string | AccountMeta<string> = string,
  TAccountErcCertificate extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountOrder extends string
        ? WritableAccount<TAccountOrder>
        : TAccountOrder,
      TAccountErcCertificate extends string
        ? ReadonlyAccount<TAccountErcCertificate>
        : TAccountErcCertificate,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateSellOrderInstructionData = {
  discriminator: ReadonlyUint8Array;
  energyAmount: bigint;
  pricePerKwh: bigint;
};

export type CreateSellOrderInstructionDataArgs = {
  energyAmount: number | bigint;
  pricePerKwh: number | bigint;
};

export function getCreateSellOrderInstructionDataEncoder(): FixedSizeEncoder<CreateSellOrderInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['energyAmount', getU64Encoder()],
      ['pricePerKwh', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_SELL_ORDER_DISCRIMINATOR })
  );
}

export function getCreateSellOrderInstructionDataDecoder(): FixedSizeDecoder<CreateSellOrderInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['energyAmount', getU64Decoder()],
    ['pricePerKwh', getU64Decoder()],
  ]);
}

export function getCreateSellOrderInstructionDataCodec(): FixedSizeCodec<
  CreateSellOrderInstructionDataArgs,
  CreateSellOrderInstructionData
> {
  return combineCodec(
    getCreateSellOrderInstructionDataEncoder(),
    getCreateSellOrderInstructionDataDecoder()
  );
}

export type CreateSellOrderInput<
  TAccountMarket extends string = string,
  TAccountOrder extends string = string,
  TAccountErcCertificate extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  market: Address<TAccountMarket>;
  order: Address<TAccountOrder>;
  /**
   * Optional: ERC certificate for prosumers
   * When provided, validates the seller has certified renewable energy
   */
  ercCertificate?: Address<TAccountErcCertificate>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  energyAmount: CreateSellOrderInstructionDataArgs['energyAmount'];
  pricePerKwh: CreateSellOrderInstructionDataArgs['pricePerKwh'];
};

export function getCreateSellOrderInstruction<
  TAccountMarket extends string,
  TAccountOrder extends string,
  TAccountErcCertificate extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRADING_PROGRAM_ADDRESS,
>(
  input: CreateSellOrderInput<
    TAccountMarket,
    TAccountOrder,
    TAccountErcCertificate,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CreateSellOrderInstruction<
  TProgramAddress,
  TAccountMarket,
  TAccountOrder,
  TAccountErcCertificate,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? TRADING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    market: { value: input.market ?? null, isWritable: true },
    order: { value: input.order ?? null, isWritable: true },
    ercCertificate: { value: input.ercCertificate ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.order),
      getAccountMeta(accounts.ercCertificate),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateSellOrderInstructionDataEncoder().encode(
      args as CreateSellOrderInstructionDataArgs
    ),
    programAddress,
  } as CreateSellOrderInstruction<
    TProgramAddress,
    TAccountMarket,
    TAccountOrder,
    TAccountErcCertificate,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedCreateSellOrderInstruction<
  TProgram extends string = typeof TRADING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    market: TAccountMetas[0];
    order: TAccountMetas[1];
    /**
     * Optional: ERC certificate for prosumers
     * When provided, validates the seller has certified renewable energy
     */
    ercCertificate?: TAccountMetas[2] | undefined;
    authority: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CreateSellOrderInstructionData;
};

export function parseCreateSellOrderInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreateSellOrderInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === TRADING_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      market: getNextAccount(),
      order: getNextAccount(),
      ercCertificate: getNextOptionalAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateSellOrderInstructionDataDecoder().decode(instruction.data),
  };
}
