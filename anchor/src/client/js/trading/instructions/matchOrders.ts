/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { TRADING_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const MATCH_ORDERS_DISCRIMINATOR = new Uint8Array([
  17, 1, 201, 93, 7, 51, 251, 134,
]);

export function getMatchOrdersDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MATCH_ORDERS_DISCRIMINATOR
  );
}

export type MatchOrdersInstruction<
  TProgram extends string = typeof TRADING_PROGRAM_ADDRESS,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountBuyOrder extends string | AccountMeta<string> = string,
  TAccountSellOrder extends string | AccountMeta<string> = string,
  TAccountTradeRecord extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountBuyOrder extends string
        ? WritableAccount<TAccountBuyOrder>
        : TAccountBuyOrder,
      TAccountSellOrder extends string
        ? WritableAccount<TAccountSellOrder>
        : TAccountSellOrder,
      TAccountTradeRecord extends string
        ? WritableAccount<TAccountTradeRecord>
        : TAccountTradeRecord,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type MatchOrdersInstructionData = {
  discriminator: ReadonlyUint8Array;
  matchAmount: bigint;
};

export type MatchOrdersInstructionDataArgs = { matchAmount: number | bigint };

export function getMatchOrdersInstructionDataEncoder(): FixedSizeEncoder<MatchOrdersInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['matchAmount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: MATCH_ORDERS_DISCRIMINATOR })
  );
}

export function getMatchOrdersInstructionDataDecoder(): FixedSizeDecoder<MatchOrdersInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['matchAmount', getU64Decoder()],
  ]);
}

export function getMatchOrdersInstructionDataCodec(): FixedSizeCodec<
  MatchOrdersInstructionDataArgs,
  MatchOrdersInstructionData
> {
  return combineCodec(
    getMatchOrdersInstructionDataEncoder(),
    getMatchOrdersInstructionDataDecoder()
  );
}

export type MatchOrdersAsyncInput<
  TAccountMarket extends string = string,
  TAccountBuyOrder extends string = string,
  TAccountSellOrder extends string = string,
  TAccountTradeRecord extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  market: Address<TAccountMarket>;
  buyOrder: Address<TAccountBuyOrder>;
  sellOrder: Address<TAccountSellOrder>;
  tradeRecord?: Address<TAccountTradeRecord>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  matchAmount: MatchOrdersInstructionDataArgs['matchAmount'];
};

export async function getMatchOrdersInstructionAsync<
  TAccountMarket extends string,
  TAccountBuyOrder extends string,
  TAccountSellOrder extends string,
  TAccountTradeRecord extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRADING_PROGRAM_ADDRESS,
>(
  input: MatchOrdersAsyncInput<
    TAccountMarket,
    TAccountBuyOrder,
    TAccountSellOrder,
    TAccountTradeRecord,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  MatchOrdersInstruction<
    TProgramAddress,
    TAccountMarket,
    TAccountBuyOrder,
    TAccountSellOrder,
    TAccountTradeRecord,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? TRADING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    market: { value: input.market ?? null, isWritable: true },
    buyOrder: { value: input.buyOrder ?? null, isWritable: true },
    sellOrder: { value: input.sellOrder ?? null, isWritable: true },
    tradeRecord: { value: input.tradeRecord ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tradeRecord.value) {
    accounts.tradeRecord.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([116, 114, 97, 100, 101])),
        getAddressEncoder().encode(expectAddress(accounts.buyOrder.value)),
        getAddressEncoder().encode(expectAddress(accounts.sellOrder.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.buyOrder),
      getAccountMeta(accounts.sellOrder),
      getAccountMeta(accounts.tradeRecord),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getMatchOrdersInstructionDataEncoder().encode(
      args as MatchOrdersInstructionDataArgs
    ),
    programAddress,
  } as MatchOrdersInstruction<
    TProgramAddress,
    TAccountMarket,
    TAccountBuyOrder,
    TAccountSellOrder,
    TAccountTradeRecord,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type MatchOrdersInput<
  TAccountMarket extends string = string,
  TAccountBuyOrder extends string = string,
  TAccountSellOrder extends string = string,
  TAccountTradeRecord extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  market: Address<TAccountMarket>;
  buyOrder: Address<TAccountBuyOrder>;
  sellOrder: Address<TAccountSellOrder>;
  tradeRecord: Address<TAccountTradeRecord>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  matchAmount: MatchOrdersInstructionDataArgs['matchAmount'];
};

export function getMatchOrdersInstruction<
  TAccountMarket extends string,
  TAccountBuyOrder extends string,
  TAccountSellOrder extends string,
  TAccountTradeRecord extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRADING_PROGRAM_ADDRESS,
>(
  input: MatchOrdersInput<
    TAccountMarket,
    TAccountBuyOrder,
    TAccountSellOrder,
    TAccountTradeRecord,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): MatchOrdersInstruction<
  TProgramAddress,
  TAccountMarket,
  TAccountBuyOrder,
  TAccountSellOrder,
  TAccountTradeRecord,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? TRADING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    market: { value: input.market ?? null, isWritable: true },
    buyOrder: { value: input.buyOrder ?? null, isWritable: true },
    sellOrder: { value: input.sellOrder ?? null, isWritable: true },
    tradeRecord: { value: input.tradeRecord ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.buyOrder),
      getAccountMeta(accounts.sellOrder),
      getAccountMeta(accounts.tradeRecord),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getMatchOrdersInstructionDataEncoder().encode(
      args as MatchOrdersInstructionDataArgs
    ),
    programAddress,
  } as MatchOrdersInstruction<
    TProgramAddress,
    TAccountMarket,
    TAccountBuyOrder,
    TAccountSellOrder,
    TAccountTradeRecord,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedMatchOrdersInstruction<
  TProgram extends string = typeof TRADING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    market: TAccountMetas[0];
    buyOrder: TAccountMetas[1];
    sellOrder: TAccountMetas[2];
    tradeRecord: TAccountMetas[3];
    authority: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: MatchOrdersInstructionData;
};

export function parseMatchOrdersInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedMatchOrdersInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      market: getNextAccount(),
      buyOrder: getNextAccount(),
      sellOrder: getNextAccount(),
      tradeRecord: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getMatchOrdersInstructionDataDecoder().decode(instruction.data),
  };
}
