/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { TRADING_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CREATE_BUY_ORDER_DISCRIMINATOR = new Uint8Array([
  182, 87, 0, 160, 192, 66, 151, 130,
]);

export function getCreateBuyOrderDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_BUY_ORDER_DISCRIMINATOR
  );
}

export type CreateBuyOrderInstruction<
  TProgram extends string = typeof TRADING_PROGRAM_ADDRESS,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountOrder extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountOrder extends string
        ? WritableAccount<TAccountOrder>
        : TAccountOrder,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateBuyOrderInstructionData = {
  discriminator: ReadonlyUint8Array;
  energyAmount: bigint;
  maxPricePerKwh: bigint;
};

export type CreateBuyOrderInstructionDataArgs = {
  energyAmount: number | bigint;
  maxPricePerKwh: number | bigint;
};

export function getCreateBuyOrderInstructionDataEncoder(): FixedSizeEncoder<CreateBuyOrderInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['energyAmount', getU64Encoder()],
      ['maxPricePerKwh', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_BUY_ORDER_DISCRIMINATOR })
  );
}

export function getCreateBuyOrderInstructionDataDecoder(): FixedSizeDecoder<CreateBuyOrderInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['energyAmount', getU64Decoder()],
    ['maxPricePerKwh', getU64Decoder()],
  ]);
}

export function getCreateBuyOrderInstructionDataCodec(): FixedSizeCodec<
  CreateBuyOrderInstructionDataArgs,
  CreateBuyOrderInstructionData
> {
  return combineCodec(
    getCreateBuyOrderInstructionDataEncoder(),
    getCreateBuyOrderInstructionDataDecoder()
  );
}

export type CreateBuyOrderInput<
  TAccountMarket extends string = string,
  TAccountOrder extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  market: Address<TAccountMarket>;
  order: Address<TAccountOrder>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  energyAmount: CreateBuyOrderInstructionDataArgs['energyAmount'];
  maxPricePerKwh: CreateBuyOrderInstructionDataArgs['maxPricePerKwh'];
};

export function getCreateBuyOrderInstruction<
  TAccountMarket extends string,
  TAccountOrder extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRADING_PROGRAM_ADDRESS,
>(
  input: CreateBuyOrderInput<
    TAccountMarket,
    TAccountOrder,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CreateBuyOrderInstruction<
  TProgramAddress,
  TAccountMarket,
  TAccountOrder,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? TRADING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    market: { value: input.market ?? null, isWritable: true },
    order: { value: input.order ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.order),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateBuyOrderInstructionDataEncoder().encode(
      args as CreateBuyOrderInstructionDataArgs
    ),
    programAddress,
  } as CreateBuyOrderInstruction<
    TProgramAddress,
    TAccountMarket,
    TAccountOrder,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedCreateBuyOrderInstruction<
  TProgram extends string = typeof TRADING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    market: TAccountMetas[0];
    order: TAccountMetas[1];
    authority: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CreateBuyOrderInstructionData;
};

export function parseCreateBuyOrderInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreateBuyOrderInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      market: getNextAccount(),
      order: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateBuyOrderInstructionDataDecoder().decode(instruction.data),
  };
}
