/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { ORACLE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_API_GATEWAY_DISCRIMINATOR = new Uint8Array([
  66, 69, 252, 242, 127, 168, 42, 112,
]);

export function getUpdateApiGatewayDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_API_GATEWAY_DISCRIMINATOR
  );
}

export type UpdateApiGatewayInstruction<
  TProgram extends string = typeof ORACLE_PROGRAM_ADDRESS,
  TAccountOracleData extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOracleData extends string
        ? WritableAccount<TAccountOracleData>
        : TAccountOracleData,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateApiGatewayInstructionData = {
  discriminator: ReadonlyUint8Array;
  newApiGateway: Address;
};

export type UpdateApiGatewayInstructionDataArgs = { newApiGateway: Address };

export function getUpdateApiGatewayInstructionDataEncoder(): FixedSizeEncoder<UpdateApiGatewayInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newApiGateway', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_API_GATEWAY_DISCRIMINATOR })
  );
}

export function getUpdateApiGatewayInstructionDataDecoder(): FixedSizeDecoder<UpdateApiGatewayInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newApiGateway', getAddressDecoder()],
  ]);
}

export function getUpdateApiGatewayInstructionDataCodec(): FixedSizeCodec<
  UpdateApiGatewayInstructionDataArgs,
  UpdateApiGatewayInstructionData
> {
  return combineCodec(
    getUpdateApiGatewayInstructionDataEncoder(),
    getUpdateApiGatewayInstructionDataDecoder()
  );
}

export type UpdateApiGatewayInput<
  TAccountOracleData extends string = string,
  TAccountAuthority extends string = string,
> = {
  oracleData: Address<TAccountOracleData>;
  authority: TransactionSigner<TAccountAuthority>;
  newApiGateway: UpdateApiGatewayInstructionDataArgs['newApiGateway'];
};

export function getUpdateApiGatewayInstruction<
  TAccountOracleData extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof ORACLE_PROGRAM_ADDRESS,
>(
  input: UpdateApiGatewayInput<TAccountOracleData, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): UpdateApiGatewayInstruction<
  TProgramAddress,
  TAccountOracleData,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    oracleData: { value: input.oracleData ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.oracleData),
      getAccountMeta(accounts.authority),
    ],
    data: getUpdateApiGatewayInstructionDataEncoder().encode(
      args as UpdateApiGatewayInstructionDataArgs
    ),
    programAddress,
  } as UpdateApiGatewayInstruction<
    TProgramAddress,
    TAccountOracleData,
    TAccountAuthority
  >);
}

export type ParsedUpdateApiGatewayInstruction<
  TProgram extends string = typeof ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    oracleData: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: UpdateApiGatewayInstructionData;
};

export function parseUpdateApiGatewayInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateApiGatewayInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { oracleData: getNextAccount(), authority: getNextAccount() },
    data: getUpdateApiGatewayInstructionDataDecoder().decode(instruction.data),
  };
}
