/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { ORACLE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const TRIGGER_MARKET_CLEARING_DISCRIMINATOR = new Uint8Array([
  180, 116, 162, 167, 37, 28, 78, 159,
]);

export function getTriggerMarketClearingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TRIGGER_MARKET_CLEARING_DISCRIMINATOR
  );
}

export type TriggerMarketClearingInstruction<
  TProgram extends string = typeof ORACLE_PROGRAM_ADDRESS,
  TAccountOracleData extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOracleData extends string
        ? WritableAccount<TAccountOracleData>
        : TAccountOracleData,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type TriggerMarketClearingInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type TriggerMarketClearingInstructionDataArgs = {};

export function getTriggerMarketClearingInstructionDataEncoder(): FixedSizeEncoder<TriggerMarketClearingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: TRIGGER_MARKET_CLEARING_DISCRIMINATOR,
    })
  );
}

export function getTriggerMarketClearingInstructionDataDecoder(): FixedSizeDecoder<TriggerMarketClearingInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getTriggerMarketClearingInstructionDataCodec(): FixedSizeCodec<
  TriggerMarketClearingInstructionDataArgs,
  TriggerMarketClearingInstructionData
> {
  return combineCodec(
    getTriggerMarketClearingInstructionDataEncoder(),
    getTriggerMarketClearingInstructionDataDecoder()
  );
}

export type TriggerMarketClearingInput<
  TAccountOracleData extends string = string,
  TAccountAuthority extends string = string,
> = {
  oracleData: Address<TAccountOracleData>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getTriggerMarketClearingInstruction<
  TAccountOracleData extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof ORACLE_PROGRAM_ADDRESS,
>(
  input: TriggerMarketClearingInput<TAccountOracleData, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): TriggerMarketClearingInstruction<
  TProgramAddress,
  TAccountOracleData,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    oracleData: { value: input.oracleData ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.oracleData),
      getAccountMeta(accounts.authority),
    ],
    data: getTriggerMarketClearingInstructionDataEncoder().encode({}),
    programAddress,
  } as TriggerMarketClearingInstruction<
    TProgramAddress,
    TAccountOracleData,
    TAccountAuthority
  >);
}

export type ParsedTriggerMarketClearingInstruction<
  TProgram extends string = typeof ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    oracleData: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: TriggerMarketClearingInstructionData;
};

export function parseTriggerMarketClearingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedTriggerMarketClearingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { oracleData: getNextAccount(), authority: getNextAccount() },
    data: getTriggerMarketClearingInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
