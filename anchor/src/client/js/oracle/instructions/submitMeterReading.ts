/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { ORACLE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SUBMIT_METER_READING_DISCRIMINATOR = new Uint8Array([
  181, 247, 196, 139, 78, 88, 192, 206,
]);

export function getSubmitMeterReadingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SUBMIT_METER_READING_DISCRIMINATOR
  );
}

export type SubmitMeterReadingInstruction<
  TProgram extends string = typeof ORACLE_PROGRAM_ADDRESS,
  TAccountOracleData extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOracleData extends string
        ? WritableAccount<TAccountOracleData>
        : TAccountOracleData,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type SubmitMeterReadingInstructionData = {
  discriminator: ReadonlyUint8Array;
  meterId: string;
  energyProduced: bigint;
  energyConsumed: bigint;
  readingTimestamp: bigint;
};

export type SubmitMeterReadingInstructionDataArgs = {
  meterId: string;
  energyProduced: number | bigint;
  energyConsumed: number | bigint;
  readingTimestamp: number | bigint;
};

export function getSubmitMeterReadingInstructionDataEncoder(): Encoder<SubmitMeterReadingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['meterId', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['energyProduced', getU64Encoder()],
      ['energyConsumed', getU64Encoder()],
      ['readingTimestamp', getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SUBMIT_METER_READING_DISCRIMINATOR })
  );
}

export function getSubmitMeterReadingInstructionDataDecoder(): Decoder<SubmitMeterReadingInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['meterId', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['energyProduced', getU64Decoder()],
    ['energyConsumed', getU64Decoder()],
    ['readingTimestamp', getI64Decoder()],
  ]);
}

export function getSubmitMeterReadingInstructionDataCodec(): Codec<
  SubmitMeterReadingInstructionDataArgs,
  SubmitMeterReadingInstructionData
> {
  return combineCodec(
    getSubmitMeterReadingInstructionDataEncoder(),
    getSubmitMeterReadingInstructionDataDecoder()
  );
}

export type SubmitMeterReadingInput<
  TAccountOracleData extends string = string,
  TAccountAuthority extends string = string,
> = {
  oracleData: Address<TAccountOracleData>;
  authority: TransactionSigner<TAccountAuthority>;
  meterId: SubmitMeterReadingInstructionDataArgs['meterId'];
  energyProduced: SubmitMeterReadingInstructionDataArgs['energyProduced'];
  energyConsumed: SubmitMeterReadingInstructionDataArgs['energyConsumed'];
  readingTimestamp: SubmitMeterReadingInstructionDataArgs['readingTimestamp'];
};

export function getSubmitMeterReadingInstruction<
  TAccountOracleData extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof ORACLE_PROGRAM_ADDRESS,
>(
  input: SubmitMeterReadingInput<TAccountOracleData, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): SubmitMeterReadingInstruction<
  TProgramAddress,
  TAccountOracleData,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    oracleData: { value: input.oracleData ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.oracleData),
      getAccountMeta(accounts.authority),
    ],
    data: getSubmitMeterReadingInstructionDataEncoder().encode(
      args as SubmitMeterReadingInstructionDataArgs
    ),
    programAddress,
  } as SubmitMeterReadingInstruction<
    TProgramAddress,
    TAccountOracleData,
    TAccountAuthority
  >);
}

export type ParsedSubmitMeterReadingInstruction<
  TProgram extends string = typeof ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    oracleData: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: SubmitMeterReadingInstructionData;
};

export function parseSubmitMeterReadingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSubmitMeterReadingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { oracleData: getNextAccount(), authority: getNextAccount() },
    data: getSubmitMeterReadingInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
