/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { REGISTRY_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_METER_READING_DISCRIMINATOR = new Uint8Array([
  192, 220, 135, 23, 89, 22, 163, 130,
]);

export function getUpdateMeterReadingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_METER_READING_DISCRIMINATOR
  );
}

export type UpdateMeterReadingInstruction<
  TProgram extends string = typeof REGISTRY_PROGRAM_ADDRESS,
  TAccountMeterAccount extends string | AccountMeta<string> = string,
  TAccountOracleAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMeterAccount extends string
        ? WritableAccount<TAccountMeterAccount>
        : TAccountMeterAccount,
      TAccountOracleAuthority extends string
        ? ReadonlySignerAccount<TAccountOracleAuthority> &
            AccountSignerMeta<TAccountOracleAuthority>
        : TAccountOracleAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateMeterReadingInstructionData = {
  discriminator: ReadonlyUint8Array;
  energyGenerated: bigint;
  energyConsumed: bigint;
  readingTimestamp: bigint;
};

export type UpdateMeterReadingInstructionDataArgs = {
  energyGenerated: number | bigint;
  energyConsumed: number | bigint;
  readingTimestamp: number | bigint;
};

export function getUpdateMeterReadingInstructionDataEncoder(): FixedSizeEncoder<UpdateMeterReadingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['energyGenerated', getU64Encoder()],
      ['energyConsumed', getU64Encoder()],
      ['readingTimestamp', getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_METER_READING_DISCRIMINATOR })
  );
}

export function getUpdateMeterReadingInstructionDataDecoder(): FixedSizeDecoder<UpdateMeterReadingInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['energyGenerated', getU64Decoder()],
    ['energyConsumed', getU64Decoder()],
    ['readingTimestamp', getI64Decoder()],
  ]);
}

export function getUpdateMeterReadingInstructionDataCodec(): FixedSizeCodec<
  UpdateMeterReadingInstructionDataArgs,
  UpdateMeterReadingInstructionData
> {
  return combineCodec(
    getUpdateMeterReadingInstructionDataEncoder(),
    getUpdateMeterReadingInstructionDataDecoder()
  );
}

export type UpdateMeterReadingInput<
  TAccountMeterAccount extends string = string,
  TAccountOracleAuthority extends string = string,
> = {
  meterAccount: Address<TAccountMeterAccount>;
  oracleAuthority: TransactionSigner<TAccountOracleAuthority>;
  energyGenerated: UpdateMeterReadingInstructionDataArgs['energyGenerated'];
  energyConsumed: UpdateMeterReadingInstructionDataArgs['energyConsumed'];
  readingTimestamp: UpdateMeterReadingInstructionDataArgs['readingTimestamp'];
};

export function getUpdateMeterReadingInstruction<
  TAccountMeterAccount extends string,
  TAccountOracleAuthority extends string,
  TProgramAddress extends Address = typeof REGISTRY_PROGRAM_ADDRESS,
>(
  input: UpdateMeterReadingInput<TAccountMeterAccount, TAccountOracleAuthority>,
  config?: { programAddress?: TProgramAddress }
): UpdateMeterReadingInstruction<
  TProgramAddress,
  TAccountMeterAccount,
  TAccountOracleAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    meterAccount: { value: input.meterAccount ?? null, isWritable: true },
    oracleAuthority: {
      value: input.oracleAuthority ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.meterAccount),
      getAccountMeta(accounts.oracleAuthority),
    ],
    data: getUpdateMeterReadingInstructionDataEncoder().encode(
      args as UpdateMeterReadingInstructionDataArgs
    ),
    programAddress,
  } as UpdateMeterReadingInstruction<
    TProgramAddress,
    TAccountMeterAccount,
    TAccountOracleAuthority
  >);
}

export type ParsedUpdateMeterReadingInstruction<
  TProgram extends string = typeof REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    meterAccount: TAccountMetas[0];
    oracleAuthority: TAccountMetas[1];
  };
  data: UpdateMeterReadingInstructionData;
};

export function parseUpdateMeterReadingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateMeterReadingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      meterAccount: getNextAccount(),
      oracleAuthority: getNextAccount(),
    },
    data: getUpdateMeterReadingInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
