/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';
import { REGISTRY_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const GET_UNSETTLED_BALANCE_DISCRIMINATOR = new Uint8Array([
  178, 120, 95, 9, 18, 81, 181, 83,
]);

export function getGetUnsettledBalanceDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GET_UNSETTLED_BALANCE_DISCRIMINATOR
  );
}

export type GetUnsettledBalanceInstruction<
  TProgram extends string = typeof REGISTRY_PROGRAM_ADDRESS,
  TAccountMeterAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMeterAccount extends string
        ? ReadonlyAccount<TAccountMeterAccount>
        : TAccountMeterAccount,
      ...TRemainingAccounts,
    ]
  >;

export type GetUnsettledBalanceInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type GetUnsettledBalanceInstructionDataArgs = {};

export function getGetUnsettledBalanceInstructionDataEncoder(): FixedSizeEncoder<GetUnsettledBalanceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: GET_UNSETTLED_BALANCE_DISCRIMINATOR,
    })
  );
}

export function getGetUnsettledBalanceInstructionDataDecoder(): FixedSizeDecoder<GetUnsettledBalanceInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getGetUnsettledBalanceInstructionDataCodec(): FixedSizeCodec<
  GetUnsettledBalanceInstructionDataArgs,
  GetUnsettledBalanceInstructionData
> {
  return combineCodec(
    getGetUnsettledBalanceInstructionDataEncoder(),
    getGetUnsettledBalanceInstructionDataDecoder()
  );
}

export type GetUnsettledBalanceInput<
  TAccountMeterAccount extends string = string,
> = {
  meterAccount: Address<TAccountMeterAccount>;
};

export function getGetUnsettledBalanceInstruction<
  TAccountMeterAccount extends string,
  TProgramAddress extends Address = typeof REGISTRY_PROGRAM_ADDRESS,
>(
  input: GetUnsettledBalanceInput<TAccountMeterAccount>,
  config?: { programAddress?: TProgramAddress }
): GetUnsettledBalanceInstruction<TProgramAddress, TAccountMeterAccount> {
  // Program address.
  const programAddress = config?.programAddress ?? REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    meterAccount: { value: input.meterAccount ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.meterAccount)],
    data: getGetUnsettledBalanceInstructionDataEncoder().encode({}),
    programAddress,
  } as GetUnsettledBalanceInstruction<TProgramAddress, TAccountMeterAccount>);
}

export type ParsedGetUnsettledBalanceInstruction<
  TProgram extends string = typeof REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    meterAccount: TAccountMetas[0];
  };
  data: GetUnsettledBalanceInstructionData;
};

export function parseGetUnsettledBalanceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedGetUnsettledBalanceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { meterAccount: getNextAccount() },
    data: getGetUnsettledBalanceInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
