/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { REGISTRY_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getUserStatusDecoder,
  getUserStatusEncoder,
  type UserStatus,
  type UserStatusArgs,
} from '../types';

export const UPDATE_USER_STATUS_DISCRIMINATOR = new Uint8Array([
  4, 129, 231, 220, 216, 44, 151, 55,
]);

export function getUpdateUserStatusDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_USER_STATUS_DISCRIMINATOR
  );
}

export type UpdateUserStatusInstruction<
  TProgram extends string = typeof REGISTRY_PROGRAM_ADDRESS,
  TAccountRegistry extends string | AccountMeta<string> = string,
  TAccountUserAccount extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRegistry extends string
        ? ReadonlyAccount<TAccountRegistry>
        : TAccountRegistry,
      TAccountUserAccount extends string
        ? WritableAccount<TAccountUserAccount>
        : TAccountUserAccount,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateUserStatusInstructionData = {
  discriminator: ReadonlyUint8Array;
  newStatus: UserStatus;
};

export type UpdateUserStatusInstructionDataArgs = { newStatus: UserStatusArgs };

export function getUpdateUserStatusInstructionDataEncoder(): FixedSizeEncoder<UpdateUserStatusInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newStatus', getUserStatusEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_USER_STATUS_DISCRIMINATOR })
  );
}

export function getUpdateUserStatusInstructionDataDecoder(): FixedSizeDecoder<UpdateUserStatusInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newStatus', getUserStatusDecoder()],
  ]);
}

export function getUpdateUserStatusInstructionDataCodec(): FixedSizeCodec<
  UpdateUserStatusInstructionDataArgs,
  UpdateUserStatusInstructionData
> {
  return combineCodec(
    getUpdateUserStatusInstructionDataEncoder(),
    getUpdateUserStatusInstructionDataDecoder()
  );
}

export type UpdateUserStatusInput<
  TAccountRegistry extends string = string,
  TAccountUserAccount extends string = string,
  TAccountAuthority extends string = string,
> = {
  registry: Address<TAccountRegistry>;
  userAccount: Address<TAccountUserAccount>;
  authority: TransactionSigner<TAccountAuthority>;
  newStatus: UpdateUserStatusInstructionDataArgs['newStatus'];
};

export function getUpdateUserStatusInstruction<
  TAccountRegistry extends string,
  TAccountUserAccount extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof REGISTRY_PROGRAM_ADDRESS,
>(
  input: UpdateUserStatusInput<
    TAccountRegistry,
    TAccountUserAccount,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress }
): UpdateUserStatusInstruction<
  TProgramAddress,
  TAccountRegistry,
  TAccountUserAccount,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    registry: { value: input.registry ?? null, isWritable: false },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.registry),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.authority),
    ],
    data: getUpdateUserStatusInstructionDataEncoder().encode(
      args as UpdateUserStatusInstructionDataArgs
    ),
    programAddress,
  } as UpdateUserStatusInstruction<
    TProgramAddress,
    TAccountRegistry,
    TAccountUserAccount,
    TAccountAuthority
  >);
}

export type ParsedUpdateUserStatusInstruction<
  TProgram extends string = typeof REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    registry: TAccountMetas[0];
    userAccount: TAccountMetas[1];
    authority: TAccountMetas[2];
  };
  data: UpdateUserStatusInstructionData;
};

export function parseUpdateUserStatusInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateUserStatusInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      registry: getNextAccount(),
      userAccount: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getUpdateUserStatusInstructionDataDecoder().decode(instruction.data),
  };
}
