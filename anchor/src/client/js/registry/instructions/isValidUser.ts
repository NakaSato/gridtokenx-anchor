/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';
import { REGISTRY_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const IS_VALID_USER_DISCRIMINATOR = new Uint8Array([
  204, 118, 77, 53, 39, 63, 237, 250,
]);

export function getIsValidUserDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    IS_VALID_USER_DISCRIMINATOR
  );
}

export type IsValidUserInstruction<
  TProgram extends string = typeof REGISTRY_PROGRAM_ADDRESS,
  TAccountUserAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUserAccount extends string
        ? ReadonlyAccount<TAccountUserAccount>
        : TAccountUserAccount,
      ...TRemainingAccounts,
    ]
  >;

export type IsValidUserInstructionData = { discriminator: ReadonlyUint8Array };

export type IsValidUserInstructionDataArgs = {};

export function getIsValidUserInstructionDataEncoder(): FixedSizeEncoder<IsValidUserInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: IS_VALID_USER_DISCRIMINATOR })
  );
}

export function getIsValidUserInstructionDataDecoder(): FixedSizeDecoder<IsValidUserInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getIsValidUserInstructionDataCodec(): FixedSizeCodec<
  IsValidUserInstructionDataArgs,
  IsValidUserInstructionData
> {
  return combineCodec(
    getIsValidUserInstructionDataEncoder(),
    getIsValidUserInstructionDataDecoder()
  );
}

export type IsValidUserInput<TAccountUserAccount extends string = string> = {
  userAccount: Address<TAccountUserAccount>;
};

export function getIsValidUserInstruction<
  TAccountUserAccount extends string,
  TProgramAddress extends Address = typeof REGISTRY_PROGRAM_ADDRESS,
>(
  input: IsValidUserInput<TAccountUserAccount>,
  config?: { programAddress?: TProgramAddress }
): IsValidUserInstruction<TProgramAddress, TAccountUserAccount> {
  // Program address.
  const programAddress = config?.programAddress ?? REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    userAccount: { value: input.userAccount ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.userAccount)],
    data: getIsValidUserInstructionDataEncoder().encode({}),
    programAddress,
  } as IsValidUserInstruction<TProgramAddress, TAccountUserAccount>);
}

export type ParsedIsValidUserInstruction<
  TProgram extends string = typeof REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    userAccount: TAccountMetas[0];
  };
  data: IsValidUserInstructionData;
};

export function parseIsValidUserInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedIsValidUserInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { userAccount: getNextAccount() },
    data: getIsValidUserInstructionDataDecoder().decode(instruction.data),
  };
}
