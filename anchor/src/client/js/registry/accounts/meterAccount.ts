/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';
import {
  getMeterStatusDecoder,
  getMeterStatusEncoder,
  getMeterTypeDecoder,
  getMeterTypeEncoder,
  type MeterStatus,
  type MeterStatusArgs,
  type MeterType,
  type MeterTypeArgs,
} from '../types';

export const METER_ACCOUNT_DISCRIMINATOR = new Uint8Array([
  87, 111, 139, 87, 181, 20, 104, 255,
]);

export function getMeterAccountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    METER_ACCOUNT_DISCRIMINATOR
  );
}

export type MeterAccount = {
  discriminator: ReadonlyUint8Array;
  meterId: string;
  owner: Address;
  meterType: MeterType;
  status: MeterStatus;
  registeredAt: bigint;
  lastReadingAt: bigint;
  totalGeneration: bigint;
  totalConsumption: bigint;
  settledNetGeneration: bigint;
  claimedErcGeneration: bigint;
};

export type MeterAccountArgs = {
  meterId: string;
  owner: Address;
  meterType: MeterTypeArgs;
  status: MeterStatusArgs;
  registeredAt: number | bigint;
  lastReadingAt: number | bigint;
  totalGeneration: number | bigint;
  totalConsumption: number | bigint;
  settledNetGeneration: number | bigint;
  claimedErcGeneration: number | bigint;
};

/** Gets the encoder for {@link MeterAccountArgs} account data. */
export function getMeterAccountEncoder(): Encoder<MeterAccountArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['meterId', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['owner', getAddressEncoder()],
      ['meterType', getMeterTypeEncoder()],
      ['status', getMeterStatusEncoder()],
      ['registeredAt', getI64Encoder()],
      ['lastReadingAt', getI64Encoder()],
      ['totalGeneration', getU64Encoder()],
      ['totalConsumption', getU64Encoder()],
      ['settledNetGeneration', getU64Encoder()],
      ['claimedErcGeneration', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: METER_ACCOUNT_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link MeterAccount} account data. */
export function getMeterAccountDecoder(): Decoder<MeterAccount> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['meterId', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['owner', getAddressDecoder()],
    ['meterType', getMeterTypeDecoder()],
    ['status', getMeterStatusDecoder()],
    ['registeredAt', getI64Decoder()],
    ['lastReadingAt', getI64Decoder()],
    ['totalGeneration', getU64Decoder()],
    ['totalConsumption', getU64Decoder()],
    ['settledNetGeneration', getU64Decoder()],
    ['claimedErcGeneration', getU64Decoder()],
  ]);
}

/** Gets the codec for {@link MeterAccount} account data. */
export function getMeterAccountCodec(): Codec<MeterAccountArgs, MeterAccount> {
  return combineCodec(getMeterAccountEncoder(), getMeterAccountDecoder());
}

export function decodeMeterAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<MeterAccount, TAddress>;
export function decodeMeterAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<MeterAccount, TAddress>;
export function decodeMeterAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<MeterAccount, TAddress> | MaybeAccount<MeterAccount, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getMeterAccountDecoder()
  );
}

export async function fetchMeterAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<MeterAccount, TAddress>> {
  const maybeAccount = await fetchMaybeMeterAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeMeterAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<MeterAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeMeterAccount(maybeAccount);
}

export async function fetchAllMeterAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<MeterAccount>[]> {
  const maybeAccounts = await fetchAllMaybeMeterAccount(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeMeterAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<MeterAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeMeterAccount(maybeAccount));
}
