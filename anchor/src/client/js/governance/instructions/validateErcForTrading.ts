/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { GOVERNANCE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const VALIDATE_ERC_FOR_TRADING_DISCRIMINATOR = new Uint8Array([
  9, 215, 176, 63, 247, 150, 72, 139,
]);

export function getValidateErcForTradingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VALIDATE_ERC_FOR_TRADING_DISCRIMINATOR
  );
}

export type ValidateErcForTradingInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountPoaConfig extends string | AccountMeta<string> = string,
  TAccountErcCertificate extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPoaConfig extends string
        ? ReadonlyAccount<TAccountPoaConfig>
        : TAccountPoaConfig,
      TAccountErcCertificate extends string
        ? WritableAccount<TAccountErcCertificate>
        : TAccountErcCertificate,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type ValidateErcForTradingInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ValidateErcForTradingInstructionDataArgs = {};

export function getValidateErcForTradingInstructionDataEncoder(): FixedSizeEncoder<ValidateErcForTradingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: VALIDATE_ERC_FOR_TRADING_DISCRIMINATOR,
    })
  );
}

export function getValidateErcForTradingInstructionDataDecoder(): FixedSizeDecoder<ValidateErcForTradingInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getValidateErcForTradingInstructionDataCodec(): FixedSizeCodec<
  ValidateErcForTradingInstructionDataArgs,
  ValidateErcForTradingInstructionData
> {
  return combineCodec(
    getValidateErcForTradingInstructionDataEncoder(),
    getValidateErcForTradingInstructionDataDecoder()
  );
}

export type ValidateErcForTradingAsyncInput<
  TAccountPoaConfig extends string = string,
  TAccountErcCertificate extends string = string,
  TAccountAuthority extends string = string,
> = {
  poaConfig?: Address<TAccountPoaConfig>;
  ercCertificate: Address<TAccountErcCertificate>;
  authority: TransactionSigner<TAccountAuthority>;
};

export async function getValidateErcForTradingInstructionAsync<
  TAccountPoaConfig extends string,
  TAccountErcCertificate extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: ValidateErcForTradingAsyncInput<
    TAccountPoaConfig,
    TAccountErcCertificate,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ValidateErcForTradingInstruction<
    TProgramAddress,
    TAccountPoaConfig,
    TAccountErcCertificate,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    poaConfig: { value: input.poaConfig ?? null, isWritable: false },
    ercCertificate: { value: input.ercCertificate ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.poaConfig.value) {
    accounts.poaConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 111, 97, 95, 99, 111, 110, 102, 105, 103])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.poaConfig),
      getAccountMeta(accounts.ercCertificate),
      getAccountMeta(accounts.authority),
    ],
    data: getValidateErcForTradingInstructionDataEncoder().encode({}),
    programAddress,
  } as ValidateErcForTradingInstruction<
    TProgramAddress,
    TAccountPoaConfig,
    TAccountErcCertificate,
    TAccountAuthority
  >);
}

export type ValidateErcForTradingInput<
  TAccountPoaConfig extends string = string,
  TAccountErcCertificate extends string = string,
  TAccountAuthority extends string = string,
> = {
  poaConfig: Address<TAccountPoaConfig>;
  ercCertificate: Address<TAccountErcCertificate>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getValidateErcForTradingInstruction<
  TAccountPoaConfig extends string,
  TAccountErcCertificate extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: ValidateErcForTradingInput<
    TAccountPoaConfig,
    TAccountErcCertificate,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress }
): ValidateErcForTradingInstruction<
  TProgramAddress,
  TAccountPoaConfig,
  TAccountErcCertificate,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    poaConfig: { value: input.poaConfig ?? null, isWritable: false },
    ercCertificate: { value: input.ercCertificate ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.poaConfig),
      getAccountMeta(accounts.ercCertificate),
      getAccountMeta(accounts.authority),
    ],
    data: getValidateErcForTradingInstructionDataEncoder().encode({}),
    programAddress,
  } as ValidateErcForTradingInstruction<
    TProgramAddress,
    TAccountPoaConfig,
    TAccountErcCertificate,
    TAccountAuthority
  >);
}

export type ParsedValidateErcForTradingInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    poaConfig: TAccountMetas[0];
    ercCertificate: TAccountMetas[1];
    authority: TAccountMetas[2];
  };
  data: ValidateErcForTradingInstructionData;
};

export function parseValidateErcForTradingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedValidateErcForTradingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      poaConfig: getNextAccount(),
      ercCertificate: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getValidateErcForTradingInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
