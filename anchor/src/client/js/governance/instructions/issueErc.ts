/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { GOVERNANCE_PROGRAM_ADDRESS } from '../programs';
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const ISSUE_ERC_DISCRIMINATOR = new Uint8Array([
  174, 248, 149, 107, 155, 4, 196, 8,
]);

export function getIssueErcDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ISSUE_ERC_DISCRIMINATOR);
}

export type IssueErcInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountPoaConfig extends string | AccountMeta<string> = string,
  TAccountErcCertificate extends string | AccountMeta<string> = string,
  TAccountMeterAccount extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPoaConfig extends string
        ? ReadonlyAccount<TAccountPoaConfig>
        : TAccountPoaConfig,
      TAccountErcCertificate extends string
        ? WritableAccount<TAccountErcCertificate>
        : TAccountErcCertificate,
      TAccountMeterAccount extends string
        ? WritableAccount<TAccountMeterAccount>
        : TAccountMeterAccount,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type IssueErcInstructionData = {
  discriminator: ReadonlyUint8Array;
  certificateId: string;
  energyAmount: bigint;
  renewableSource: string;
  validationData: string;
};

export type IssueErcInstructionDataArgs = {
  certificateId: string;
  energyAmount: number | bigint;
  renewableSource: string;
  validationData: string;
};

export function getIssueErcInstructionDataEncoder(): Encoder<IssueErcInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      [
        'certificateId',
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
      ['energyAmount', getU64Encoder()],
      [
        'renewableSource',
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
      [
        'validationData',
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
    ]),
    (value) => ({ ...value, discriminator: ISSUE_ERC_DISCRIMINATOR })
  );
}

export function getIssueErcInstructionDataDecoder(): Decoder<IssueErcInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['certificateId', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['energyAmount', getU64Decoder()],
    [
      'renewableSource',
      addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()),
    ],
    ['validationData', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getIssueErcInstructionDataCodec(): Codec<
  IssueErcInstructionDataArgs,
  IssueErcInstructionData
> {
  return combineCodec(
    getIssueErcInstructionDataEncoder(),
    getIssueErcInstructionDataDecoder()
  );
}

export type IssueErcAsyncInput<
  TAccountPoaConfig extends string = string,
  TAccountErcCertificate extends string = string,
  TAccountMeterAccount extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  poaConfig?: Address<TAccountPoaConfig>;
  ercCertificate?: Address<TAccountErcCertificate>;
  /** Meter account from registry program - tracks claimed ERC generation */
  meterAccount: Address<TAccountMeterAccount>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  certificateId: IssueErcInstructionDataArgs['certificateId'];
  energyAmount: IssueErcInstructionDataArgs['energyAmount'];
  renewableSource: IssueErcInstructionDataArgs['renewableSource'];
  validationData: IssueErcInstructionDataArgs['validationData'];
};

export async function getIssueErcInstructionAsync<
  TAccountPoaConfig extends string,
  TAccountErcCertificate extends string,
  TAccountMeterAccount extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: IssueErcAsyncInput<
    TAccountPoaConfig,
    TAccountErcCertificate,
    TAccountMeterAccount,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  IssueErcInstruction<
    TProgramAddress,
    TAccountPoaConfig,
    TAccountErcCertificate,
    TAccountMeterAccount,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    poaConfig: { value: input.poaConfig ?? null, isWritable: false },
    ercCertificate: { value: input.ercCertificate ?? null, isWritable: true },
    meterAccount: { value: input.meterAccount ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.poaConfig.value) {
    accounts.poaConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 111, 97, 95, 99, 111, 110, 102, 105, 103])
        ),
      ],
    });
  }
  if (!accounts.ercCertificate.value) {
    accounts.ercCertificate.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            101, 114, 99, 95, 99, 101, 114, 116, 105, 102, 105, 99, 97, 116,
            101,
          ])
        ),
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()).encode(
          expectSome(args.certificateId)
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.poaConfig),
      getAccountMeta(accounts.ercCertificate),
      getAccountMeta(accounts.meterAccount),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getIssueErcInstructionDataEncoder().encode(
      args as IssueErcInstructionDataArgs
    ),
    programAddress,
  } as IssueErcInstruction<
    TProgramAddress,
    TAccountPoaConfig,
    TAccountErcCertificate,
    TAccountMeterAccount,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type IssueErcInput<
  TAccountPoaConfig extends string = string,
  TAccountErcCertificate extends string = string,
  TAccountMeterAccount extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  poaConfig: Address<TAccountPoaConfig>;
  ercCertificate: Address<TAccountErcCertificate>;
  /** Meter account from registry program - tracks claimed ERC generation */
  meterAccount: Address<TAccountMeterAccount>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  certificateId: IssueErcInstructionDataArgs['certificateId'];
  energyAmount: IssueErcInstructionDataArgs['energyAmount'];
  renewableSource: IssueErcInstructionDataArgs['renewableSource'];
  validationData: IssueErcInstructionDataArgs['validationData'];
};

export function getIssueErcInstruction<
  TAccountPoaConfig extends string,
  TAccountErcCertificate extends string,
  TAccountMeterAccount extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: IssueErcInput<
    TAccountPoaConfig,
    TAccountErcCertificate,
    TAccountMeterAccount,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): IssueErcInstruction<
  TProgramAddress,
  TAccountPoaConfig,
  TAccountErcCertificate,
  TAccountMeterAccount,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    poaConfig: { value: input.poaConfig ?? null, isWritable: false },
    ercCertificate: { value: input.ercCertificate ?? null, isWritable: true },
    meterAccount: { value: input.meterAccount ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.poaConfig),
      getAccountMeta(accounts.ercCertificate),
      getAccountMeta(accounts.meterAccount),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getIssueErcInstructionDataEncoder().encode(
      args as IssueErcInstructionDataArgs
    ),
    programAddress,
  } as IssueErcInstruction<
    TProgramAddress,
    TAccountPoaConfig,
    TAccountErcCertificate,
    TAccountMeterAccount,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedIssueErcInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    poaConfig: TAccountMetas[0];
    ercCertificate: TAccountMetas[1];
    /** Meter account from registry program - tracks claimed ERC generation */
    meterAccount: TAccountMetas[2];
    authority: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: IssueErcInstructionData;
};

export function parseIssueErcInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedIssueErcInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      poaConfig: getNextAccount(),
      ercCertificate: getNextAccount(),
      meterAccount: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getIssueErcInstructionDataDecoder().decode(instruction.data),
  };
}
