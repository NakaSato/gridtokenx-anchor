/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { GOVERNANCE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_AUTHORITY_INFO_DISCRIMINATOR = new Uint8Array([
  199, 12, 195, 232, 185, 99, 112, 145,
]);

export function getUpdateAuthorityInfoDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_AUTHORITY_INFO_DISCRIMINATOR
  );
}

export type UpdateAuthorityInfoInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountPoaConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPoaConfig extends string
        ? WritableAccount<TAccountPoaConfig>
        : TAccountPoaConfig,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateAuthorityInfoInstructionData = {
  discriminator: ReadonlyUint8Array;
  contactInfo: string;
};

export type UpdateAuthorityInfoInstructionDataArgs = { contactInfo: string };

export function getUpdateAuthorityInfoInstructionDataEncoder(): Encoder<UpdateAuthorityInfoInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['contactInfo', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_AUTHORITY_INFO_DISCRIMINATOR,
    })
  );
}

export function getUpdateAuthorityInfoInstructionDataDecoder(): Decoder<UpdateAuthorityInfoInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['contactInfo', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getUpdateAuthorityInfoInstructionDataCodec(): Codec<
  UpdateAuthorityInfoInstructionDataArgs,
  UpdateAuthorityInfoInstructionData
> {
  return combineCodec(
    getUpdateAuthorityInfoInstructionDataEncoder(),
    getUpdateAuthorityInfoInstructionDataDecoder()
  );
}

export type UpdateAuthorityInfoAsyncInput<
  TAccountPoaConfig extends string = string,
  TAccountAuthority extends string = string,
> = {
  poaConfig?: Address<TAccountPoaConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  contactInfo: UpdateAuthorityInfoInstructionDataArgs['contactInfo'];
};

export async function getUpdateAuthorityInfoInstructionAsync<
  TAccountPoaConfig extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: UpdateAuthorityInfoAsyncInput<TAccountPoaConfig, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  UpdateAuthorityInfoInstruction<
    TProgramAddress,
    TAccountPoaConfig,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    poaConfig: { value: input.poaConfig ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.poaConfig.value) {
    accounts.poaConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 111, 97, 95, 99, 111, 110, 102, 105, 103])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.poaConfig),
      getAccountMeta(accounts.authority),
    ],
    data: getUpdateAuthorityInfoInstructionDataEncoder().encode(
      args as UpdateAuthorityInfoInstructionDataArgs
    ),
    programAddress,
  } as UpdateAuthorityInfoInstruction<
    TProgramAddress,
    TAccountPoaConfig,
    TAccountAuthority
  >);
}

export type UpdateAuthorityInfoInput<
  TAccountPoaConfig extends string = string,
  TAccountAuthority extends string = string,
> = {
  poaConfig: Address<TAccountPoaConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  contactInfo: UpdateAuthorityInfoInstructionDataArgs['contactInfo'];
};

export function getUpdateAuthorityInfoInstruction<
  TAccountPoaConfig extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: UpdateAuthorityInfoInput<TAccountPoaConfig, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): UpdateAuthorityInfoInstruction<
  TProgramAddress,
  TAccountPoaConfig,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    poaConfig: { value: input.poaConfig ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.poaConfig),
      getAccountMeta(accounts.authority),
    ],
    data: getUpdateAuthorityInfoInstructionDataEncoder().encode(
      args as UpdateAuthorityInfoInstructionDataArgs
    ),
    programAddress,
  } as UpdateAuthorityInfoInstruction<
    TProgramAddress,
    TAccountPoaConfig,
    TAccountAuthority
  >);
}

export type ParsedUpdateAuthorityInfoInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    poaConfig: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: UpdateAuthorityInfoInstructionData;
};

export function parseUpdateAuthorityInfoInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateAuthorityInfoInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { poaConfig: getNextAccount(), authority: getNextAccount() },
    data: getUpdateAuthorityInfoInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
