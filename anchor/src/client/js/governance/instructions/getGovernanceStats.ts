/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';
import { GOVERNANCE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const GET_GOVERNANCE_STATS_DISCRIMINATOR = new Uint8Array([
  67, 29, 241, 120, 15, 8, 9, 16,
]);

export function getGetGovernanceStatsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GET_GOVERNANCE_STATS_DISCRIMINATOR
  );
}

export type GetGovernanceStatsInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountPoaConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPoaConfig extends string
        ? ReadonlyAccount<TAccountPoaConfig>
        : TAccountPoaConfig,
      ...TRemainingAccounts,
    ]
  >;

export type GetGovernanceStatsInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type GetGovernanceStatsInstructionDataArgs = {};

export function getGetGovernanceStatsInstructionDataEncoder(): FixedSizeEncoder<GetGovernanceStatsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: GET_GOVERNANCE_STATS_DISCRIMINATOR })
  );
}

export function getGetGovernanceStatsInstructionDataDecoder(): FixedSizeDecoder<GetGovernanceStatsInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getGetGovernanceStatsInstructionDataCodec(): FixedSizeCodec<
  GetGovernanceStatsInstructionDataArgs,
  GetGovernanceStatsInstructionData
> {
  return combineCodec(
    getGetGovernanceStatsInstructionDataEncoder(),
    getGetGovernanceStatsInstructionDataDecoder()
  );
}

export type GetGovernanceStatsAsyncInput<
  TAccountPoaConfig extends string = string,
> = {
  poaConfig?: Address<TAccountPoaConfig>;
};

export async function getGetGovernanceStatsInstructionAsync<
  TAccountPoaConfig extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: GetGovernanceStatsAsyncInput<TAccountPoaConfig>,
  config?: { programAddress?: TProgramAddress }
): Promise<GetGovernanceStatsInstruction<TProgramAddress, TAccountPoaConfig>> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    poaConfig: { value: input.poaConfig ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.poaConfig.value) {
    accounts.poaConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 111, 97, 95, 99, 111, 110, 102, 105, 103])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.poaConfig)],
    data: getGetGovernanceStatsInstructionDataEncoder().encode({}),
    programAddress,
  } as GetGovernanceStatsInstruction<TProgramAddress, TAccountPoaConfig>);
}

export type GetGovernanceStatsInput<TAccountPoaConfig extends string = string> =
  {
    poaConfig: Address<TAccountPoaConfig>;
  };

export function getGetGovernanceStatsInstruction<
  TAccountPoaConfig extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: GetGovernanceStatsInput<TAccountPoaConfig>,
  config?: { programAddress?: TProgramAddress }
): GetGovernanceStatsInstruction<TProgramAddress, TAccountPoaConfig> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    poaConfig: { value: input.poaConfig ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.poaConfig)],
    data: getGetGovernanceStatsInstructionDataEncoder().encode({}),
    programAddress,
  } as GetGovernanceStatsInstruction<TProgramAddress, TAccountPoaConfig>);
}

export type ParsedGetGovernanceStatsInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    poaConfig: TAccountMetas[0];
  };
  data: GetGovernanceStatsInstructionData;
};

export function parseGetGovernanceStatsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedGetGovernanceStatsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { poaConfig: getNextAccount() },
    data: getGetGovernanceStatsInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
