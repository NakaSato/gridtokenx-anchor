/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from '@solana/kit';
import {
  getErcStatusDecoder,
  getErcStatusEncoder,
  type ErcStatus,
  type ErcStatusArgs,
} from '../types';

export const ERC_CERTIFICATE_DISCRIMINATOR = new Uint8Array([
  83, 161, 134, 16, 240, 92, 186, 157,
]);

export function getErcCertificateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ERC_CERTIFICATE_DISCRIMINATOR
  );
}

export type ErcCertificate = {
  discriminator: ReadonlyUint8Array;
  /** Unique certificate identifier */
  certificateId: string;
  /** Issuing authority (Engineering Department) */
  authority: Address;
  /** Amount of renewable energy (kWh) */
  energyAmount: bigint;
  /** Source of renewable energy (solar, wind, etc.) */
  renewableSource: string;
  /** Additional validation data */
  validationData: string;
  /** When the certificate was issued */
  issuedAt: bigint;
  /** When the certificate expires */
  expiresAt: Option<bigint>;
  /** Current status of the certificate */
  status: ErcStatus;
  /** Whether validated for trading */
  validatedForTrading: boolean;
  /** When validated for trading */
  tradingValidatedAt: Option<bigint>;
};

export type ErcCertificateArgs = {
  /** Unique certificate identifier */
  certificateId: string;
  /** Issuing authority (Engineering Department) */
  authority: Address;
  /** Amount of renewable energy (kWh) */
  energyAmount: number | bigint;
  /** Source of renewable energy (solar, wind, etc.) */
  renewableSource: string;
  /** Additional validation data */
  validationData: string;
  /** When the certificate was issued */
  issuedAt: number | bigint;
  /** When the certificate expires */
  expiresAt: OptionOrNullable<number | bigint>;
  /** Current status of the certificate */
  status: ErcStatusArgs;
  /** Whether validated for trading */
  validatedForTrading: boolean;
  /** When validated for trading */
  tradingValidatedAt: OptionOrNullable<number | bigint>;
};

/** Gets the encoder for {@link ErcCertificateArgs} account data. */
export function getErcCertificateEncoder(): Encoder<ErcCertificateArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      [
        'certificateId',
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
      ['authority', getAddressEncoder()],
      ['energyAmount', getU64Encoder()],
      [
        'renewableSource',
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
      [
        'validationData',
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
      ['issuedAt', getI64Encoder()],
      ['expiresAt', getOptionEncoder(getI64Encoder())],
      ['status', getErcStatusEncoder()],
      ['validatedForTrading', getBooleanEncoder()],
      ['tradingValidatedAt', getOptionEncoder(getI64Encoder())],
    ]),
    (value) => ({ ...value, discriminator: ERC_CERTIFICATE_DISCRIMINATOR })
  );
}

/** Gets the decoder for {@link ErcCertificate} account data. */
export function getErcCertificateDecoder(): Decoder<ErcCertificate> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['certificateId', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['authority', getAddressDecoder()],
    ['energyAmount', getU64Decoder()],
    [
      'renewableSource',
      addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()),
    ],
    ['validationData', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['issuedAt', getI64Decoder()],
    ['expiresAt', getOptionDecoder(getI64Decoder())],
    ['status', getErcStatusDecoder()],
    ['validatedForTrading', getBooleanDecoder()],
    ['tradingValidatedAt', getOptionDecoder(getI64Decoder())],
  ]);
}

/** Gets the codec for {@link ErcCertificate} account data. */
export function getErcCertificateCodec(): Codec<
  ErcCertificateArgs,
  ErcCertificate
> {
  return combineCodec(getErcCertificateEncoder(), getErcCertificateDecoder());
}

export function decodeErcCertificate<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<ErcCertificate, TAddress>;
export function decodeErcCertificate<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<ErcCertificate, TAddress>;
export function decodeErcCertificate<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<ErcCertificate, TAddress> | MaybeAccount<ErcCertificate, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getErcCertificateDecoder()
  );
}

export async function fetchErcCertificate<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<ErcCertificate, TAddress>> {
  const maybeAccount = await fetchMaybeErcCertificate(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeErcCertificate<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<ErcCertificate, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeErcCertificate(maybeAccount);
}

export async function fetchAllErcCertificate(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<ErcCertificate>[]> {
  const maybeAccounts = await fetchAllMaybeErcCertificate(
    rpc,
    addresses,
    config
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeErcCertificate(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<ErcCertificate>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeErcCertificate(maybeAccount)
  );
}
