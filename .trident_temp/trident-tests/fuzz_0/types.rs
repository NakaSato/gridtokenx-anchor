//! # Trident Generated Types
//!
//! This file is automatically generated by Trident.
//! **DO NOT EDIT THIS FILE MANUALLY**

#![allow(dead_code)]
#![allow(unused_imports)]

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use trident_fuzz::fuzzing::*;

// ============================================================================
// PROGRAM MODULES
// ============================================================================

// ----------------------------------------------------------------------------
// Program: trading
// ----------------------------------------------------------------------------
pub mod trading {
    use super::*;

    // ------------------------------------------------------------------------
    // Program ID
    // ------------------------------------------------------------------------

    /// Returns the program ID for trading
    pub fn program_id() -> Pubkey {
        pubkey!("8S2e2p4ghqMJuzTz5AkAKSka7jqsjgBH7eWDcCHzXPND")
    }

    // ------------------------------------------------------------------------
    // Instructions
    // ------------------------------------------------------------------------

    // ....................................................................
    // Instruction: AuthorizeOracle
    // ....................................................................

    /// Main instruction struct for AuthorizeOracle
    pub struct AuthorizeOracleInstruction {
        pub accounts: AuthorizeOracleInstructionAccountMetas,
        pub data: AuthorizeOracleInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for AuthorizeOracle instruction
    #[derive(Debug, Clone, Default)]
    pub struct AuthorizeOracleInstructionAccountMetas {
        pub config: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for AuthorizeOracle instruction
    #[derive(Debug, Clone)]
    pub struct AuthorizeOracleInstructionAccounts {
        pub config: Pubkey,

        pub authority: Pubkey,
    }

    impl AuthorizeOracleInstructionAccounts {
        pub fn new(config: Pubkey, authority: Pubkey) -> Self {
            Self { config, authority }
        }
    }

    /// Instruction data for AuthorizeOracle
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct AuthorizeOracleInstructionData {
        pub oracle: Pubkey,
    }

    impl AuthorizeOracleInstructionData {
        pub fn new(oracle: Pubkey) -> Self {
            Self { oracle }
        }
    }

    /// Implementation for AuthorizeOracleInstruction
    impl AuthorizeOracleInstruction {
        fn discriminator() -> [u8; 8] {
            [160u8, 242u8, 137u8, 241u8, 147u8, 184u8, 241u8, 64u8]
        }

        pub fn data(data: AuthorizeOracleInstructionData) -> Self {
            Self {
                accounts: AuthorizeOracleInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: AuthorizeOracleInstructionAccounts) -> Self {
            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CancelAuctionOrder
    // ....................................................................

    /// Main instruction struct for CancelAuctionOrder
    pub struct CancelAuctionOrderInstruction {
        pub accounts: CancelAuctionOrderInstructionAccountMetas,
        pub data: CancelAuctionOrderInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CancelAuctionOrder instruction
    #[derive(Debug, Clone, Default)]
    pub struct CancelAuctionOrderInstructionAccountMetas {
        pub batch: AccountMeta,

        pub user_token_account: AccountMeta,

        pub vault: AccountMeta,

        pub token_mint: AccountMeta,

        pub authority: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for CancelAuctionOrder instruction
    #[derive(Debug, Clone)]
    pub struct CancelAuctionOrderInstructionAccounts {
        pub batch: Pubkey,

        pub user_token_account: Pubkey,

        pub vault: Pubkey,

        pub token_mint: Pubkey,

        pub authority: Pubkey,

        pub token_program: Pubkey,
    }

    impl CancelAuctionOrderInstructionAccounts {
        pub fn new(
            batch: Pubkey,

            user_token_account: Pubkey,

            vault: Pubkey,

            token_mint: Pubkey,

            authority: Pubkey,

            token_program: Pubkey,
        ) -> Self {
            Self {
                batch,

                user_token_account,

                vault,

                token_mint,

                authority,

                token_program,
            }
        }
    }

    /// Instruction data for CancelAuctionOrder
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CancelAuctionOrderInstructionData {
        pub order_idx: u32,
    }

    impl CancelAuctionOrderInstructionData {
        pub fn new(order_idx: u32) -> Self {
            Self { order_idx }
        }
    }

    /// Implementation for CancelAuctionOrderInstruction
    impl CancelAuctionOrderInstruction {
        fn discriminator() -> [u8; 8] {
            [34u8, 217u8, 2u8, 121u8, 49u8, 136u8, 43u8, 76u8]
        }

        pub fn data(data: CancelAuctionOrderInstructionData) -> Self {
            Self {
                accounts: CancelAuctionOrderInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CancelAuctionOrderInstructionAccounts) -> Self {
            self.accounts.batch = AccountMeta::new(accounts.batch, false);

            self.accounts.user_token_account = AccountMeta::new(accounts.user_token_account, false);

            self.accounts.vault = AccountMeta::new(accounts.vault, false);

            self.accounts.token_mint = AccountMeta::new_readonly(accounts.token_mint, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.batch.clone());

            metas.push(self.accounts.user_token_account.clone());

            metas.push(self.accounts.vault.clone());

            metas.push(self.accounts.token_mint.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CancelOrder
    // ....................................................................

    /// Main instruction struct for CancelOrder
    pub struct CancelOrderInstruction {
        pub accounts: CancelOrderInstructionAccountMetas,
        pub data: CancelOrderInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CancelOrder instruction
    #[derive(Debug, Clone, Default)]
    pub struct CancelOrderInstructionAccountMetas {
        pub market: AccountMeta,

        pub order: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for CancelOrder instruction
    #[derive(Debug, Clone)]
    pub struct CancelOrderInstructionAccounts {
        pub market: Pubkey,

        pub order: Pubkey,

        pub authority: Pubkey,
    }

    impl CancelOrderInstructionAccounts {
        pub fn new(market: Pubkey, order: Pubkey, authority: Pubkey) -> Self {
            Self {
                market,

                order,

                authority,
            }
        }
    }

    /// Instruction data for CancelOrder
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CancelOrderInstructionData {}

    impl CancelOrderInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for CancelOrderInstruction
    impl CancelOrderInstruction {
        fn discriminator() -> [u8; 8] {
            [95u8, 129u8, 237u8, 240u8, 8u8, 49u8, 223u8, 132u8]
        }

        pub fn data(data: CancelOrderInstructionData) -> Self {
            Self {
                accounts: CancelOrderInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CancelOrderInstructionAccounts) -> Self {
            self.accounts.market = AccountMeta::new(accounts.market, false);

            self.accounts.order = AccountMeta::new(accounts.order, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.order.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CompleteBridgeTransfer
    // ....................................................................

    /// Main instruction struct for CompleteBridgeTransfer
    pub struct CompleteBridgeTransferInstruction {
        pub accounts: CompleteBridgeTransferInstructionAccountMetas,
        pub data: CompleteBridgeTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CompleteBridgeTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct CompleteBridgeTransferInstructionAccountMetas {
        pub market: AccountMeta,

        pub bridge_config: AccountMeta,

        pub bridge_transfer: AccountMeta,

        pub wrapped_record: AccountMeta,

        pub token_mint: AccountMeta,

        pub user_token_account: AccountMeta,

        pub bridge_escrow: AccountMeta,

        pub user: AccountMeta,

        pub token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for CompleteBridgeTransfer instruction
    #[derive(Debug, Clone)]
    pub struct CompleteBridgeTransferInstructionAccounts {
        pub market: Pubkey,

        pub bridge_config: Pubkey,

        pub bridge_transfer: Pubkey,

        pub wrapped_record: Pubkey,

        pub token_mint: Pubkey,

        pub user_token_account: Pubkey,

        pub bridge_escrow: Pubkey,

        pub user: Pubkey,

        pub token_program: Pubkey,

        pub system_program: Pubkey,
    }

    impl CompleteBridgeTransferInstructionAccounts {
        pub fn new(
            market: Pubkey,

            bridge_config: Pubkey,

            bridge_transfer: Pubkey,

            wrapped_record: Pubkey,

            token_mint: Pubkey,

            user_token_account: Pubkey,

            bridge_escrow: Pubkey,

            user: Pubkey,

            token_program: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                market,

                bridge_config,

                bridge_transfer,

                wrapped_record,

                token_mint,

                user_token_account,

                bridge_escrow,

                user,

                token_program,

                system_program,
            }
        }
    }

    /// Instruction data for CompleteBridgeTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CompleteBridgeTransferInstructionData {
        pub vaa_hash: [u8; 32],
    }

    impl CompleteBridgeTransferInstructionData {
        pub fn new(vaa_hash: [u8; 32]) -> Self {
            Self { vaa_hash }
        }
    }

    /// Implementation for CompleteBridgeTransferInstruction
    impl CompleteBridgeTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [92u8, 186u8, 3u8, 87u8, 117u8, 124u8, 209u8, 142u8]
        }

        pub fn data(data: CompleteBridgeTransferInstructionData) -> Self {
            Self {
                accounts: CompleteBridgeTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CompleteBridgeTransferInstructionAccounts) -> Self {
            self.accounts.market = AccountMeta::new_readonly(accounts.market, false);

            self.accounts.bridge_config = AccountMeta::new_readonly(accounts.bridge_config, false);

            self.accounts.bridge_transfer = AccountMeta::new(accounts.bridge_transfer, false);

            self.accounts.wrapped_record = AccountMeta::new(accounts.wrapped_record, false);

            self.accounts.token_mint = AccountMeta::new_readonly(accounts.token_mint, false);

            self.accounts.user_token_account = AccountMeta::new(accounts.user_token_account, false);

            self.accounts.bridge_escrow = AccountMeta::new(accounts.bridge_escrow, false);

            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.bridge_config.clone());

            metas.push(self.accounts.bridge_transfer.clone());

            metas.push(self.accounts.wrapped_record.clone());

            metas.push(self.accounts.token_mint.clone());

            metas.push(self.accounts.user_token_account.clone());

            metas.push(self.accounts.bridge_escrow.clone());

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ConfigurePaymentToken
    // ....................................................................

    /// Main instruction struct for ConfigurePaymentToken
    pub struct ConfigurePaymentTokenInstruction {
        pub accounts: ConfigurePaymentTokenInstructionAccountMetas,
        pub data: ConfigurePaymentTokenInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ConfigurePaymentToken instruction
    #[derive(Debug, Clone, Default)]
    pub struct ConfigurePaymentTokenInstructionAccountMetas {
        pub market: AccountMeta,

        pub token_config: AccountMeta,

        pub token_mint: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for ConfigurePaymentToken instruction
    #[derive(Debug, Clone)]
    pub struct ConfigurePaymentTokenInstructionAccounts {
        pub market: Pubkey,

        pub token_config: Pubkey,

        pub token_mint: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl ConfigurePaymentTokenInstructionAccounts {
        pub fn new(
            market: Pubkey,

            token_config: Pubkey,

            token_mint: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                market,

                token_config,

                token_mint,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for ConfigurePaymentToken
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ConfigurePaymentTokenInstructionData {
        pub token_type: u8,

        pub min_order_size: u64,

        pub max_price_deviation_bps: u16,
    }

    impl ConfigurePaymentTokenInstructionData {
        pub fn new(token_type: u8, min_order_size: u64, max_price_deviation_bps: u16) -> Self {
            Self {
                token_type,

                min_order_size,

                max_price_deviation_bps,
            }
        }
    }

    /// Implementation for ConfigurePaymentTokenInstruction
    impl ConfigurePaymentTokenInstruction {
        fn discriminator() -> [u8; 8] {
            [1u8, 135u8, 93u8, 201u8, 62u8, 217u8, 114u8, 27u8]
        }

        pub fn data(data: ConfigurePaymentTokenInstructionData) -> Self {
            Self {
                accounts: ConfigurePaymentTokenInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ConfigurePaymentTokenInstructionAccounts) -> Self {
            self.accounts.market = AccountMeta::new(accounts.market, false);

            self.accounts.token_config = AccountMeta::new(accounts.token_config, false);

            self.accounts.token_mint = AccountMeta::new_readonly(accounts.token_mint, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.token_config.clone());

            metas.push(self.accounts.token_mint.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CreateBuyOrder
    // ....................................................................

    /// Main instruction struct for CreateBuyOrder
    pub struct CreateBuyOrderInstruction {
        pub accounts: CreateBuyOrderInstructionAccountMetas,
        pub data: CreateBuyOrderInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CreateBuyOrder instruction
    #[derive(Debug, Clone, Default)]
    pub struct CreateBuyOrderInstructionAccountMetas {
        pub market: AccountMeta,

        pub order: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for CreateBuyOrder instruction
    #[derive(Debug, Clone)]
    pub struct CreateBuyOrderInstructionAccounts {
        pub market: Pubkey,

        pub order: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl CreateBuyOrderInstructionAccounts {
        pub fn new(
            market: Pubkey,

            order: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                market,

                order,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for CreateBuyOrder
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CreateBuyOrderInstructionData {
        pub order_id: u64,

        pub energy_amount: u64,

        pub max_price_per_kwh: u64,
    }

    impl CreateBuyOrderInstructionData {
        pub fn new(order_id: u64, energy_amount: u64, max_price_per_kwh: u64) -> Self {
            Self {
                order_id,

                energy_amount,

                max_price_per_kwh,
            }
        }
    }

    /// Implementation for CreateBuyOrderInstruction
    impl CreateBuyOrderInstruction {
        fn discriminator() -> [u8; 8] {
            [182u8, 87u8, 0u8, 160u8, 192u8, 66u8, 151u8, 130u8]
        }

        pub fn data(data: CreateBuyOrderInstructionData) -> Self {
            Self {
                accounts: CreateBuyOrderInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CreateBuyOrderInstructionAccounts) -> Self {
            self.accounts.market = AccountMeta::new(accounts.market, false);

            self.accounts.order = AccountMeta::new(accounts.order, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.order.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CreateCrossChainOrder
    // ....................................................................

    /// Main instruction struct for CreateCrossChainOrder
    pub struct CreateCrossChainOrderInstruction {
        pub accounts: CreateCrossChainOrderInstructionAccountMetas,
        pub data: CreateCrossChainOrderInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CreateCrossChainOrder instruction
    #[derive(Debug, Clone, Default)]
    pub struct CreateCrossChainOrderInstructionAccountMetas {
        pub cross_chain_order: AccountMeta,

        pub solana_order: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for CreateCrossChainOrder instruction
    #[derive(Debug, Clone)]
    pub struct CreateCrossChainOrderInstructionAccounts {
        pub cross_chain_order: Pubkey,

        pub solana_order: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl CreateCrossChainOrderInstructionAccounts {
        pub fn new(
            cross_chain_order: Pubkey,

            solana_order: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                cross_chain_order,

                solana_order,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for CreateCrossChainOrder
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CreateCrossChainOrderInstructionData {
        pub origin_chain: u16,

        pub origin_order_id: [u8; 32],

        pub origin_user: [u8; 32],

        pub energy_amount: u64,

        pub price: u64,

        pub payment_token: [u8; 32],
    }

    impl CreateCrossChainOrderInstructionData {
        pub fn new(
            origin_chain: u16,

            origin_order_id: [u8; 32],

            origin_user: [u8; 32],

            energy_amount: u64,

            price: u64,

            payment_token: [u8; 32],
        ) -> Self {
            Self {
                origin_chain,

                origin_order_id,

                origin_user,

                energy_amount,

                price,

                payment_token,
            }
        }
    }

    /// Implementation for CreateCrossChainOrderInstruction
    impl CreateCrossChainOrderInstruction {
        fn discriminator() -> [u8; 8] {
            [194u8, 177u8, 97u8, 108u8, 138u8, 86u8, 192u8, 172u8]
        }

        pub fn data(data: CreateCrossChainOrderInstructionData) -> Self {
            Self {
                accounts: CreateCrossChainOrderInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CreateCrossChainOrderInstructionAccounts) -> Self {
            self.accounts.cross_chain_order = AccountMeta::new(accounts.cross_chain_order, false);

            self.accounts.solana_order = AccountMeta::new_readonly(accounts.solana_order, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.cross_chain_order.clone());

            metas.push(self.accounts.solana_order.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CreatePriceSnapshot
    // ....................................................................

    /// Main instruction struct for CreatePriceSnapshot
    pub struct CreatePriceSnapshotInstruction {
        pub accounts: CreatePriceSnapshotInstructionAccountMetas,
        pub data: CreatePriceSnapshotInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CreatePriceSnapshot instruction
    #[derive(Debug, Clone, Default)]
    pub struct CreatePriceSnapshotInstructionAccountMetas {
        pub pricing_config: AccountMeta,

        pub snapshot: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for CreatePriceSnapshot instruction
    #[derive(Debug, Clone)]
    pub struct CreatePriceSnapshotInstructionAccounts {
        pub pricing_config: Pubkey,

        pub snapshot: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl CreatePriceSnapshotInstructionAccounts {
        pub fn new(
            pricing_config: Pubkey,

            snapshot: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                pricing_config,

                snapshot,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for CreatePriceSnapshot
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CreatePriceSnapshotInstructionData {
        pub timestamp: i64,
    }

    impl CreatePriceSnapshotInstructionData {
        pub fn new(timestamp: i64) -> Self {
            Self { timestamp }
        }
    }

    /// Implementation for CreatePriceSnapshotInstruction
    impl CreatePriceSnapshotInstruction {
        fn discriminator() -> [u8; 8] {
            [151u8, 75u8, 60u8, 210u8, 168u8, 113u8, 255u8, 248u8]
        }

        pub fn data(data: CreatePriceSnapshotInstructionData) -> Self {
            Self {
                accounts: CreatePriceSnapshotInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CreatePriceSnapshotInstructionAccounts) -> Self {
            self.accounts.pricing_config =
                AccountMeta::new_readonly(accounts.pricing_config, false);

            self.accounts.snapshot = AccountMeta::new(accounts.snapshot, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.pricing_config.clone());

            metas.push(self.accounts.snapshot.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CreateSellOrder
    // ....................................................................

    /// Main instruction struct for CreateSellOrder
    pub struct CreateSellOrderInstruction {
        pub accounts: CreateSellOrderInstructionAccountMetas,
        pub data: CreateSellOrderInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CreateSellOrder instruction
    #[derive(Debug, Clone, Default)]
    pub struct CreateSellOrderInstructionAccountMetas {
        pub market: AccountMeta,

        pub order: AccountMeta,

        pub erc_certificate: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for CreateSellOrder instruction
    #[derive(Debug, Clone)]
    pub struct CreateSellOrderInstructionAccounts {
        pub market: Pubkey,

        pub order: Pubkey,

        pub erc_certificate: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl CreateSellOrderInstructionAccounts {
        pub fn new(
            market: Pubkey,

            order: Pubkey,

            erc_certificate: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                market,

                order,

                erc_certificate,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for CreateSellOrder
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CreateSellOrderInstructionData {
        pub order_id: u64,

        pub energy_amount: u64,

        pub price_per_kwh: u64,
    }

    impl CreateSellOrderInstructionData {
        pub fn new(order_id: u64, energy_amount: u64, price_per_kwh: u64) -> Self {
            Self {
                order_id,

                energy_amount,

                price_per_kwh,
            }
        }
    }

    /// Implementation for CreateSellOrderInstruction
    impl CreateSellOrderInstruction {
        fn discriminator() -> [u8; 8] {
            [53u8, 52u8, 255u8, 44u8, 191u8, 74u8, 171u8, 225u8]
        }

        pub fn data(data: CreateSellOrderInstructionData) -> Self {
            Self {
                accounts: CreateSellOrderInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CreateSellOrderInstructionAccounts) -> Self {
            self.accounts.market = AccountMeta::new(accounts.market, false);

            self.accounts.order = AccountMeta::new(accounts.order, false);

            self.accounts.erc_certificate =
                AccountMeta::new_readonly(accounts.erc_certificate, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.order.clone());

            metas.push(self.accounts.erc_certificate.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CreateStablecoinBuyOrder
    // ....................................................................

    /// Main instruction struct for CreateStablecoinBuyOrder
    pub struct CreateStablecoinBuyOrderInstruction {
        pub accounts: CreateStablecoinBuyOrderInstructionAccountMetas,
        pub data: CreateStablecoinBuyOrderInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CreateStablecoinBuyOrder instruction
    #[derive(Debug, Clone, Default)]
    pub struct CreateStablecoinBuyOrderInstructionAccountMetas {
        pub market: AccountMeta,

        pub order: AccountMeta,

        pub payment_info: AccountMeta,

        pub token_config: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for CreateStablecoinBuyOrder instruction
    #[derive(Debug, Clone)]
    pub struct CreateStablecoinBuyOrderInstructionAccounts {
        pub market: Pubkey,

        pub order: Pubkey,

        pub payment_info: Pubkey,

        pub token_config: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl CreateStablecoinBuyOrderInstructionAccounts {
        pub fn new(
            market: Pubkey,

            order: Pubkey,

            payment_info: Pubkey,

            token_config: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                market,

                order,

                payment_info,

                token_config,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for CreateStablecoinBuyOrder
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CreateStablecoinBuyOrderInstructionData {
        pub energy_amount: u64,

        pub max_price_per_kwh: u64,

        pub payment_token: u8,
    }

    impl CreateStablecoinBuyOrderInstructionData {
        pub fn new(energy_amount: u64, max_price_per_kwh: u64, payment_token: u8) -> Self {
            Self {
                energy_amount,

                max_price_per_kwh,

                payment_token,
            }
        }
    }

    /// Implementation for CreateStablecoinBuyOrderInstruction
    impl CreateStablecoinBuyOrderInstruction {
        fn discriminator() -> [u8; 8] {
            [18u8, 128u8, 231u8, 63u8, 85u8, 14u8, 89u8, 175u8]
        }

        pub fn data(data: CreateStablecoinBuyOrderInstructionData) -> Self {
            Self {
                accounts: CreateStablecoinBuyOrderInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CreateStablecoinBuyOrderInstructionAccounts) -> Self {
            self.accounts.market = AccountMeta::new(accounts.market, false);

            self.accounts.order = AccountMeta::new(accounts.order, false);

            self.accounts.payment_info = AccountMeta::new(accounts.payment_info, false);

            self.accounts.token_config = AccountMeta::new_readonly(accounts.token_config, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.order.clone());

            metas.push(self.accounts.payment_info.clone());

            metas.push(self.accounts.token_config.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CreateStablecoinSellOrder
    // ....................................................................

    /// Main instruction struct for CreateStablecoinSellOrder
    pub struct CreateStablecoinSellOrderInstruction {
        pub accounts: CreateStablecoinSellOrderInstructionAccountMetas,
        pub data: CreateStablecoinSellOrderInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CreateStablecoinSellOrder instruction
    #[derive(Debug, Clone, Default)]
    pub struct CreateStablecoinSellOrderInstructionAccountMetas {
        pub market: AccountMeta,

        pub order: AccountMeta,

        pub payment_info: AccountMeta,

        pub token_config: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for CreateStablecoinSellOrder instruction
    #[derive(Debug, Clone)]
    pub struct CreateStablecoinSellOrderInstructionAccounts {
        pub market: Pubkey,

        pub order: Pubkey,

        pub payment_info: Pubkey,

        pub token_config: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl CreateStablecoinSellOrderInstructionAccounts {
        pub fn new(
            market: Pubkey,

            order: Pubkey,

            payment_info: Pubkey,

            token_config: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                market,

                order,

                payment_info,

                token_config,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for CreateStablecoinSellOrder
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CreateStablecoinSellOrderInstructionData {
        pub energy_amount: u64,

        pub price_per_kwh: u64,

        pub payment_token: u8,
    }

    impl CreateStablecoinSellOrderInstructionData {
        pub fn new(energy_amount: u64, price_per_kwh: u64, payment_token: u8) -> Self {
            Self {
                energy_amount,

                price_per_kwh,

                payment_token,
            }
        }
    }

    /// Implementation for CreateStablecoinSellOrderInstruction
    impl CreateStablecoinSellOrderInstruction {
        fn discriminator() -> [u8; 8] {
            [234u8, 235u8, 8u8, 34u8, 149u8, 174u8, 66u8, 46u8]
        }

        pub fn data(data: CreateStablecoinSellOrderInstructionData) -> Self {
            Self {
                accounts: CreateStablecoinSellOrderInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CreateStablecoinSellOrderInstructionAccounts) -> Self {
            self.accounts.market = AccountMeta::new(accounts.market, false);

            self.accounts.order = AccountMeta::new(accounts.order, false);

            self.accounts.payment_info = AccountMeta::new(accounts.payment_info, false);

            self.accounts.token_config = AccountMeta::new_readonly(accounts.token_config, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.order.clone());

            metas.push(self.accounts.payment_info.clone());

            metas.push(self.accounts.token_config.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ExecuteAtomicSettlement
    // ....................................................................

    /// Main instruction struct for ExecuteAtomicSettlement
    pub struct ExecuteAtomicSettlementInstruction {
        pub accounts: ExecuteAtomicSettlementInstructionAccountMetas,
        pub data: ExecuteAtomicSettlementInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ExecuteAtomicSettlement instruction
    #[derive(Debug, Clone, Default)]
    pub struct ExecuteAtomicSettlementInstructionAccountMetas {
        pub market: AccountMeta,

        pub buy_order: AccountMeta,

        pub sell_order: AccountMeta,

        pub buyer_currency_escrow: AccountMeta,

        pub seller_energy_escrow: AccountMeta,

        pub seller_currency_account: AccountMeta,

        pub buyer_energy_account: AccountMeta,

        pub fee_collector: AccountMeta,

        pub wheeling_collector: AccountMeta,

        pub energy_mint: AccountMeta,

        pub currency_mint: AccountMeta,

        pub escrow_authority: AccountMeta,

        pub market_authority: AccountMeta,

        pub token_program: AccountMeta,

        pub system_program: AccountMeta,

        pub secondary_token_program: AccountMeta,
    }

    /// Account pubkeys for ExecuteAtomicSettlement instruction
    #[derive(Debug, Clone)]
    pub struct ExecuteAtomicSettlementInstructionAccounts {
        pub market: Pubkey,

        pub buy_order: Pubkey,

        pub sell_order: Pubkey,

        pub buyer_currency_escrow: Pubkey,

        pub seller_energy_escrow: Pubkey,

        pub seller_currency_account: Pubkey,

        pub buyer_energy_account: Pubkey,

        pub fee_collector: Pubkey,

        pub wheeling_collector: Pubkey,

        pub energy_mint: Pubkey,

        pub currency_mint: Pubkey,

        pub escrow_authority: Pubkey,

        pub market_authority: Pubkey,

        pub token_program: Pubkey,

        pub system_program: Pubkey,

        pub secondary_token_program: Pubkey,
    }

    impl ExecuteAtomicSettlementInstructionAccounts {
        pub fn new(
            market: Pubkey,

            buy_order: Pubkey,

            sell_order: Pubkey,

            buyer_currency_escrow: Pubkey,

            seller_energy_escrow: Pubkey,

            seller_currency_account: Pubkey,

            buyer_energy_account: Pubkey,

            fee_collector: Pubkey,

            wheeling_collector: Pubkey,

            energy_mint: Pubkey,

            currency_mint: Pubkey,

            escrow_authority: Pubkey,

            market_authority: Pubkey,

            token_program: Pubkey,

            system_program: Pubkey,

            secondary_token_program: Pubkey,
        ) -> Self {
            Self {
                market,

                buy_order,

                sell_order,

                buyer_currency_escrow,

                seller_energy_escrow,

                seller_currency_account,

                buyer_energy_account,

                fee_collector,

                wheeling_collector,

                energy_mint,

                currency_mint,

                escrow_authority,

                market_authority,

                token_program,

                system_program,

                secondary_token_program,
            }
        }
    }

    /// Instruction data for ExecuteAtomicSettlement
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ExecuteAtomicSettlementInstructionData {
        pub amount: u64,

        pub price: u64,

        pub wheeling_charge: u64,
    }

    impl ExecuteAtomicSettlementInstructionData {
        pub fn new(amount: u64, price: u64, wheeling_charge: u64) -> Self {
            Self {
                amount,

                price,

                wheeling_charge,
            }
        }
    }

    /// Implementation for ExecuteAtomicSettlementInstruction
    impl ExecuteAtomicSettlementInstruction {
        fn discriminator() -> [u8; 8] {
            [86u8, 216u8, 13u8, 114u8, 76u8, 114u8, 212u8, 11u8]
        }

        pub fn data(data: ExecuteAtomicSettlementInstructionData) -> Self {
            Self {
                accounts: ExecuteAtomicSettlementInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ExecuteAtomicSettlementInstructionAccounts) -> Self {
            self.accounts.market = AccountMeta::new(accounts.market, false);

            self.accounts.buy_order = AccountMeta::new(accounts.buy_order, false);

            self.accounts.sell_order = AccountMeta::new(accounts.sell_order, false);

            self.accounts.buyer_currency_escrow =
                AccountMeta::new(accounts.buyer_currency_escrow, false);

            self.accounts.seller_energy_escrow =
                AccountMeta::new(accounts.seller_energy_escrow, false);

            self.accounts.seller_currency_account =
                AccountMeta::new(accounts.seller_currency_account, false);

            self.accounts.buyer_energy_account =
                AccountMeta::new(accounts.buyer_energy_account, false);

            self.accounts.fee_collector = AccountMeta::new(accounts.fee_collector, false);

            self.accounts.wheeling_collector = AccountMeta::new(accounts.wheeling_collector, false);

            self.accounts.energy_mint = AccountMeta::new_readonly(accounts.energy_mint, false);

            self.accounts.currency_mint = AccountMeta::new_readonly(accounts.currency_mint, false);

            self.accounts.escrow_authority =
                AccountMeta::new_readonly(accounts.escrow_authority, true);

            self.accounts.market_authority =
                AccountMeta::new_readonly(accounts.market_authority, true);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self.accounts.secondary_token_program =
                AccountMeta::new_readonly(accounts.secondary_token_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.buy_order.clone());

            metas.push(self.accounts.sell_order.clone());

            metas.push(self.accounts.buyer_currency_escrow.clone());

            metas.push(self.accounts.seller_energy_escrow.clone());

            metas.push(self.accounts.seller_currency_account.clone());

            metas.push(self.accounts.buyer_energy_account.clone());

            metas.push(self.accounts.fee_collector.clone());

            metas.push(self.accounts.wheeling_collector.clone());

            metas.push(self.accounts.energy_mint.clone());

            metas.push(self.accounts.currency_mint.clone());

            metas.push(self.accounts.escrow_authority.clone());

            metas.push(self.accounts.market_authority.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.secondary_token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ExecuteBatch
    // ....................................................................

    /// Main instruction struct for ExecuteBatch
    pub struct ExecuteBatchInstruction {
        pub accounts: ExecuteBatchInstructionAccountMetas,
        pub data: ExecuteBatchInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ExecuteBatch instruction
    #[derive(Debug, Clone, Default)]
    pub struct ExecuteBatchInstructionAccountMetas {
        pub market: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for ExecuteBatch instruction
    #[derive(Debug, Clone)]
    pub struct ExecuteBatchInstructionAccounts {
        pub market: Pubkey,

        pub authority: Pubkey,
    }

    impl ExecuteBatchInstructionAccounts {
        pub fn new(market: Pubkey, authority: Pubkey) -> Self {
            Self { market, authority }
        }
    }

    /// Instruction data for ExecuteBatch
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ExecuteBatchInstructionData {
        pub amount: Vec<u64>,

        pub price: Vec<u64>,

        pub wheeling_charge: Vec<u64>,
    }

    impl ExecuteBatchInstructionData {
        pub fn new(amount: Vec<u64>, price: Vec<u64>, wheeling_charge: Vec<u64>) -> Self {
            Self {
                amount,

                price,

                wheeling_charge,
            }
        }
    }

    /// Implementation for ExecuteBatchInstruction
    impl ExecuteBatchInstruction {
        fn discriminator() -> [u8; 8] {
            [112u8, 159u8, 211u8, 51u8, 238u8, 70u8, 212u8, 60u8]
        }

        pub fn data(data: ExecuteBatchInstructionData) -> Self {
            Self {
                accounts: ExecuteBatchInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ExecuteBatchInstructionAccounts) -> Self {
            self.accounts.market = AccountMeta::new(accounts.market, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ExecuteSettlement
    // ....................................................................

    /// Main instruction struct for ExecuteSettlement
    pub struct ExecuteSettlementInstruction {
        pub accounts: ExecuteSettlementInstructionAccountMetas,
        pub data: ExecuteSettlementInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ExecuteSettlement instruction
    #[derive(Debug, Clone, Default)]
    pub struct ExecuteSettlementInstructionAccountMetas {
        pub batch: AccountMeta,

        pub buyer_currency_vault: AccountMeta,

        pub seller_energy_vault: AccountMeta,

        pub seller_currency: AccountMeta,

        pub buyer_energy: AccountMeta,

        pub currency_mint: AccountMeta,

        pub energy_mint: AccountMeta,

        pub buyer_authority: AccountMeta,

        pub seller_authority: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for ExecuteSettlement instruction
    #[derive(Debug, Clone)]
    pub struct ExecuteSettlementInstructionAccounts {
        pub batch: Pubkey,

        pub buyer_currency_vault: Pubkey,

        pub seller_energy_vault: Pubkey,

        pub seller_currency: Pubkey,

        pub buyer_energy: Pubkey,

        pub currency_mint: Pubkey,

        pub energy_mint: Pubkey,

        pub buyer_authority: Pubkey,

        pub seller_authority: Pubkey,

        pub token_program: Pubkey,
    }

    impl ExecuteSettlementInstructionAccounts {
        pub fn new(
            batch: Pubkey,

            buyer_currency_vault: Pubkey,

            seller_energy_vault: Pubkey,

            seller_currency: Pubkey,

            buyer_energy: Pubkey,

            currency_mint: Pubkey,

            energy_mint: Pubkey,

            buyer_authority: Pubkey,

            seller_authority: Pubkey,

            token_program: Pubkey,
        ) -> Self {
            Self {
                batch,

                buyer_currency_vault,

                seller_energy_vault,

                seller_currency,

                buyer_energy,

                currency_mint,

                energy_mint,

                buyer_authority,

                seller_authority,

                token_program,
            }
        }
    }

    /// Instruction data for ExecuteSettlement
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ExecuteSettlementInstructionData {
        pub bid_order_idx: u32,

        pub ask_order_idx: u32,

        pub settle_amount: u64,
    }

    impl ExecuteSettlementInstructionData {
        pub fn new(bid_order_idx: u32, ask_order_idx: u32, settle_amount: u64) -> Self {
            Self {
                bid_order_idx,

                ask_order_idx,

                settle_amount,
            }
        }
    }

    /// Implementation for ExecuteSettlementInstruction
    impl ExecuteSettlementInstruction {
        fn discriminator() -> [u8; 8] {
            [237u8, 120u8, 82u8, 62u8, 224u8, 193u8, 147u8, 137u8]
        }

        pub fn data(data: ExecuteSettlementInstructionData) -> Self {
            Self {
                accounts: ExecuteSettlementInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ExecuteSettlementInstructionAccounts) -> Self {
            self.accounts.batch = AccountMeta::new(accounts.batch, false);

            self.accounts.buyer_currency_vault =
                AccountMeta::new(accounts.buyer_currency_vault, false);

            self.accounts.seller_energy_vault =
                AccountMeta::new(accounts.seller_energy_vault, false);

            self.accounts.seller_currency = AccountMeta::new(accounts.seller_currency, false);

            self.accounts.buyer_energy = AccountMeta::new(accounts.buyer_energy, false);

            self.accounts.currency_mint = AccountMeta::new_readonly(accounts.currency_mint, false);

            self.accounts.energy_mint = AccountMeta::new_readonly(accounts.energy_mint, false);

            self.accounts.buyer_authority =
                AccountMeta::new_readonly(accounts.buyer_authority, false);

            self.accounts.seller_authority =
                AccountMeta::new_readonly(accounts.seller_authority, false);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.batch.clone());

            metas.push(self.accounts.buyer_currency_vault.clone());

            metas.push(self.accounts.seller_energy_vault.clone());

            metas.push(self.accounts.seller_currency.clone());

            metas.push(self.accounts.buyer_energy.clone());

            metas.push(self.accounts.currency_mint.clone());

            metas.push(self.accounts.energy_mint.clone());

            metas.push(self.accounts.buyer_authority.clone());

            metas.push(self.accounts.seller_authority.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ExecuteStablecoinSettlement
    // ....................................................................

    /// Main instruction struct for ExecuteStablecoinSettlement
    pub struct ExecuteStablecoinSettlementInstruction {
        pub accounts: ExecuteStablecoinSettlementInstructionAccountMetas,
        pub data: ExecuteStablecoinSettlementInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ExecuteStablecoinSettlement instruction
    #[derive(Debug, Clone, Default)]
    pub struct ExecuteStablecoinSettlementInstructionAccountMetas {
        pub market: AccountMeta,

        pub buy_order: AccountMeta,

        pub sell_order: AccountMeta,

        pub buy_payment_info: AccountMeta,

        pub sell_payment_info: AccountMeta,

        pub stablecoin_mint: AccountMeta,

        pub energy_mint: AccountMeta,

        pub buyer_stablecoin: AccountMeta,

        pub seller_stablecoin: AccountMeta,

        pub buyer_energy: AccountMeta,

        pub seller_energy: AccountMeta,

        pub fee_collector: AccountMeta,

        pub escrow_authority: AccountMeta,

        pub authority: AccountMeta,

        pub token_program: AccountMeta,

        pub energy_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for ExecuteStablecoinSettlement instruction
    #[derive(Debug, Clone)]
    pub struct ExecuteStablecoinSettlementInstructionAccounts {
        pub market: Pubkey,

        pub buy_order: Pubkey,

        pub sell_order: Pubkey,

        pub buy_payment_info: Pubkey,

        pub sell_payment_info: Pubkey,

        pub stablecoin_mint: Pubkey,

        pub energy_mint: Pubkey,

        pub buyer_stablecoin: Pubkey,

        pub seller_stablecoin: Pubkey,

        pub buyer_energy: Pubkey,

        pub seller_energy: Pubkey,

        pub fee_collector: Pubkey,

        pub escrow_authority: Pubkey,

        pub authority: Pubkey,

        pub token_program: Pubkey,

        pub energy_token_program: Pubkey,

        pub system_program: Pubkey,
    }

    impl ExecuteStablecoinSettlementInstructionAccounts {
        pub fn new(
            market: Pubkey,

            buy_order: Pubkey,

            sell_order: Pubkey,

            buy_payment_info: Pubkey,

            sell_payment_info: Pubkey,

            stablecoin_mint: Pubkey,

            energy_mint: Pubkey,

            buyer_stablecoin: Pubkey,

            seller_stablecoin: Pubkey,

            buyer_energy: Pubkey,

            seller_energy: Pubkey,

            fee_collector: Pubkey,

            escrow_authority: Pubkey,

            authority: Pubkey,

            token_program: Pubkey,

            energy_token_program: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                market,

                buy_order,

                sell_order,

                buy_payment_info,

                sell_payment_info,

                stablecoin_mint,

                energy_mint,

                buyer_stablecoin,

                seller_stablecoin,

                buyer_energy,

                seller_energy,

                fee_collector,

                escrow_authority,

                authority,

                token_program,

                energy_token_program,

                system_program,
            }
        }
    }

    /// Instruction data for ExecuteStablecoinSettlement
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ExecuteStablecoinSettlementInstructionData {
        pub amount: u64,

        pub exchange_rate: u64,
    }

    impl ExecuteStablecoinSettlementInstructionData {
        pub fn new(amount: u64, exchange_rate: u64) -> Self {
            Self {
                amount,

                exchange_rate,
            }
        }
    }

    /// Implementation for ExecuteStablecoinSettlementInstruction
    impl ExecuteStablecoinSettlementInstruction {
        fn discriminator() -> [u8; 8] {
            [191u8, 33u8, 51u8, 226u8, 68u8, 211u8, 91u8, 31u8]
        }

        pub fn data(data: ExecuteStablecoinSettlementInstructionData) -> Self {
            Self {
                accounts: ExecuteStablecoinSettlementInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(
            mut self,
            accounts: ExecuteStablecoinSettlementInstructionAccounts,
        ) -> Self {
            self.accounts.market = AccountMeta::new(accounts.market, false);

            self.accounts.buy_order = AccountMeta::new(accounts.buy_order, false);

            self.accounts.sell_order = AccountMeta::new(accounts.sell_order, false);

            self.accounts.buy_payment_info = AccountMeta::new(accounts.buy_payment_info, false);

            self.accounts.sell_payment_info = AccountMeta::new(accounts.sell_payment_info, false);

            self.accounts.stablecoin_mint =
                AccountMeta::new_readonly(accounts.stablecoin_mint, false);

            self.accounts.energy_mint = AccountMeta::new_readonly(accounts.energy_mint, false);

            self.accounts.buyer_stablecoin = AccountMeta::new(accounts.buyer_stablecoin, false);

            self.accounts.seller_stablecoin = AccountMeta::new(accounts.seller_stablecoin, false);

            self.accounts.buyer_energy = AccountMeta::new(accounts.buyer_energy, false);

            self.accounts.seller_energy = AccountMeta::new(accounts.seller_energy, false);

            self.accounts.fee_collector = AccountMeta::new(accounts.fee_collector, false);

            self.accounts.escrow_authority =
                AccountMeta::new_readonly(accounts.escrow_authority, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self.accounts.energy_token_program =
                AccountMeta::new_readonly(accounts.energy_token_program, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.buy_order.clone());

            metas.push(self.accounts.sell_order.clone());

            metas.push(self.accounts.buy_payment_info.clone());

            metas.push(self.accounts.sell_payment_info.clone());

            metas.push(self.accounts.stablecoin_mint.clone());

            metas.push(self.accounts.energy_mint.clone());

            metas.push(self.accounts.buyer_stablecoin.clone());

            metas.push(self.accounts.seller_stablecoin.clone());

            metas.push(self.accounts.buyer_energy.clone());

            metas.push(self.accounts.seller_energy.clone());

            metas.push(self.accounts.fee_collector.clone());

            metas.push(self.accounts.escrow_authority.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.energy_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Initialize
    // ....................................................................

    /// Main instruction struct for Initialize
    pub struct InitializeInstruction {
        pub accounts: InitializeInstructionAccountMetas,
        pub data: InitializeInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Initialize instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeInstructionAccountMetas {
        pub authority: AccountMeta,
    }

    /// Account pubkeys for Initialize instruction
    #[derive(Debug, Clone)]
    pub struct InitializeInstructionAccounts {
        pub authority: Pubkey,
    }

    impl InitializeInstructionAccounts {
        pub fn new(authority: Pubkey) -> Self {
            Self { authority }
        }
    }

    /// Instruction data for Initialize
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeInstructionData {}

    impl InitializeInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for InitializeInstruction
    impl InitializeInstruction {
        fn discriminator() -> [u8; 8] {
            [175u8, 175u8, 109u8, 31u8, 13u8, 152u8, 155u8, 237u8]
        }

        pub fn data(data: InitializeInstructionData) -> Self {
            Self {
                accounts: InitializeInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeAmmPool
    // ....................................................................

    /// Main instruction struct for InitializeAmmPool
    pub struct InitializeAmmPoolInstruction {
        pub accounts: InitializeAmmPoolInstructionAccountMetas,
        pub data: InitializeAmmPoolInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeAmmPool instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeAmmPoolInstructionAccountMetas {
        pub pool: AccountMeta,

        pub market: AccountMeta,

        pub energy_mint: AccountMeta,

        pub currency_mint: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitializeAmmPool instruction
    #[derive(Debug, Clone)]
    pub struct InitializeAmmPoolInstructionAccounts {
        pub pool: Pubkey,

        pub market: Pubkey,

        pub energy_mint: Pubkey,

        pub currency_mint: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl InitializeAmmPoolInstructionAccounts {
        pub fn new(
            pool: Pubkey,

            market: Pubkey,

            energy_mint: Pubkey,

            currency_mint: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                pool,

                market,

                energy_mint,

                currency_mint,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for InitializeAmmPool
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeAmmPoolInstructionData {
        pub curve_type: CurveType,

        pub slope: u64,

        pub base: u64,

        pub fee_bps: u16,
    }

    impl InitializeAmmPoolInstructionData {
        pub fn new(curve_type: CurveType, slope: u64, base: u64, fee_bps: u16) -> Self {
            Self {
                curve_type,

                slope,

                base,

                fee_bps,
            }
        }
    }

    /// Implementation for InitializeAmmPoolInstruction
    impl InitializeAmmPoolInstruction {
        fn discriminator() -> [u8; 8] {
            [20u8, 58u8, 19u8, 89u8, 14u8, 193u8, 139u8, 31u8]
        }

        pub fn data(data: InitializeAmmPoolInstructionData) -> Self {
            Self {
                accounts: InitializeAmmPoolInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeAmmPoolInstructionAccounts) -> Self {
            self.accounts.pool = AccountMeta::new(accounts.pool, false);

            self.accounts.market = AccountMeta::new_readonly(accounts.market, false);

            self.accounts.energy_mint = AccountMeta::new_readonly(accounts.energy_mint, false);

            self.accounts.currency_mint = AccountMeta::new_readonly(accounts.currency_mint, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.pool.clone());

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.energy_mint.clone());

            metas.push(self.accounts.currency_mint.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeAuction
    // ....................................................................

    /// Main instruction struct for InitializeAuction
    pub struct InitializeAuctionInstruction {
        pub accounts: InitializeAuctionInstructionAccountMetas,
        pub data: InitializeAuctionInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeAuction instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeAuctionInstructionAccountMetas {
        pub batch: AccountMeta,

        pub market: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitializeAuction instruction
    #[derive(Debug, Clone)]
    pub struct InitializeAuctionInstructionAccounts {
        pub batch: Pubkey,

        pub market: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl InitializeAuctionInstructionAccounts {
        pub fn new(
            batch: Pubkey,

            market: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                batch,

                market,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for InitializeAuction
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeAuctionInstructionData {
        pub batch_id: u64,

        pub duration: i64,
    }

    impl InitializeAuctionInstructionData {
        pub fn new(batch_id: u64, duration: i64) -> Self {
            Self { batch_id, duration }
        }
    }

    /// Implementation for InitializeAuctionInstruction
    impl InitializeAuctionInstruction {
        fn discriminator() -> [u8; 8] {
            [37u8, 10u8, 117u8, 197u8, 208u8, 88u8, 117u8, 62u8]
        }

        pub fn data(data: InitializeAuctionInstructionData) -> Self {
            Self {
                accounts: InitializeAuctionInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeAuctionInstructionAccounts) -> Self {
            self.accounts.batch = AccountMeta::new(accounts.batch, false);

            self.accounts.market = AccountMeta::new_readonly(accounts.market, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.batch.clone());

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeBridge
    // ....................................................................

    /// Main instruction struct for InitializeBridge
    pub struct InitializeBridgeInstruction {
        pub accounts: InitializeBridgeInstructionAccountMetas,
        pub data: InitializeBridgeInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeBridge instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeBridgeInstructionAccountMetas {
        pub market: AccountMeta,

        pub bridge_config: AccountMeta,

        pub wormhole_program: AccountMeta,

        pub token_bridge_program: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitializeBridge instruction
    #[derive(Debug, Clone)]
    pub struct InitializeBridgeInstructionAccounts {
        pub market: Pubkey,

        pub bridge_config: Pubkey,

        pub wormhole_program: Pubkey,

        pub token_bridge_program: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl InitializeBridgeInstructionAccounts {
        pub fn new(
            market: Pubkey,

            bridge_config: Pubkey,

            wormhole_program: Pubkey,

            token_bridge_program: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                market,

                bridge_config,

                wormhole_program,

                token_bridge_program,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for InitializeBridge
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeBridgeInstructionData {
        pub min_bridge_amount: u64,

        pub bridge_fee_bps: u16,

        pub relayer_fee: u64,
    }

    impl InitializeBridgeInstructionData {
        pub fn new(min_bridge_amount: u64, bridge_fee_bps: u16, relayer_fee: u64) -> Self {
            Self {
                min_bridge_amount,

                bridge_fee_bps,

                relayer_fee,
            }
        }
    }

    /// Implementation for InitializeBridgeInstruction
    impl InitializeBridgeInstruction {
        fn discriminator() -> [u8; 8] {
            [6u8, 173u8, 152u8, 229u8, 35u8, 112u8, 127u8, 151u8]
        }

        pub fn data(data: InitializeBridgeInstructionData) -> Self {
            Self {
                accounts: InitializeBridgeInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeBridgeInstructionAccounts) -> Self {
            self.accounts.market = AccountMeta::new(accounts.market, false);

            self.accounts.bridge_config = AccountMeta::new(accounts.bridge_config, false);

            self.accounts.wormhole_program =
                AccountMeta::new_readonly(accounts.wormhole_program, false);

            self.accounts.token_bridge_program =
                AccountMeta::new_readonly(accounts.token_bridge_program, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.bridge_config.clone());

            metas.push(self.accounts.wormhole_program.clone());

            metas.push(self.accounts.token_bridge_program.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeCarbonMarketplace
    // ....................................................................

    /// Main instruction struct for InitializeCarbonMarketplace
    pub struct InitializeCarbonMarketplaceInstruction {
        pub accounts: InitializeCarbonMarketplaceInstructionAccountMetas,
        pub data: InitializeCarbonMarketplaceInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeCarbonMarketplace instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeCarbonMarketplaceInstructionAccountMetas {
        pub marketplace: AccountMeta,

        pub rec_mint: AccountMeta,

        pub carbon_mint: AccountMeta,

        pub treasury: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitializeCarbonMarketplace instruction
    #[derive(Debug, Clone)]
    pub struct InitializeCarbonMarketplaceInstructionAccounts {
        pub marketplace: Pubkey,

        pub rec_mint: Pubkey,

        pub carbon_mint: Pubkey,

        pub treasury: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl InitializeCarbonMarketplaceInstructionAccounts {
        pub fn new(
            marketplace: Pubkey,

            rec_mint: Pubkey,

            carbon_mint: Pubkey,

            treasury: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                marketplace,

                rec_mint,

                carbon_mint,

                treasury,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for InitializeCarbonMarketplace
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeCarbonMarketplaceInstructionData {
        pub minting_fee_bps: u16,

        pub trading_fee_bps: u16,

        pub kwh_to_rec_rate: u32,

        pub carbon_intensity: u32,
    }

    impl InitializeCarbonMarketplaceInstructionData {
        pub fn new(
            minting_fee_bps: u16,

            trading_fee_bps: u16,

            kwh_to_rec_rate: u32,

            carbon_intensity: u32,
        ) -> Self {
            Self {
                minting_fee_bps,

                trading_fee_bps,

                kwh_to_rec_rate,

                carbon_intensity,
            }
        }
    }

    /// Implementation for InitializeCarbonMarketplaceInstruction
    impl InitializeCarbonMarketplaceInstruction {
        fn discriminator() -> [u8; 8] {
            [28u8, 238u8, 200u8, 43u8, 93u8, 133u8, 242u8, 96u8]
        }

        pub fn data(data: InitializeCarbonMarketplaceInstructionData) -> Self {
            Self {
                accounts: InitializeCarbonMarketplaceInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(
            mut self,
            accounts: InitializeCarbonMarketplaceInstructionAccounts,
        ) -> Self {
            self.accounts.marketplace = AccountMeta::new(accounts.marketplace, false);

            self.accounts.rec_mint = AccountMeta::new_readonly(accounts.rec_mint, false);

            self.accounts.carbon_mint = AccountMeta::new_readonly(accounts.carbon_mint, false);

            self.accounts.treasury = AccountMeta::new_readonly(accounts.treasury, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.marketplace.clone());

            metas.push(self.accounts.rec_mint.clone());

            metas.push(self.accounts.carbon_mint.clone());

            metas.push(self.accounts.treasury.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeConfidentialBalance
    // ....................................................................

    /// Main instruction struct for InitializeConfidentialBalance
    pub struct InitializeConfidentialBalanceInstruction {
        pub accounts: InitializeConfidentialBalanceInstructionAccountMetas,
        pub data: InitializeConfidentialBalanceInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeConfidentialBalance instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeConfidentialBalanceInstructionAccountMetas {
        pub confidential_balance: AccountMeta,

        pub mint: AccountMeta,

        pub owner: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitializeConfidentialBalance instruction
    #[derive(Debug, Clone)]
    pub struct InitializeConfidentialBalanceInstructionAccounts {
        pub confidential_balance: Pubkey,

        pub mint: Pubkey,

        pub owner: Pubkey,

        pub system_program: Pubkey,
    }

    impl InitializeConfidentialBalanceInstructionAccounts {
        pub fn new(
            confidential_balance: Pubkey,

            mint: Pubkey,

            owner: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                confidential_balance,

                mint,

                owner,

                system_program,
            }
        }
    }

    /// Instruction data for InitializeConfidentialBalance
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeConfidentialBalanceInstructionData {}

    impl InitializeConfidentialBalanceInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for InitializeConfidentialBalanceInstruction
    impl InitializeConfidentialBalanceInstruction {
        fn discriminator() -> [u8; 8] {
            [1u8, 102u8, 22u8, 142u8, 241u8, 31u8, 99u8, 41u8]
        }

        pub fn data(data: InitializeConfidentialBalanceInstructionData) -> Self {
            Self {
                accounts: InitializeConfidentialBalanceInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(
            mut self,
            accounts: InitializeConfidentialBalanceInstructionAccounts,
        ) -> Self {
            self.accounts.confidential_balance =
                AccountMeta::new(accounts.confidential_balance, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self.accounts.owner = AccountMeta::new(accounts.owner, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.confidential_balance.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.owner.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeMarket
    // ....................................................................

    /// Main instruction struct for InitializeMarket
    pub struct InitializeMarketInstruction {
        pub accounts: InitializeMarketInstructionAccountMetas,
        pub data: InitializeMarketInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeMarket instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeMarketInstructionAccountMetas {
        pub market: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitializeMarket instruction
    #[derive(Debug, Clone)]
    pub struct InitializeMarketInstructionAccounts {
        pub market: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl InitializeMarketInstructionAccounts {
        pub fn new(market: Pubkey, authority: Pubkey, system_program: Pubkey) -> Self {
            Self {
                market,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for InitializeMarket
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeMarketInstructionData {}

    impl InitializeMarketInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for InitializeMarketInstruction
    impl InitializeMarketInstruction {
        fn discriminator() -> [u8; 8] {
            [35u8, 35u8, 189u8, 193u8, 155u8, 48u8, 170u8, 203u8]
        }

        pub fn data(data: InitializeMarketInstructionData) -> Self {
            Self {
                accounts: InitializeMarketInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeMarketInstructionAccounts) -> Self {
            self.accounts.market = AccountMeta::new(accounts.market, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeMeterConfig
    // ....................................................................

    /// Main instruction struct for InitializeMeterConfig
    pub struct InitializeMeterConfigInstruction {
        pub accounts: InitializeMeterConfigInstructionAccountMetas,
        pub data: InitializeMeterConfigInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeMeterConfig instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeMeterConfigInstructionAccountMetas {
        pub config: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitializeMeterConfig instruction
    #[derive(Debug, Clone)]
    pub struct InitializeMeterConfigInstructionAccounts {
        pub config: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl InitializeMeterConfigInstructionAccounts {
        pub fn new(config: Pubkey, authority: Pubkey, system_program: Pubkey) -> Self {
            Self {
                config,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for InitializeMeterConfig
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeMeterConfigInstructionData {
        pub max_delta_per_hour: u64,

        pub min_interval: u32,
    }

    impl InitializeMeterConfigInstructionData {
        pub fn new(max_delta_per_hour: u64, min_interval: u32) -> Self {
            Self {
                max_delta_per_hour,

                min_interval,
            }
        }
    }

    /// Implementation for InitializeMeterConfigInstruction
    impl InitializeMeterConfigInstruction {
        fn discriminator() -> [u8; 8] {
            [212u8, 143u8, 244u8, 254u8, 89u8, 91u8, 15u8, 88u8]
        }

        pub fn data(data: InitializeMeterConfigInstructionData) -> Self {
            Self {
                accounts: InitializeMeterConfigInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeMeterConfigInstructionAccounts) -> Self {
            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeMeterHistory
    // ....................................................................

    /// Main instruction struct for InitializeMeterHistory
    pub struct InitializeMeterHistoryInstruction {
        pub accounts: InitializeMeterHistoryInstructionAccountMetas,
        pub data: InitializeMeterHistoryInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeMeterHistory instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeMeterHistoryInstructionAccountMetas {
        pub history: AccountMeta,

        pub meter: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitializeMeterHistory instruction
    #[derive(Debug, Clone)]
    pub struct InitializeMeterHistoryInstructionAccounts {
        pub history: Pubkey,

        pub meter: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl InitializeMeterHistoryInstructionAccounts {
        pub fn new(
            history: Pubkey,

            meter: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                history,

                meter,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for InitializeMeterHistory
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeMeterHistoryInstructionData {}

    impl InitializeMeterHistoryInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for InitializeMeterHistoryInstruction
    impl InitializeMeterHistoryInstruction {
        fn discriminator() -> [u8; 8] {
            [44u8, 204u8, 191u8, 237u8, 59u8, 178u8, 107u8, 169u8]
        }

        pub fn data(data: InitializeMeterHistoryInstructionData) -> Self {
            Self {
                accounts: InitializeMeterHistoryInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeMeterHistoryInstructionAccounts) -> Self {
            self.accounts.history = AccountMeta::new(accounts.history, false);

            self.accounts.meter = AccountMeta::new_readonly(accounts.meter, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.history.clone());

            metas.push(self.accounts.meter.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializePricingConfig
    // ....................................................................

    /// Main instruction struct for InitializePricingConfig
    pub struct InitializePricingConfigInstruction {
        pub accounts: InitializePricingConfigInstructionAccountMetas,
        pub data: InitializePricingConfigInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializePricingConfig instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializePricingConfigInstructionAccountMetas {
        pub pricing_config: AccountMeta,

        pub market: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitializePricingConfig instruction
    #[derive(Debug, Clone)]
    pub struct InitializePricingConfigInstructionAccounts {
        pub pricing_config: Pubkey,

        pub market: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl InitializePricingConfigInstructionAccounts {
        pub fn new(
            pricing_config: Pubkey,

            market: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                pricing_config,

                market,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for InitializePricingConfig
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializePricingConfigInstructionData {
        pub base_price: u64,

        pub min_price: u64,

        pub max_price: u64,

        pub timezone_offset: i16,
    }

    impl InitializePricingConfigInstructionData {
        pub fn new(base_price: u64, min_price: u64, max_price: u64, timezone_offset: i16) -> Self {
            Self {
                base_price,

                min_price,

                max_price,

                timezone_offset,
            }
        }
    }

    /// Implementation for InitializePricingConfigInstruction
    impl InitializePricingConfigInstruction {
        fn discriminator() -> [u8; 8] {
            [86u8, 141u8, 171u8, 141u8, 30u8, 244u8, 39u8, 107u8]
        }

        pub fn data(data: InitializePricingConfigInstructionData) -> Self {
            Self {
                accounts: InitializePricingConfigInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializePricingConfigInstructionAccounts) -> Self {
            self.accounts.pricing_config = AccountMeta::new(accounts.pricing_config, false);

            self.accounts.market = AccountMeta::new_readonly(accounts.market, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.pricing_config.clone());

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitiateBridgeTransfer
    // ....................................................................

    /// Main instruction struct for InitiateBridgeTransfer
    pub struct InitiateBridgeTransferInstruction {
        pub accounts: InitiateBridgeTransferInstructionAccountMetas,
        pub data: InitiateBridgeTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitiateBridgeTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitiateBridgeTransferInstructionAccountMetas {
        pub bridge_config: AccountMeta,

        pub bridge_transfer: AccountMeta,

        pub token_mint: AccountMeta,

        pub user_token_account: AccountMeta,

        pub bridge_escrow: AccountMeta,

        pub user: AccountMeta,

        pub token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitiateBridgeTransfer instruction
    #[derive(Debug, Clone)]
    pub struct InitiateBridgeTransferInstructionAccounts {
        pub bridge_config: Pubkey,

        pub bridge_transfer: Pubkey,

        pub token_mint: Pubkey,

        pub user_token_account: Pubkey,

        pub bridge_escrow: Pubkey,

        pub user: Pubkey,

        pub token_program: Pubkey,

        pub system_program: Pubkey,
    }

    impl InitiateBridgeTransferInstructionAccounts {
        pub fn new(
            bridge_config: Pubkey,

            bridge_transfer: Pubkey,

            token_mint: Pubkey,

            user_token_account: Pubkey,

            bridge_escrow: Pubkey,

            user: Pubkey,

            token_program: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                bridge_config,

                bridge_transfer,

                token_mint,

                user_token_account,

                bridge_escrow,

                user,

                token_program,

                system_program,
            }
        }
    }

    /// Instruction data for InitiateBridgeTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitiateBridgeTransferInstructionData {
        pub destination_chain: u16,

        pub destination_address: [u8; 32],

        pub amount: u64,

        pub timestamp: u64,
    }

    impl InitiateBridgeTransferInstructionData {
        pub fn new(
            destination_chain: u16,

            destination_address: [u8; 32],

            amount: u64,

            timestamp: u64,
        ) -> Self {
            Self {
                destination_chain,

                destination_address,

                amount,

                timestamp,
            }
        }
    }

    /// Implementation for InitiateBridgeTransferInstruction
    impl InitiateBridgeTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [91u8, 245u8, 11u8, 223u8, 231u8, 116u8, 200u8, 42u8]
        }

        pub fn data(data: InitiateBridgeTransferInstructionData) -> Self {
            Self {
                accounts: InitiateBridgeTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitiateBridgeTransferInstructionAccounts) -> Self {
            self.accounts.bridge_config = AccountMeta::new_readonly(accounts.bridge_config, false);

            self.accounts.bridge_transfer = AccountMeta::new(accounts.bridge_transfer, false);

            self.accounts.token_mint = AccountMeta::new_readonly(accounts.token_mint, false);

            self.accounts.user_token_account = AccountMeta::new(accounts.user_token_account, false);

            self.accounts.bridge_escrow = AccountMeta::new(accounts.bridge_escrow, false);

            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.bridge_config.clone());

            metas.push(self.accounts.bridge_transfer.clone());

            metas.push(self.accounts.token_mint.clone());

            metas.push(self.accounts.user_token_account.clone());

            metas.push(self.accounts.bridge_escrow.clone());

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: MatchCrossChainOrder
    // ....................................................................

    /// Main instruction struct for MatchCrossChainOrder
    pub struct MatchCrossChainOrderInstruction {
        pub accounts: MatchCrossChainOrderInstructionAccountMetas,
        pub data: MatchCrossChainOrderInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for MatchCrossChainOrder instruction
    #[derive(Debug, Clone, Default)]
    pub struct MatchCrossChainOrderInstructionAccountMetas {
        pub solana_order: AccountMeta,

        pub cross_chain_order: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for MatchCrossChainOrder instruction
    #[derive(Debug, Clone)]
    pub struct MatchCrossChainOrderInstructionAccounts {
        pub solana_order: Pubkey,

        pub cross_chain_order: Pubkey,

        pub authority: Pubkey,
    }

    impl MatchCrossChainOrderInstructionAccounts {
        pub fn new(solana_order: Pubkey, cross_chain_order: Pubkey, authority: Pubkey) -> Self {
            Self {
                solana_order,

                cross_chain_order,

                authority,
            }
        }
    }

    /// Instruction data for MatchCrossChainOrder
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct MatchCrossChainOrderInstructionData {
        pub amount: u64,
    }

    impl MatchCrossChainOrderInstructionData {
        pub fn new(amount: u64) -> Self {
            Self { amount }
        }
    }

    /// Implementation for MatchCrossChainOrderInstruction
    impl MatchCrossChainOrderInstruction {
        fn discriminator() -> [u8; 8] {
            [170u8, 147u8, 117u8, 60u8, 205u8, 21u8, 60u8, 90u8]
        }

        pub fn data(data: MatchCrossChainOrderInstructionData) -> Self {
            Self {
                accounts: MatchCrossChainOrderInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: MatchCrossChainOrderInstructionAccounts) -> Self {
            self.accounts.solana_order = AccountMeta::new(accounts.solana_order, false);

            self.accounts.cross_chain_order = AccountMeta::new(accounts.cross_chain_order, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.solana_order.clone());

            metas.push(self.accounts.cross_chain_order.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: MatchOrders
    // ....................................................................

    /// Main instruction struct for MatchOrders
    pub struct MatchOrdersInstruction {
        pub accounts: MatchOrdersInstructionAccountMetas,
        pub data: MatchOrdersInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for MatchOrders instruction
    #[derive(Debug, Clone, Default)]
    pub struct MatchOrdersInstructionAccountMetas {
        pub market: AccountMeta,

        pub buy_order: AccountMeta,

        pub sell_order: AccountMeta,

        pub trade_record: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for MatchOrders instruction
    #[derive(Debug, Clone)]
    pub struct MatchOrdersInstructionAccounts {
        pub market: Pubkey,

        pub buy_order: Pubkey,

        pub sell_order: Pubkey,

        pub trade_record: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl MatchOrdersInstructionAccounts {
        pub fn new(
            market: Pubkey,

            buy_order: Pubkey,

            sell_order: Pubkey,

            trade_record: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                market,

                buy_order,

                sell_order,

                trade_record,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for MatchOrders
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct MatchOrdersInstructionData {
        pub match_amount: u64,
    }

    impl MatchOrdersInstructionData {
        pub fn new(match_amount: u64) -> Self {
            Self { match_amount }
        }
    }

    /// Implementation for MatchOrdersInstruction
    impl MatchOrdersInstruction {
        fn discriminator() -> [u8; 8] {
            [17u8, 1u8, 201u8, 93u8, 7u8, 51u8, 251u8, 134u8]
        }

        pub fn data(data: MatchOrdersInstructionData) -> Self {
            Self {
                accounts: MatchOrdersInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: MatchOrdersInstructionAccounts) -> Self {
            self.accounts.market = AccountMeta::new(accounts.market, false);

            self.accounts.buy_order = AccountMeta::new(accounts.buy_order, false);

            self.accounts.sell_order = AccountMeta::new(accounts.sell_order, false);

            self.accounts.trade_record = AccountMeta::new(accounts.trade_record, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.buy_order.clone());

            metas.push(self.accounts.sell_order.clone());

            metas.push(self.accounts.trade_record.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: MintRecCertificate
    // ....................................................................

    /// Main instruction struct for MintRecCertificate
    pub struct MintRecCertificateInstruction {
        pub accounts: MintRecCertificateInstructionAccountMetas,
        pub data: MintRecCertificateInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for MintRecCertificate instruction
    #[derive(Debug, Clone, Default)]
    pub struct MintRecCertificateInstructionAccountMetas {
        pub marketplace: AccountMeta,

        pub certificate: AccountMeta,

        pub issuer: AccountMeta,

        pub verified_reading: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for MintRecCertificate instruction
    #[derive(Debug, Clone)]
    pub struct MintRecCertificateInstructionAccounts {
        pub marketplace: Pubkey,

        pub certificate: Pubkey,

        pub issuer: Pubkey,

        pub verified_reading: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl MintRecCertificateInstructionAccounts {
        pub fn new(
            marketplace: Pubkey,

            certificate: Pubkey,

            issuer: Pubkey,

            verified_reading: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                marketplace,

                certificate,

                issuer,

                verified_reading,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for MintRecCertificate
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct MintRecCertificateInstructionData {
        pub generation_start: i64,

        pub generation_end: i64,
    }

    impl MintRecCertificateInstructionData {
        pub fn new(generation_start: i64, generation_end: i64) -> Self {
            Self {
                generation_start,

                generation_end,
            }
        }
    }

    /// Implementation for MintRecCertificateInstruction
    impl MintRecCertificateInstruction {
        fn discriminator() -> [u8; 8] {
            [60u8, 243u8, 107u8, 203u8, 199u8, 82u8, 49u8, 117u8]
        }

        pub fn data(data: MintRecCertificateInstructionData) -> Self {
            Self {
                accounts: MintRecCertificateInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: MintRecCertificateInstructionAccounts) -> Self {
            self.accounts.marketplace = AccountMeta::new(accounts.marketplace, false);

            self.accounts.certificate = AccountMeta::new(accounts.certificate, false);

            self.accounts.issuer = AccountMeta::new(accounts.issuer, true);

            self.accounts.verified_reading =
                AccountMeta::new_readonly(accounts.verified_reading, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.marketplace.clone());

            metas.push(self.accounts.certificate.clone());

            metas.push(self.accounts.issuer.clone());

            metas.push(self.accounts.verified_reading.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: PrivateTransfer
    // ....................................................................

    /// Main instruction struct for PrivateTransfer
    pub struct PrivateTransferInstruction {
        pub accounts: PrivateTransferInstructionAccountMetas,
        pub data: PrivateTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for PrivateTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct PrivateTransferInstructionAccountMetas {
        pub sender_balance: AccountMeta,

        pub receiver_balance: AccountMeta,

        pub receiver_owner: AccountMeta,

        pub mint: AccountMeta,

        pub owner: AccountMeta,
    }

    /// Account pubkeys for PrivateTransfer instruction
    #[derive(Debug, Clone)]
    pub struct PrivateTransferInstructionAccounts {
        pub sender_balance: Pubkey,

        pub receiver_balance: Pubkey,

        pub receiver_owner: Pubkey,

        pub mint: Pubkey,

        pub owner: Pubkey,
    }

    impl PrivateTransferInstructionAccounts {
        pub fn new(
            sender_balance: Pubkey,

            receiver_balance: Pubkey,

            receiver_owner: Pubkey,

            mint: Pubkey,

            owner: Pubkey,
        ) -> Self {
            Self {
                sender_balance,

                receiver_balance,

                receiver_owner,

                mint,

                owner,
            }
        }
    }

    /// Instruction data for PrivateTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct PrivateTransferInstructionData {
        pub amount: u64,

        pub encrypted_amount: ElGamalCiphertext,

        pub proof: TransferProof,
    }

    impl PrivateTransferInstructionData {
        pub fn new(amount: u64, encrypted_amount: ElGamalCiphertext, proof: TransferProof) -> Self {
            Self {
                amount,

                encrypted_amount,

                proof,
            }
        }
    }

    /// Implementation for PrivateTransferInstruction
    impl PrivateTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [107u8, 20u8, 177u8, 94u8, 33u8, 119u8, 16u8, 110u8]
        }

        pub fn data(data: PrivateTransferInstructionData) -> Self {
            Self {
                accounts: PrivateTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: PrivateTransferInstructionAccounts) -> Self {
            self.accounts.sender_balance = AccountMeta::new(accounts.sender_balance, false);

            self.accounts.receiver_balance = AccountMeta::new(accounts.receiver_balance, false);

            self.accounts.receiver_owner =
                AccountMeta::new_readonly(accounts.receiver_owner, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self.accounts.owner = AccountMeta::new(accounts.owner, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.sender_balance.clone());

            metas.push(self.accounts.receiver_balance.clone());

            metas.push(self.accounts.receiver_owner.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.owner.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ResolveAuction
    // ....................................................................

    /// Main instruction struct for ResolveAuction
    pub struct ResolveAuctionInstruction {
        pub accounts: ResolveAuctionInstructionAccountMetas,
        pub data: ResolveAuctionInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ResolveAuction instruction
    #[derive(Debug, Clone, Default)]
    pub struct ResolveAuctionInstructionAccountMetas {
        pub batch: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for ResolveAuction instruction
    #[derive(Debug, Clone)]
    pub struct ResolveAuctionInstructionAccounts {
        pub batch: Pubkey,

        pub authority: Pubkey,
    }

    impl ResolveAuctionInstructionAccounts {
        pub fn new(batch: Pubkey, authority: Pubkey) -> Self {
            Self { batch, authority }
        }
    }

    /// Instruction data for ResolveAuction
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ResolveAuctionInstructionData {}

    impl ResolveAuctionInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for ResolveAuctionInstruction
    impl ResolveAuctionInstruction {
        fn discriminator() -> [u8; 8] {
            [191u8, 112u8, 64u8, 241u8, 38u8, 232u8, 227u8, 26u8]
        }

        pub fn data(data: ResolveAuctionInstructionData) -> Self {
            Self {
                accounts: ResolveAuctionInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ResolveAuctionInstructionAccounts) -> Self {
            self.accounts.batch = AccountMeta::new(accounts.batch, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.batch.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ShieldEnergy
    // ....................................................................

    /// Main instruction struct for ShieldEnergy
    pub struct ShieldEnergyInstruction {
        pub accounts: ShieldEnergyInstructionAccountMetas,
        pub data: ShieldEnergyInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ShieldEnergy instruction
    #[derive(Debug, Clone, Default)]
    pub struct ShieldEnergyInstructionAccountMetas {
        pub confidential_balance: AccountMeta,

        pub mint: AccountMeta,

        pub user_token_account: AccountMeta,

        pub owner: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for ShieldEnergy instruction
    #[derive(Debug, Clone)]
    pub struct ShieldEnergyInstructionAccounts {
        pub confidential_balance: Pubkey,

        pub mint: Pubkey,

        pub user_token_account: Pubkey,

        pub owner: Pubkey,

        pub token_program: Pubkey,
    }

    impl ShieldEnergyInstructionAccounts {
        pub fn new(
            confidential_balance: Pubkey,

            mint: Pubkey,

            user_token_account: Pubkey,

            owner: Pubkey,

            token_program: Pubkey,
        ) -> Self {
            Self {
                confidential_balance,

                mint,

                user_token_account,

                owner,

                token_program,
            }
        }
    }

    /// Instruction data for ShieldEnergy
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ShieldEnergyInstructionData {
        pub amount: u64,

        pub encrypted_amount: ElGamalCiphertext,

        pub proof: RangeProof,
    }

    impl ShieldEnergyInstructionData {
        pub fn new(amount: u64, encrypted_amount: ElGamalCiphertext, proof: RangeProof) -> Self {
            Self {
                amount,

                encrypted_amount,

                proof,
            }
        }
    }

    /// Implementation for ShieldEnergyInstruction
    impl ShieldEnergyInstruction {
        fn discriminator() -> [u8; 8] {
            [2u8, 240u8, 24u8, 223u8, 148u8, 177u8, 176u8, 206u8]
        }

        pub fn data(data: ShieldEnergyInstructionData) -> Self {
            Self {
                accounts: ShieldEnergyInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ShieldEnergyInstructionAccounts) -> Self {
            self.accounts.confidential_balance =
                AccountMeta::new(accounts.confidential_balance, false);

            self.accounts.mint = AccountMeta::new(accounts.mint, false);

            self.accounts.user_token_account = AccountMeta::new(accounts.user_token_account, false);

            self.accounts.owner = AccountMeta::new(accounts.owner, true);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.confidential_balance.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.user_token_account.clone());

            metas.push(self.accounts.owner.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: SubmitAuctionOrder
    // ....................................................................

    /// Main instruction struct for SubmitAuctionOrder
    pub struct SubmitAuctionOrderInstruction {
        pub accounts: SubmitAuctionOrderInstructionAccountMetas,
        pub data: SubmitAuctionOrderInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for SubmitAuctionOrder instruction
    #[derive(Debug, Clone, Default)]
    pub struct SubmitAuctionOrderInstructionAccountMetas {
        pub batch: AccountMeta,

        pub user_token_account: AccountMeta,

        pub vault: AccountMeta,

        pub token_mint: AccountMeta,

        pub authority: AccountMeta,

        pub token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for SubmitAuctionOrder instruction
    #[derive(Debug, Clone)]
    pub struct SubmitAuctionOrderInstructionAccounts {
        pub batch: Pubkey,

        pub user_token_account: Pubkey,

        pub vault: Pubkey,

        pub token_mint: Pubkey,

        pub authority: Pubkey,

        pub token_program: Pubkey,

        pub system_program: Pubkey,
    }

    impl SubmitAuctionOrderInstructionAccounts {
        pub fn new(
            batch: Pubkey,

            user_token_account: Pubkey,

            vault: Pubkey,

            token_mint: Pubkey,

            authority: Pubkey,

            token_program: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                batch,

                user_token_account,

                vault,

                token_mint,

                authority,

                token_program,

                system_program,
            }
        }
    }

    /// Instruction data for SubmitAuctionOrder
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct SubmitAuctionOrderInstructionData {
        pub price: u64,

        pub amount: u64,

        pub is_bid: bool,
    }

    impl SubmitAuctionOrderInstructionData {
        pub fn new(price: u64, amount: u64, is_bid: bool) -> Self {
            Self {
                price,

                amount,

                is_bid,
            }
        }
    }

    /// Implementation for SubmitAuctionOrderInstruction
    impl SubmitAuctionOrderInstruction {
        fn discriminator() -> [u8; 8] {
            [158u8, 126u8, 157u8, 69u8, 54u8, 69u8, 146u8, 19u8]
        }

        pub fn data(data: SubmitAuctionOrderInstructionData) -> Self {
            Self {
                accounts: SubmitAuctionOrderInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: SubmitAuctionOrderInstructionAccounts) -> Self {
            self.accounts.batch = AccountMeta::new(accounts.batch, false);

            self.accounts.user_token_account = AccountMeta::new(accounts.user_token_account, false);

            self.accounts.vault = AccountMeta::new(accounts.vault, false);

            self.accounts.token_mint = AccountMeta::new_readonly(accounts.token_mint, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.batch.clone());

            metas.push(self.accounts.user_token_account.clone());

            metas.push(self.accounts.vault.clone());

            metas.push(self.accounts.token_mint.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: SwapBuyEnergy
    // ....................................................................

    /// Main instruction struct for SwapBuyEnergy
    pub struct SwapBuyEnergyInstruction {
        pub accounts: SwapBuyEnergyInstructionAccountMetas,
        pub data: SwapBuyEnergyInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for SwapBuyEnergy instruction
    #[derive(Debug, Clone, Default)]
    pub struct SwapBuyEnergyInstructionAccountMetas {
        pub pool: AccountMeta,

        pub user_energy_account: AccountMeta,

        pub user_currency_account: AccountMeta,

        pub pool_energy_vault: AccountMeta,

        pub pool_currency_vault: AccountMeta,

        pub energy_mint: AccountMeta,

        pub currency_mint: AccountMeta,

        pub user: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for SwapBuyEnergy instruction
    #[derive(Debug, Clone)]
    pub struct SwapBuyEnergyInstructionAccounts {
        pub pool: Pubkey,

        pub user_energy_account: Pubkey,

        pub user_currency_account: Pubkey,

        pub pool_energy_vault: Pubkey,

        pub pool_currency_vault: Pubkey,

        pub energy_mint: Pubkey,

        pub currency_mint: Pubkey,

        pub user: Pubkey,

        pub token_program: Pubkey,
    }

    impl SwapBuyEnergyInstructionAccounts {
        pub fn new(
            pool: Pubkey,

            user_energy_account: Pubkey,

            user_currency_account: Pubkey,

            pool_energy_vault: Pubkey,

            pool_currency_vault: Pubkey,

            energy_mint: Pubkey,

            currency_mint: Pubkey,

            user: Pubkey,

            token_program: Pubkey,
        ) -> Self {
            Self {
                pool,

                user_energy_account,

                user_currency_account,

                pool_energy_vault,

                pool_currency_vault,

                energy_mint,

                currency_mint,

                user,

                token_program,
            }
        }
    }

    /// Instruction data for SwapBuyEnergy
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct SwapBuyEnergyInstructionData {
        pub amount_milli_kwh: u64,

        pub max_currency: u64,
    }

    impl SwapBuyEnergyInstructionData {
        pub fn new(amount_milli_kwh: u64, max_currency: u64) -> Self {
            Self {
                amount_milli_kwh,

                max_currency,
            }
        }
    }

    /// Implementation for SwapBuyEnergyInstruction
    impl SwapBuyEnergyInstruction {
        fn discriminator() -> [u8; 8] {
            [72u8, 84u8, 79u8, 31u8, 14u8, 54u8, 64u8, 73u8]
        }

        pub fn data(data: SwapBuyEnergyInstructionData) -> Self {
            Self {
                accounts: SwapBuyEnergyInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: SwapBuyEnergyInstructionAccounts) -> Self {
            self.accounts.pool = AccountMeta::new(accounts.pool, false);

            self.accounts.user_energy_account =
                AccountMeta::new(accounts.user_energy_account, false);

            self.accounts.user_currency_account =
                AccountMeta::new(accounts.user_currency_account, false);

            self.accounts.pool_energy_vault = AccountMeta::new(accounts.pool_energy_vault, false);

            self.accounts.pool_currency_vault =
                AccountMeta::new(accounts.pool_currency_vault, false);

            self.accounts.energy_mint = AccountMeta::new_readonly(accounts.energy_mint, false);

            self.accounts.currency_mint = AccountMeta::new_readonly(accounts.currency_mint, false);

            self.accounts.user = AccountMeta::new_readonly(accounts.user, true);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.pool.clone());

            metas.push(self.accounts.user_energy_account.clone());

            metas.push(self.accounts.user_currency_account.clone());

            metas.push(self.accounts.pool_energy_vault.clone());

            metas.push(self.accounts.pool_currency_vault.clone());

            metas.push(self.accounts.energy_mint.clone());

            metas.push(self.accounts.currency_mint.clone());

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: TransferCarbonCredits
    // ....................................................................

    /// Main instruction struct for TransferCarbonCredits
    pub struct TransferCarbonCreditsInstruction {
        pub accounts: TransferCarbonCreditsInstructionAccountMetas,
        pub data: TransferCarbonCreditsInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for TransferCarbonCredits instruction
    #[derive(Debug, Clone, Default)]
    pub struct TransferCarbonCreditsInstructionAccountMetas {
        pub sender: AccountMeta,

        pub receiver: AccountMeta,

        pub sender_rec_account: AccountMeta,

        pub receiver_rec_account: AccountMeta,

        pub rec_mint: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for TransferCarbonCredits instruction
    #[derive(Debug, Clone)]
    pub struct TransferCarbonCreditsInstructionAccounts {
        pub sender: Pubkey,

        pub receiver: Pubkey,

        pub sender_rec_account: Pubkey,

        pub receiver_rec_account: Pubkey,

        pub rec_mint: Pubkey,

        pub token_program: Pubkey,
    }

    impl TransferCarbonCreditsInstructionAccounts {
        pub fn new(
            sender: Pubkey,

            receiver: Pubkey,

            sender_rec_account: Pubkey,

            receiver_rec_account: Pubkey,

            rec_mint: Pubkey,

            token_program: Pubkey,
        ) -> Self {
            Self {
                sender,

                receiver,

                sender_rec_account,

                receiver_rec_account,

                rec_mint,

                token_program,
            }
        }
    }

    /// Instruction data for TransferCarbonCredits
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct TransferCarbonCreditsInstructionData {
        pub amount: u64,
    }

    impl TransferCarbonCreditsInstructionData {
        pub fn new(amount: u64) -> Self {
            Self { amount }
        }
    }

    /// Implementation for TransferCarbonCreditsInstruction
    impl TransferCarbonCreditsInstruction {
        fn discriminator() -> [u8; 8] {
            [113u8, 160u8, 162u8, 2u8, 155u8, 207u8, 160u8, 221u8]
        }

        pub fn data(data: TransferCarbonCreditsInstructionData) -> Self {
            Self {
                accounts: TransferCarbonCreditsInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: TransferCarbonCreditsInstructionAccounts) -> Self {
            self.accounts.sender = AccountMeta::new(accounts.sender, true);

            self.accounts.receiver = AccountMeta::new_readonly(accounts.receiver, false);

            self.accounts.sender_rec_account = AccountMeta::new(accounts.sender_rec_account, false);

            self.accounts.receiver_rec_account =
                AccountMeta::new(accounts.receiver_rec_account, false);

            self.accounts.rec_mint = AccountMeta::new_readonly(accounts.rec_mint, false);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.sender.clone());

            metas.push(self.accounts.receiver.clone());

            metas.push(self.accounts.sender_rec_account.clone());

            metas.push(self.accounts.receiver_rec_account.clone());

            metas.push(self.accounts.rec_mint.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: UnshieldEnergy
    // ....................................................................

    /// Main instruction struct for UnshieldEnergy
    pub struct UnshieldEnergyInstruction {
        pub accounts: UnshieldEnergyInstructionAccountMetas,
        pub data: UnshieldEnergyInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for UnshieldEnergy instruction
    #[derive(Debug, Clone, Default)]
    pub struct UnshieldEnergyInstructionAccountMetas {
        pub confidential_balance: AccountMeta,

        pub mint: AccountMeta,

        pub user_token_account: AccountMeta,

        pub mint_authority: AccountMeta,

        pub owner: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for UnshieldEnergy instruction
    #[derive(Debug, Clone)]
    pub struct UnshieldEnergyInstructionAccounts {
        pub confidential_balance: Pubkey,

        pub mint: Pubkey,

        pub user_token_account: Pubkey,

        pub mint_authority: Pubkey,

        pub owner: Pubkey,

        pub token_program: Pubkey,
    }

    impl UnshieldEnergyInstructionAccounts {
        pub fn new(
            confidential_balance: Pubkey,

            mint: Pubkey,

            user_token_account: Pubkey,

            mint_authority: Pubkey,

            owner: Pubkey,

            token_program: Pubkey,
        ) -> Self {
            Self {
                confidential_balance,

                mint,

                user_token_account,

                mint_authority,

                owner,

                token_program,
            }
        }
    }

    /// Instruction data for UnshieldEnergy
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UnshieldEnergyInstructionData {
        pub amount: u64,

        pub new_encrypted_amount: ElGamalCiphertext,

        pub proof: TransferProof,
    }

    impl UnshieldEnergyInstructionData {
        pub fn new(
            amount: u64,

            new_encrypted_amount: ElGamalCiphertext,

            proof: TransferProof,
        ) -> Self {
            Self {
                amount,

                new_encrypted_amount,

                proof,
            }
        }
    }

    /// Implementation for UnshieldEnergyInstruction
    impl UnshieldEnergyInstruction {
        fn discriminator() -> [u8; 8] {
            [211u8, 99u8, 73u8, 96u8, 249u8, 157u8, 125u8, 46u8]
        }

        pub fn data(data: UnshieldEnergyInstructionData) -> Self {
            Self {
                accounts: UnshieldEnergyInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UnshieldEnergyInstructionAccounts) -> Self {
            self.accounts.confidential_balance =
                AccountMeta::new(accounts.confidential_balance, false);

            self.accounts.mint = AccountMeta::new(accounts.mint, false);

            self.accounts.user_token_account = AccountMeta::new(accounts.user_token_account, false);

            self.accounts.mint_authority =
                AccountMeta::new_readonly(accounts.mint_authority, false);

            self.accounts.owner = AccountMeta::new(accounts.owner, true);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.confidential_balance.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.user_token_account.clone());

            metas.push(self.accounts.mint_authority.clone());

            metas.push(self.accounts.owner.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: UpdateBatchConfig
    // ....................................................................

    /// Main instruction struct for UpdateBatchConfig
    pub struct UpdateBatchConfigInstruction {
        pub accounts: UpdateBatchConfigInstructionAccountMetas,
        pub data: UpdateBatchConfigInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for UpdateBatchConfig instruction
    #[derive(Debug, Clone, Default)]
    pub struct UpdateBatchConfigInstructionAccountMetas {
        pub market: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for UpdateBatchConfig instruction
    #[derive(Debug, Clone)]
    pub struct UpdateBatchConfigInstructionAccounts {
        pub market: Pubkey,

        pub authority: Pubkey,
    }

    impl UpdateBatchConfigInstructionAccounts {
        pub fn new(market: Pubkey, authority: Pubkey) -> Self {
            Self { market, authority }
        }
    }

    /// Instruction data for UpdateBatchConfig
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UpdateBatchConfigInstructionData {
        pub config: BatchConfig,
    }

    impl UpdateBatchConfigInstructionData {
        pub fn new(config: BatchConfig) -> Self {
            Self { config }
        }
    }

    /// Implementation for UpdateBatchConfigInstruction
    impl UpdateBatchConfigInstruction {
        fn discriminator() -> [u8; 8] {
            [191u8, 189u8, 236u8, 146u8, 4u8, 221u8, 31u8, 192u8]
        }

        pub fn data(data: UpdateBatchConfigInstructionData) -> Self {
            Self {
                accounts: UpdateBatchConfigInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UpdateBatchConfigInstructionAccounts) -> Self {
            self.accounts.market = AccountMeta::new(accounts.market, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: UpdateMarketData
    // ....................................................................

    /// Main instruction struct for UpdateMarketData
    pub struct UpdateMarketDataInstruction {
        pub accounts: UpdateMarketDataInstructionAccountMetas,
        pub data: UpdateMarketDataInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for UpdateMarketData instruction
    #[derive(Debug, Clone, Default)]
    pub struct UpdateMarketDataInstructionAccountMetas {
        pub pricing_config: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for UpdateMarketData instruction
    #[derive(Debug, Clone)]
    pub struct UpdateMarketDataInstructionAccounts {
        pub pricing_config: Pubkey,

        pub authority: Pubkey,
    }

    impl UpdateMarketDataInstructionAccounts {
        pub fn new(pricing_config: Pubkey, authority: Pubkey) -> Self {
            Self {
                pricing_config,

                authority,
            }
        }
    }

    /// Instruction data for UpdateMarketData
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UpdateMarketDataInstructionData {
        pub supply: u64,

        pub demand: u64,

        pub congestion_factor: u16,
    }

    impl UpdateMarketDataInstructionData {
        pub fn new(supply: u64, demand: u64, congestion_factor: u16) -> Self {
            Self {
                supply,

                demand,

                congestion_factor,
            }
        }
    }

    /// Implementation for UpdateMarketDataInstruction
    impl UpdateMarketDataInstruction {
        fn discriminator() -> [u8; 8] {
            [38u8, 27u8, 206u8, 55u8, 6u8, 151u8, 187u8, 10u8]
        }

        pub fn data(data: UpdateMarketDataInstructionData) -> Self {
            Self {
                accounts: UpdateMarketDataInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UpdateMarketDataInstructionAccounts) -> Self {
            self.accounts.pricing_config = AccountMeta::new(accounts.pricing_config, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.pricing_config.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: UpdateMarketParams
    // ....................................................................

    /// Main instruction struct for UpdateMarketParams
    pub struct UpdateMarketParamsInstruction {
        pub accounts: UpdateMarketParamsInstructionAccountMetas,
        pub data: UpdateMarketParamsInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for UpdateMarketParams instruction
    #[derive(Debug, Clone, Default)]
    pub struct UpdateMarketParamsInstructionAccountMetas {
        pub market: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for UpdateMarketParams instruction
    #[derive(Debug, Clone)]
    pub struct UpdateMarketParamsInstructionAccounts {
        pub market: Pubkey,

        pub authority: Pubkey,
    }

    impl UpdateMarketParamsInstructionAccounts {
        pub fn new(market: Pubkey, authority: Pubkey) -> Self {
            Self { market, authority }
        }
    }

    /// Instruction data for UpdateMarketParams
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UpdateMarketParamsInstructionData {
        pub market_fee_bps: u16,

        pub clearing_enabled: bool,
    }

    impl UpdateMarketParamsInstructionData {
        pub fn new(market_fee_bps: u16, clearing_enabled: bool) -> Self {
            Self {
                market_fee_bps,

                clearing_enabled,
            }
        }
    }

    /// Implementation for UpdateMarketParamsInstruction
    impl UpdateMarketParamsInstruction {
        fn discriminator() -> [u8; 8] {
            [70u8, 117u8, 202u8, 191u8, 205u8, 174u8, 92u8, 82u8]
        }

        pub fn data(data: UpdateMarketParamsInstructionData) -> Self {
            Self {
                accounts: UpdateMarketParamsInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UpdateMarketParamsInstructionAccounts) -> Self {
            self.accounts.market = AccountMeta::new(accounts.market, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.market.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: VerifyMeterReading
    // ....................................................................

    /// Main instruction struct for VerifyMeterReading
    pub struct VerifyMeterReadingInstruction {
        pub accounts: VerifyMeterReadingInstructionAccountMetas,
        pub data: VerifyMeterReadingInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for VerifyMeterReading instruction
    #[derive(Debug, Clone, Default)]
    pub struct VerifyMeterReadingInstructionAccountMetas {
        pub config: AccountMeta,

        pub history: AccountMeta,

        pub verified_reading: AccountMeta,

        pub authority: AccountMeta,

        pub token_mint: AccountMeta,

        pub user_token_account: AccountMeta,

        pub token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for VerifyMeterReading instruction
    #[derive(Debug, Clone)]
    pub struct VerifyMeterReadingInstructionAccounts {
        pub config: Pubkey,

        pub history: Pubkey,

        pub verified_reading: Pubkey,

        pub authority: Pubkey,

        pub token_mint: Pubkey,

        pub user_token_account: Pubkey,

        pub token_program: Pubkey,

        pub system_program: Pubkey,
    }

    impl VerifyMeterReadingInstructionAccounts {
        pub fn new(
            config: Pubkey,

            history: Pubkey,

            verified_reading: Pubkey,

            authority: Pubkey,

            token_mint: Pubkey,

            user_token_account: Pubkey,

            token_program: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                config,

                history,

                verified_reading,

                authority,

                token_mint,

                user_token_account,

                token_program,

                system_program,
            }
        }
    }

    /// Instruction data for VerifyMeterReading
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct VerifyMeterReadingInstructionData {
        pub reading_proof: MeterReadingProof,

        pub timestamp: i64,
    }

    impl VerifyMeterReadingInstructionData {
        pub fn new(reading_proof: MeterReadingProof, timestamp: i64) -> Self {
            Self {
                reading_proof,

                timestamp,
            }
        }
    }

    /// Implementation for VerifyMeterReadingInstruction
    impl VerifyMeterReadingInstruction {
        fn discriminator() -> [u8; 8] {
            [241u8, 21u8, 121u8, 191u8, 164u8, 130u8, 56u8, 206u8]
        }

        pub fn data(data: VerifyMeterReadingInstructionData) -> Self {
            Self {
                accounts: VerifyMeterReadingInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: VerifyMeterReadingInstructionAccounts) -> Self {
            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.history = AccountMeta::new(accounts.history, false);

            self.accounts.verified_reading = AccountMeta::new(accounts.verified_reading, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.token_mint = AccountMeta::new(accounts.token_mint, false);

            self.accounts.user_token_account = AccountMeta::new(accounts.user_token_account, false);

            self.accounts.token_program = AccountMeta::new_readonly(accounts.token_program, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.history.clone());

            metas.push(self.accounts.verified_reading.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.token_mint.clone());

            metas.push(self.accounts.user_token_account.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ------------------------------------------------------------------------
    // Composite Accounts
    // ------------------------------------------------------------------------
}

// ----------------------------------------------------------------------------
// Program: governance
// ----------------------------------------------------------------------------
pub mod governance {
    use super::*;

    // ------------------------------------------------------------------------
    // Program ID
    // ------------------------------------------------------------------------

    /// Returns the program ID for governance
    pub fn program_id() -> Pubkey {
        pubkey!("8bNpJqZoqqUWKu55VWhR8LWS66BX7NPpwgYBAKhBzu2L")
    }

    // ------------------------------------------------------------------------
    // Instructions
    // ------------------------------------------------------------------------

    // ....................................................................
    // Instruction: ApproveAuthorityChange
    // ....................................................................

    /// Main instruction struct for ApproveAuthorityChange
    pub struct ApproveAuthorityChangeInstruction {
        pub accounts: ApproveAuthorityChangeInstructionAccountMetas,
        pub data: ApproveAuthorityChangeInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ApproveAuthorityChange instruction
    #[derive(Debug, Clone, Default)]
    pub struct ApproveAuthorityChangeInstructionAccountMetas {
        pub poa_config: AccountMeta,

        pub new_authority: AccountMeta,
    }

    /// Account pubkeys for ApproveAuthorityChange instruction
    #[derive(Debug, Clone)]
    pub struct ApproveAuthorityChangeInstructionAccounts {
        pub poa_config: Pubkey,

        pub new_authority: Pubkey,
    }

    impl ApproveAuthorityChangeInstructionAccounts {
        pub fn new(poa_config: Pubkey, new_authority: Pubkey) -> Self {
            Self {
                poa_config,

                new_authority,
            }
        }
    }

    /// Instruction data for ApproveAuthorityChange
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ApproveAuthorityChangeInstructionData {}

    impl ApproveAuthorityChangeInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for ApproveAuthorityChangeInstruction
    impl ApproveAuthorityChangeInstruction {
        fn discriminator() -> [u8; 8] {
            [21u8, 188u8, 156u8, 246u8, 104u8, 125u8, 107u8, 124u8]
        }

        pub fn data(data: ApproveAuthorityChangeInstructionData) -> Self {
            Self {
                accounts: ApproveAuthorityChangeInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ApproveAuthorityChangeInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new(accounts.poa_config, false);

            self.accounts.new_authority = AccountMeta::new_readonly(accounts.new_authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.push(self.accounts.new_authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CancelAuthorityChange
    // ....................................................................

    /// Main instruction struct for CancelAuthorityChange
    pub struct CancelAuthorityChangeInstruction {
        pub accounts: CancelAuthorityChangeInstructionAccountMetas,
        pub data: CancelAuthorityChangeInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CancelAuthorityChange instruction
    #[derive(Debug, Clone, Default)]
    pub struct CancelAuthorityChangeInstructionAccountMetas {
        pub poa_config: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for CancelAuthorityChange instruction
    #[derive(Debug, Clone)]
    pub struct CancelAuthorityChangeInstructionAccounts {
        pub poa_config: Pubkey,

        pub authority: Pubkey,
    }

    impl CancelAuthorityChangeInstructionAccounts {
        pub fn new(poa_config: Pubkey, authority: Pubkey) -> Self {
            Self {
                poa_config,

                authority,
            }
        }
    }

    /// Instruction data for CancelAuthorityChange
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CancelAuthorityChangeInstructionData {}

    impl CancelAuthorityChangeInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for CancelAuthorityChangeInstruction
    impl CancelAuthorityChangeInstruction {
        fn discriminator() -> [u8; 8] {
            [254u8, 193u8, 5u8, 128u8, 8u8, 87u8, 30u8, 194u8]
        }

        pub fn data(data: CancelAuthorityChangeInstructionData) -> Self {
            Self {
                accounts: CancelAuthorityChangeInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CancelAuthorityChangeInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new(accounts.poa_config, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: EmergencyPause
    // ....................................................................

    /// Main instruction struct for EmergencyPause
    pub struct EmergencyPauseInstruction {
        pub accounts: EmergencyPauseInstructionAccountMetas,
        pub data: EmergencyPauseInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for EmergencyPause instruction
    #[derive(Debug, Clone, Default)]
    pub struct EmergencyPauseInstructionAccountMetas {
        pub poa_config: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for EmergencyPause instruction
    #[derive(Debug, Clone)]
    pub struct EmergencyPauseInstructionAccounts {
        pub poa_config: Pubkey,

        pub authority: Pubkey,
    }

    impl EmergencyPauseInstructionAccounts {
        pub fn new(poa_config: Pubkey, authority: Pubkey) -> Self {
            Self {
                poa_config,

                authority,
            }
        }
    }

    /// Instruction data for EmergencyPause
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct EmergencyPauseInstructionData {}

    impl EmergencyPauseInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for EmergencyPauseInstruction
    impl EmergencyPauseInstruction {
        fn discriminator() -> [u8; 8] {
            [21u8, 143u8, 27u8, 142u8, 200u8, 181u8, 210u8, 255u8]
        }

        pub fn data(data: EmergencyPauseInstructionData) -> Self {
            Self {
                accounts: EmergencyPauseInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: EmergencyPauseInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new(accounts.poa_config, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: EmergencyUnpause
    // ....................................................................

    /// Main instruction struct for EmergencyUnpause
    pub struct EmergencyUnpauseInstruction {
        pub accounts: EmergencyUnpauseInstructionAccountMetas,
        pub data: EmergencyUnpauseInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for EmergencyUnpause instruction
    #[derive(Debug, Clone, Default)]
    pub struct EmergencyUnpauseInstructionAccountMetas {
        pub poa_config: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for EmergencyUnpause instruction
    #[derive(Debug, Clone)]
    pub struct EmergencyUnpauseInstructionAccounts {
        pub poa_config: Pubkey,

        pub authority: Pubkey,
    }

    impl EmergencyUnpauseInstructionAccounts {
        pub fn new(poa_config: Pubkey, authority: Pubkey) -> Self {
            Self {
                poa_config,

                authority,
            }
        }
    }

    /// Instruction data for EmergencyUnpause
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct EmergencyUnpauseInstructionData {}

    impl EmergencyUnpauseInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for EmergencyUnpauseInstruction
    impl EmergencyUnpauseInstruction {
        fn discriminator() -> [u8; 8] {
            [83u8, 249u8, 195u8, 57u8, 206u8, 189u8, 31u8, 85u8]
        }

        pub fn data(data: EmergencyUnpauseInstructionData) -> Self {
            Self {
                accounts: EmergencyUnpauseInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: EmergencyUnpauseInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new(accounts.poa_config, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: GetGovernanceStats
    // ....................................................................

    /// Main instruction struct for GetGovernanceStats
    pub struct GetGovernanceStatsInstruction {
        pub accounts: GetGovernanceStatsInstructionAccountMetas,
        pub data: GetGovernanceStatsInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for GetGovernanceStats instruction
    #[derive(Debug, Clone, Default)]
    pub struct GetGovernanceStatsInstructionAccountMetas {
        pub poa_config: AccountMeta,
    }

    /// Account pubkeys for GetGovernanceStats instruction
    #[derive(Debug, Clone)]
    pub struct GetGovernanceStatsInstructionAccounts {
        pub poa_config: Pubkey,
    }

    impl GetGovernanceStatsInstructionAccounts {
        pub fn new(poa_config: Pubkey) -> Self {
            Self { poa_config }
        }
    }

    /// Instruction data for GetGovernanceStats
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct GetGovernanceStatsInstructionData {}

    impl GetGovernanceStatsInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for GetGovernanceStatsInstruction
    impl GetGovernanceStatsInstruction {
        fn discriminator() -> [u8; 8] {
            [67u8, 29u8, 241u8, 120u8, 15u8, 8u8, 9u8, 16u8]
        }

        pub fn data(data: GetGovernanceStatsInstructionData) -> Self {
            Self {
                accounts: GetGovernanceStatsInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: GetGovernanceStatsInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new_readonly(accounts.poa_config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializePoa
    // ....................................................................

    /// Main instruction struct for InitializePoa
    pub struct InitializePoaInstruction {
        pub accounts: InitializePoaInstructionAccountMetas,
        pub data: InitializePoaInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializePoa instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializePoaInstructionAccountMetas {
        pub poa_config: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitializePoa instruction
    #[derive(Debug, Clone)]
    pub struct InitializePoaInstructionAccounts {
        pub poa_config: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl InitializePoaInstructionAccounts {
        pub fn new(poa_config: Pubkey, authority: Pubkey, system_program: Pubkey) -> Self {
            Self {
                poa_config,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for InitializePoa
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializePoaInstructionData {}

    impl InitializePoaInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for InitializePoaInstruction
    impl InitializePoaInstruction {
        fn discriminator() -> [u8; 8] {
            [98u8, 199u8, 82u8, 10u8, 244u8, 161u8, 157u8, 46u8]
        }

        pub fn data(data: InitializePoaInstructionData) -> Self {
            Self {
                accounts: InitializePoaInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializePoaInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new(accounts.poa_config, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: IssueErc
    // ....................................................................

    /// Main instruction struct for IssueErc
    pub struct IssueErcInstruction {
        pub accounts: IssueErcInstructionAccountMetas,
        pub data: IssueErcInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for IssueErc instruction
    #[derive(Debug, Clone, Default)]
    pub struct IssueErcInstructionAccountMetas {
        pub poa_config: AccountMeta,

        pub erc_certificate: AccountMeta,

        pub meter_account: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for IssueErc instruction
    #[derive(Debug, Clone)]
    pub struct IssueErcInstructionAccounts {
        pub poa_config: Pubkey,

        pub erc_certificate: Pubkey,

        pub meter_account: Pubkey,

        pub authority: Pubkey,

        pub system_program: Pubkey,
    }

    impl IssueErcInstructionAccounts {
        pub fn new(
            poa_config: Pubkey,

            erc_certificate: Pubkey,

            meter_account: Pubkey,

            authority: Pubkey,

            system_program: Pubkey,
        ) -> Self {
            Self {
                poa_config,

                erc_certificate,

                meter_account,

                authority,

                system_program,
            }
        }
    }

    /// Instruction data for IssueErc
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct IssueErcInstructionData {
        pub certificate_id: String,

        pub energy_amount: u64,

        pub renewable_source: String,

        pub validation_data: String,
    }

    impl IssueErcInstructionData {
        pub fn new(
            certificate_id: String,

            energy_amount: u64,

            renewable_source: String,

            validation_data: String,
        ) -> Self {
            Self {
                certificate_id,

                energy_amount,

                renewable_source,

                validation_data,
            }
        }
    }

    /// Implementation for IssueErcInstruction
    impl IssueErcInstruction {
        fn discriminator() -> [u8; 8] {
            [174u8, 248u8, 149u8, 107u8, 155u8, 4u8, 196u8, 8u8]
        }

        pub fn data(data: IssueErcInstructionData) -> Self {
            Self {
                accounts: IssueErcInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: IssueErcInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new_readonly(accounts.poa_config, false);

            self.accounts.erc_certificate = AccountMeta::new(accounts.erc_certificate, false);

            self.accounts.meter_account = AccountMeta::new_readonly(accounts.meter_account, false);

            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(accounts.system_program, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.push(self.accounts.erc_certificate.clone());

            metas.push(self.accounts.meter_account.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ProposeAuthorityChange
    // ....................................................................

    /// Main instruction struct for ProposeAuthorityChange
    pub struct ProposeAuthorityChangeInstruction {
        pub accounts: ProposeAuthorityChangeInstructionAccountMetas,
        pub data: ProposeAuthorityChangeInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ProposeAuthorityChange instruction
    #[derive(Debug, Clone, Default)]
    pub struct ProposeAuthorityChangeInstructionAccountMetas {
        pub poa_config: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for ProposeAuthorityChange instruction
    #[derive(Debug, Clone)]
    pub struct ProposeAuthorityChangeInstructionAccounts {
        pub poa_config: Pubkey,

        pub authority: Pubkey,
    }

    impl ProposeAuthorityChangeInstructionAccounts {
        pub fn new(poa_config: Pubkey, authority: Pubkey) -> Self {
            Self {
                poa_config,

                authority,
            }
        }
    }

    /// Instruction data for ProposeAuthorityChange
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ProposeAuthorityChangeInstructionData {
        pub new_authority: Pubkey,
    }

    impl ProposeAuthorityChangeInstructionData {
        pub fn new(new_authority: Pubkey) -> Self {
            Self { new_authority }
        }
    }

    /// Implementation for ProposeAuthorityChangeInstruction
    impl ProposeAuthorityChangeInstruction {
        fn discriminator() -> [u8; 8] {
            [22u8, 224u8, 215u8, 238u8, 184u8, 2u8, 83u8, 146u8]
        }

        pub fn data(data: ProposeAuthorityChangeInstructionData) -> Self {
            Self {
                accounts: ProposeAuthorityChangeInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ProposeAuthorityChangeInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new(accounts.poa_config, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: RevokeErc
    // ....................................................................

    /// Main instruction struct for RevokeErc
    pub struct RevokeErcInstruction {
        pub accounts: RevokeErcInstructionAccountMetas,
        pub data: RevokeErcInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for RevokeErc instruction
    #[derive(Debug, Clone, Default)]
    pub struct RevokeErcInstructionAccountMetas {
        pub poa_config: AccountMeta,

        pub erc_certificate: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for RevokeErc instruction
    #[derive(Debug, Clone)]
    pub struct RevokeErcInstructionAccounts {
        pub poa_config: Pubkey,

        pub erc_certificate: Pubkey,

        pub authority: Pubkey,
    }

    impl RevokeErcInstructionAccounts {
        pub fn new(poa_config: Pubkey, erc_certificate: Pubkey, authority: Pubkey) -> Self {
            Self {
                poa_config,

                erc_certificate,

                authority,
            }
        }
    }

    /// Instruction data for RevokeErc
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct RevokeErcInstructionData {
        pub reason: String,
    }

    impl RevokeErcInstructionData {
        pub fn new(reason: String) -> Self {
            Self { reason }
        }
    }

    /// Implementation for RevokeErcInstruction
    impl RevokeErcInstruction {
        fn discriminator() -> [u8; 8] {
            [16u8, 48u8, 113u8, 85u8, 118u8, 70u8, 185u8, 150u8]
        }

        pub fn data(data: RevokeErcInstructionData) -> Self {
            Self {
                accounts: RevokeErcInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: RevokeErcInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new(accounts.poa_config, false);

            self.accounts.erc_certificate = AccountMeta::new(accounts.erc_certificate, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.push(self.accounts.erc_certificate.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: SetMaintenanceMode
    // ....................................................................

    /// Main instruction struct for SetMaintenanceMode
    pub struct SetMaintenanceModeInstruction {
        pub accounts: SetMaintenanceModeInstructionAccountMetas,
        pub data: SetMaintenanceModeInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for SetMaintenanceMode instruction
    #[derive(Debug, Clone, Default)]
    pub struct SetMaintenanceModeInstructionAccountMetas {
        pub poa_config: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for SetMaintenanceMode instruction
    #[derive(Debug, Clone)]
    pub struct SetMaintenanceModeInstructionAccounts {
        pub poa_config: Pubkey,

        pub authority: Pubkey,
    }

    impl SetMaintenanceModeInstructionAccounts {
        pub fn new(poa_config: Pubkey, authority: Pubkey) -> Self {
            Self {
                poa_config,

                authority,
            }
        }
    }

    /// Instruction data for SetMaintenanceMode
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct SetMaintenanceModeInstructionData {
        pub maintenance_enabled: bool,
    }

    impl SetMaintenanceModeInstructionData {
        pub fn new(maintenance_enabled: bool) -> Self {
            Self {
                maintenance_enabled,
            }
        }
    }

    /// Implementation for SetMaintenanceModeInstruction
    impl SetMaintenanceModeInstruction {
        fn discriminator() -> [u8; 8] {
            [87u8, 100u8, 0u8, 28u8, 116u8, 52u8, 46u8, 40u8]
        }

        pub fn data(data: SetMaintenanceModeInstructionData) -> Self {
            Self {
                accounts: SetMaintenanceModeInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: SetMaintenanceModeInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new(accounts.poa_config, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: SetOracleAuthority
    // ....................................................................

    /// Main instruction struct for SetOracleAuthority
    pub struct SetOracleAuthorityInstruction {
        pub accounts: SetOracleAuthorityInstructionAccountMetas,
        pub data: SetOracleAuthorityInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for SetOracleAuthority instruction
    #[derive(Debug, Clone, Default)]
    pub struct SetOracleAuthorityInstructionAccountMetas {
        pub poa_config: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for SetOracleAuthority instruction
    #[derive(Debug, Clone)]
    pub struct SetOracleAuthorityInstructionAccounts {
        pub poa_config: Pubkey,

        pub authority: Pubkey,
    }

    impl SetOracleAuthorityInstructionAccounts {
        pub fn new(poa_config: Pubkey, authority: Pubkey) -> Self {
            Self {
                poa_config,

                authority,
            }
        }
    }

    /// Instruction data for SetOracleAuthority
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct SetOracleAuthorityInstructionData {
        pub oracle_authority: Pubkey,

        pub min_confidence: u8,

        pub require_validation: bool,
    }

    impl SetOracleAuthorityInstructionData {
        pub fn new(oracle_authority: Pubkey, min_confidence: u8, require_validation: bool) -> Self {
            Self {
                oracle_authority,

                min_confidence,

                require_validation,
            }
        }
    }

    /// Implementation for SetOracleAuthorityInstruction
    impl SetOracleAuthorityInstruction {
        fn discriminator() -> [u8; 8] {
            [39u8, 155u8, 66u8, 106u8, 213u8, 226u8, 114u8, 174u8]
        }

        pub fn data(data: SetOracleAuthorityInstructionData) -> Self {
            Self {
                accounts: SetOracleAuthorityInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: SetOracleAuthorityInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new(accounts.poa_config, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: TransferErc
    // ....................................................................

    /// Main instruction struct for TransferErc
    pub struct TransferErcInstruction {
        pub accounts: TransferErcInstructionAccountMetas,
        pub data: TransferErcInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for TransferErc instruction
    #[derive(Debug, Clone, Default)]
    pub struct TransferErcInstructionAccountMetas {
        pub poa_config: AccountMeta,

        pub erc_certificate: AccountMeta,

        pub current_owner: AccountMeta,

        pub new_owner: AccountMeta,
    }

    /// Account pubkeys for TransferErc instruction
    #[derive(Debug, Clone)]
    pub struct TransferErcInstructionAccounts {
        pub poa_config: Pubkey,

        pub erc_certificate: Pubkey,

        pub current_owner: Pubkey,

        pub new_owner: Pubkey,
    }

    impl TransferErcInstructionAccounts {
        pub fn new(
            poa_config: Pubkey,

            erc_certificate: Pubkey,

            current_owner: Pubkey,

            new_owner: Pubkey,
        ) -> Self {
            Self {
                poa_config,

                erc_certificate,

                current_owner,

                new_owner,
            }
        }
    }

    /// Instruction data for TransferErc
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct TransferErcInstructionData {}

    impl TransferErcInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for TransferErcInstruction
    impl TransferErcInstruction {
        fn discriminator() -> [u8; 8] {
            [200u8, 15u8, 16u8, 13u8, 13u8, 143u8, 11u8, 11u8]
        }

        pub fn data(data: TransferErcInstructionData) -> Self {
            Self {
                accounts: TransferErcInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: TransferErcInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new_readonly(accounts.poa_config, false);

            self.accounts.erc_certificate = AccountMeta::new(accounts.erc_certificate, false);

            self.accounts.current_owner = AccountMeta::new_readonly(accounts.current_owner, true);

            self.accounts.new_owner = AccountMeta::new_readonly(accounts.new_owner, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.push(self.accounts.erc_certificate.clone());

            metas.push(self.accounts.current_owner.clone());

            metas.push(self.accounts.new_owner.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: UpdateAuthorityInfo
    // ....................................................................

    /// Main instruction struct for UpdateAuthorityInfo
    pub struct UpdateAuthorityInfoInstruction {
        pub accounts: UpdateAuthorityInfoInstructionAccountMetas,
        pub data: UpdateAuthorityInfoInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for UpdateAuthorityInfo instruction
    #[derive(Debug, Clone, Default)]
    pub struct UpdateAuthorityInfoInstructionAccountMetas {
        pub poa_config: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for UpdateAuthorityInfo instruction
    #[derive(Debug, Clone)]
    pub struct UpdateAuthorityInfoInstructionAccounts {
        pub poa_config: Pubkey,

        pub authority: Pubkey,
    }

    impl UpdateAuthorityInfoInstructionAccounts {
        pub fn new(poa_config: Pubkey, authority: Pubkey) -> Self {
            Self {
                poa_config,

                authority,
            }
        }
    }

    /// Instruction data for UpdateAuthorityInfo
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UpdateAuthorityInfoInstructionData {
        pub contact_info: String,
    }

    impl UpdateAuthorityInfoInstructionData {
        pub fn new(contact_info: String) -> Self {
            Self { contact_info }
        }
    }

    /// Implementation for UpdateAuthorityInfoInstruction
    impl UpdateAuthorityInfoInstruction {
        fn discriminator() -> [u8; 8] {
            [199u8, 12u8, 195u8, 232u8, 185u8, 99u8, 112u8, 145u8]
        }

        pub fn data(data: UpdateAuthorityInfoInstructionData) -> Self {
            Self {
                accounts: UpdateAuthorityInfoInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UpdateAuthorityInfoInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new(accounts.poa_config, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: UpdateErcLimits
    // ....................................................................

    /// Main instruction struct for UpdateErcLimits
    pub struct UpdateErcLimitsInstruction {
        pub accounts: UpdateErcLimitsInstructionAccountMetas,
        pub data: UpdateErcLimitsInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for UpdateErcLimits instruction
    #[derive(Debug, Clone, Default)]
    pub struct UpdateErcLimitsInstructionAccountMetas {
        pub poa_config: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for UpdateErcLimits instruction
    #[derive(Debug, Clone)]
    pub struct UpdateErcLimitsInstructionAccounts {
        pub poa_config: Pubkey,

        pub authority: Pubkey,
    }

    impl UpdateErcLimitsInstructionAccounts {
        pub fn new(poa_config: Pubkey, authority: Pubkey) -> Self {
            Self {
                poa_config,

                authority,
            }
        }
    }

    /// Instruction data for UpdateErcLimits
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UpdateErcLimitsInstructionData {
        pub min_energy_amount: u64,

        pub max_erc_amount: u64,

        pub erc_validity_period: i64,
    }

    impl UpdateErcLimitsInstructionData {
        pub fn new(min_energy_amount: u64, max_erc_amount: u64, erc_validity_period: i64) -> Self {
            Self {
                min_energy_amount,

                max_erc_amount,

                erc_validity_period,
            }
        }
    }

    /// Implementation for UpdateErcLimitsInstruction
    impl UpdateErcLimitsInstruction {
        fn discriminator() -> [u8; 8] {
            [235u8, 208u8, 197u8, 149u8, 62u8, 109u8, 254u8, 110u8]
        }

        pub fn data(data: UpdateErcLimitsInstructionData) -> Self {
            Self {
                accounts: UpdateErcLimitsInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UpdateErcLimitsInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new(accounts.poa_config, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: UpdateGovernanceConfig
    // ....................................................................

    /// Main instruction struct for UpdateGovernanceConfig
    pub struct UpdateGovernanceConfigInstruction {
        pub accounts: UpdateGovernanceConfigInstructionAccountMetas,
        pub data: UpdateGovernanceConfigInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for UpdateGovernanceConfig instruction
    #[derive(Debug, Clone, Default)]
    pub struct UpdateGovernanceConfigInstructionAccountMetas {
        pub poa_config: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for UpdateGovernanceConfig instruction
    #[derive(Debug, Clone)]
    pub struct UpdateGovernanceConfigInstructionAccounts {
        pub poa_config: Pubkey,

        pub authority: Pubkey,
    }

    impl UpdateGovernanceConfigInstructionAccounts {
        pub fn new(poa_config: Pubkey, authority: Pubkey) -> Self {
            Self {
                poa_config,

                authority,
            }
        }
    }

    /// Instruction data for UpdateGovernanceConfig
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UpdateGovernanceConfigInstructionData {
        pub erc_validation_enabled: bool,
    }

    impl UpdateGovernanceConfigInstructionData {
        pub fn new(erc_validation_enabled: bool) -> Self {
            Self {
                erc_validation_enabled,
            }
        }
    }

    /// Implementation for UpdateGovernanceConfigInstruction
    impl UpdateGovernanceConfigInstruction {
        fn discriminator() -> [u8; 8] {
            [140u8, 45u8, 181u8, 17u8, 77u8, 67u8, 157u8, 248u8]
        }

        pub fn data(data: UpdateGovernanceConfigInstructionData) -> Self {
            Self {
                accounts: UpdateGovernanceConfigInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UpdateGovernanceConfigInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new(accounts.poa_config, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ValidateErcForTrading
    // ....................................................................

    /// Main instruction struct for ValidateErcForTrading
    pub struct ValidateErcForTradingInstruction {
        pub accounts: ValidateErcForTradingInstructionAccountMetas,
        pub data: ValidateErcForTradingInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ValidateErcForTrading instruction
    #[derive(Debug, Clone, Default)]
    pub struct ValidateErcForTradingInstructionAccountMetas {
        pub poa_config: AccountMeta,

        pub erc_certificate: AccountMeta,

        pub authority: AccountMeta,
    }

    /// Account pubkeys for ValidateErcForTrading instruction
    #[derive(Debug, Clone)]
    pub struct ValidateErcForTradingInstructionAccounts {
        pub poa_config: Pubkey,

        pub erc_certificate: Pubkey,

        pub authority: Pubkey,
    }

    impl ValidateErcForTradingInstructionAccounts {
        pub fn new(poa_config: Pubkey, erc_certificate: Pubkey, authority: Pubkey) -> Self {
            Self {
                poa_config,

                erc_certificate,

                authority,
            }
        }
    }

    /// Instruction data for ValidateErcForTrading
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ValidateErcForTradingInstructionData {}

    impl ValidateErcForTradingInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for ValidateErcForTradingInstruction
    impl ValidateErcForTradingInstruction {
        fn discriminator() -> [u8; 8] {
            [9u8, 215u8, 176u8, 63u8, 247u8, 150u8, 72u8, 139u8]
        }

        pub fn data(data: ValidateErcForTradingInstructionData) -> Self {
            Self {
                accounts: ValidateErcForTradingInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ValidateErcForTradingInstructionAccounts) -> Self {
            self.accounts.poa_config = AccountMeta::new_readonly(accounts.poa_config, false);

            self.accounts.erc_certificate = AccountMeta::new(accounts.erc_certificate, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.poa_config.clone());

            metas.push(self.accounts.erc_certificate.clone());

            metas.push(self.accounts.authority.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ------------------------------------------------------------------------
    // Composite Accounts
    // ------------------------------------------------------------------------
}

// ============================================================================
// CUSTOM TYPES
// ============================================================================

/// Custom struct: AmmPool
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AmmPool {
    pub market: Pubkey,

    pub energy_mint: Pubkey,

    pub currency_mint: Pubkey,

    pub energy_reserve: u64,

    pub currency_reserve: u64,

    pub curve_type: CurveType,

    pub bonding_slope: u64,

    pub bonding_base: u64,

    pub fee_bps: u16,

    pub bump: u8,
}

impl AmmPool {
    pub fn new(
        market: Pubkey,

        energy_mint: Pubkey,

        currency_mint: Pubkey,

        energy_reserve: u64,

        currency_reserve: u64,

        curve_type: CurveType,

        bonding_slope: u64,

        bonding_base: u64,

        fee_bps: u16,

        bump: u8,
    ) -> Self {
        Self {
            market,

            energy_mint,

            currency_mint,

            energy_reserve,

            currency_reserve,

            curve_type,

            bonding_slope,

            bonding_base,

            fee_bps,

            bump,
        }
    }
}

/// Custom struct: AuctionBatch
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AuctionBatch {
    pub market: Pubkey,

    pub batch_id: u64,

    pub state: u8,

    pub clearing_price: u64,

    pub clearing_volume: u64,

    pub orders: Vec<AuctionOrder>,

    pub start_time: i64,

    pub end_time: i64,

    pub bump: u8,
}

impl AuctionBatch {
    pub fn new(
        market: Pubkey,

        batch_id: u64,

        state: u8,

        clearing_price: u64,

        clearing_volume: u64,

        orders: Vec<AuctionOrder>,

        start_time: i64,

        end_time: i64,

        bump: u8,
    ) -> Self {
        Self {
            market,

            batch_id,

            state,

            clearing_price,

            clearing_volume,

            orders,

            start_time,

            end_time,

            bump,
        }
    }
}

/// Custom struct: AuctionOrder
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AuctionOrder {
    pub order_id: Pubkey,

    pub price: u64,

    pub amount: u64,

    pub is_bid: bool,

    pub timestamp: i64,
}

impl AuctionOrder {
    pub fn new(order_id: Pubkey, price: u64, amount: u64, is_bid: bool, timestamp: i64) -> Self {
        Self {
            order_id,

            price,

            amount,

            is_bid,

            timestamp,
        }
    }
}

/// Custom struct: AuctionOrderSubmitted
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AuctionOrderSubmitted {
    pub batch_id: u64,

    pub order_id: Pubkey,

    pub price: u64,

    pub amount: u64,

    pub is_bid: bool,

    pub timestamp: i64,
}

impl AuctionOrderSubmitted {
    pub fn new(
        batch_id: u64,

        order_id: Pubkey,

        price: u64,

        amount: u64,

        is_bid: bool,

        timestamp: i64,
    ) -> Self {
        Self {
            batch_id,

            order_id,

            price,

            amount,

            is_bid,

            timestamp,
        }
    }
}

/// Custom struct: AuctionResolved
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AuctionResolved {
    pub batch_id: u64,

    pub clearing_price: u64,

    pub clearing_volume: u64,

    pub timestamp: i64,
}

impl AuctionResolved {
    pub fn new(batch_id: u64, clearing_price: u64, clearing_volume: u64, timestamp: i64) -> Self {
        Self {
            batch_id,

            clearing_price,

            clearing_volume,

            timestamp,
        }
    }
}

/// Custom struct: AuctionSettled
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AuctionSettled {
    pub batch_id: u64,

    pub buyer: Pubkey,

    pub seller: Pubkey,

    pub amount: u64,

    pub price: u64,

    pub total_value: u64,

    pub timestamp: i64,
}

impl AuctionSettled {
    pub fn new(
        batch_id: u64,

        buyer: Pubkey,

        seller: Pubkey,

        amount: u64,

        price: u64,

        total_value: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            batch_id,

            buyer,

            seller,

            amount,

            price,

            total_value,

            timestamp,
        }
    }
}

/// Custom struct: BatchConfig
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct BatchConfig {
    pub enabled: u8,

    pub _padding1: [u8; 3],

    pub max_batch_size: u32,

    pub batch_timeout_seconds: u32,

    pub min_batch_size: u32,

    pub price_improvement_threshold: u16,

    pub _padding2: [u8; 6],
}

impl BatchConfig {
    pub fn new(
        enabled: u8,

        _padding1: [u8; 3],

        max_batch_size: u32,

        batch_timeout_seconds: u32,

        min_batch_size: u32,

        price_improvement_threshold: u16,

        _padding2: [u8; 6],
    ) -> Self {
        Self {
            enabled,

            _padding1,

            max_batch_size,

            batch_timeout_seconds,

            min_batch_size,

            price_improvement_threshold,

            _padding2,
        }
    }
}

/// Custom struct: BatchExecuted
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct BatchExecuted {
    pub authority: Pubkey,

    pub batch_id: u64,

    pub order_count: u32,

    pub total_volume: u64,

    pub timestamp: i64,
}

impl BatchExecuted {
    pub fn new(
        authority: Pubkey,

        batch_id: u64,

        order_count: u32,

        total_volume: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            authority,

            batch_id,

            order_count,

            total_volume,

            timestamp,
        }
    }
}

/// Custom struct: BatchInfo
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct BatchInfo {
    pub batch_id: u64,

    pub order_count: u32,

    pub _padding1: [u8; 4],

    pub total_volume: u64,

    pub created_at: i64,

    pub expires_at: i64,

    pub order_ids: [Pubkey; 32],
}

impl BatchInfo {
    pub fn new(
        batch_id: u64,

        order_count: u32,

        _padding1: [u8; 4],

        total_volume: u64,

        created_at: i64,

        expires_at: i64,

        order_ids: [Pubkey; 32],
    ) -> Self {
        Self {
            batch_id,

            order_count,

            _padding1,

            total_volume,

            created_at,

            expires_at,

            order_ids,
        }
    }
}

/// Custom struct: BridgeCompleted
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct BridgeCompleted {
    pub user: Pubkey,

    pub transfer: Pubkey,

    pub destination_chain: u16,

    pub amount: u64,

    pub vaa_hash: [u8; 32],

    pub timestamp: i64,
}

impl BridgeCompleted {
    pub fn new(
        user: Pubkey,

        transfer: Pubkey,

        destination_chain: u16,

        amount: u64,

        vaa_hash: [u8; 32],

        timestamp: i64,
    ) -> Self {
        Self {
            user,

            transfer,

            destination_chain,

            amount,

            vaa_hash,

            timestamp,
        }
    }
}

/// Custom struct: BridgeConfig
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct BridgeConfig {
    pub bump: u8,

    pub market: Pubkey,

    pub wormhole_program: Pubkey,

    pub token_bridge_program: Pubkey,

    pub authority: Pubkey,

    pub enabled: bool,

    pub min_bridge_amount: u64,

    pub bridge_fee_bps: u16,

    pub relayer_fee: u64,

    pub supported_chains: u32,

    pub total_bridged_out: u64,

    pub total_bridged_in: u64,

    pub bridge_count: u64,

    pub _reserved: [u8; 32],
}

impl BridgeConfig {
    pub fn new(
        bump: u8,

        market: Pubkey,

        wormhole_program: Pubkey,

        token_bridge_program: Pubkey,

        authority: Pubkey,

        enabled: bool,

        min_bridge_amount: u64,

        bridge_fee_bps: u16,

        relayer_fee: u64,

        supported_chains: u32,

        total_bridged_out: u64,

        total_bridged_in: u64,

        bridge_count: u64,

        _reserved: [u8; 32],
    ) -> Self {
        Self {
            bump,

            market,

            wormhole_program,

            token_bridge_program,

            authority,

            enabled,

            min_bridge_amount,

            bridge_fee_bps,

            relayer_fee,

            supported_chains,

            total_bridged_out,

            total_bridged_in,

            bridge_count,

            _reserved,
        }
    }
}

/// Custom struct: BridgeConfigUpdated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct BridgeConfigUpdated {
    pub authority: Pubkey,

    pub enabled: bool,

    pub min_bridge_amount: u64,

    pub bridge_fee_bps: u16,

    pub timestamp: i64,
}

impl BridgeConfigUpdated {
    pub fn new(
        authority: Pubkey,

        enabled: bool,

        min_bridge_amount: u64,

        bridge_fee_bps: u16,

        timestamp: i64,
    ) -> Self {
        Self {
            authority,

            enabled,

            min_bridge_amount,

            bridge_fee_bps,

            timestamp,
        }
    }
}

/// Custom struct: BridgeInitiated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct BridgeInitiated {
    pub user: Pubkey,

    pub source_mint: Pubkey,

    pub destination_chain: u16,

    pub destination_address: [u8; 32],

    pub amount: u64,

    pub fee: u64,

    pub sequence: u64,

    pub timestamp: i64,
}

impl BridgeInitiated {
    pub fn new(
        user: Pubkey,

        source_mint: Pubkey,

        destination_chain: u16,

        destination_address: [u8; 32],

        amount: u64,

        fee: u64,

        sequence: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            user,

            source_mint,

            destination_chain,

            destination_address,

            amount,

            fee,

            sequence,

            timestamp,
        }
    }
}

/// Custom struct: BridgeTransfer
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct BridgeTransfer {
    pub bump: u8,

    pub user: Pubkey,

    pub source_mint: Pubkey,

    pub destination_chain: u16,

    pub destination_address: [u8; 32],

    pub amount: u64,

    pub fee: u64,

    pub status: u8,

    pub sequence: u64,

    pub vaa_hash: [u8; 32],

    pub initiated_at: i64,

    pub completed_at: i64,

    pub _reserved: [u8; 32],
}

impl BridgeTransfer {
    pub fn new(
        bump: u8,

        user: Pubkey,

        source_mint: Pubkey,

        destination_chain: u16,

        destination_address: [u8; 32],

        amount: u64,

        fee: u64,

        status: u8,

        sequence: u64,

        vaa_hash: [u8; 32],

        initiated_at: i64,

        completed_at: i64,

        _reserved: [u8; 32],
    ) -> Self {
        Self {
            bump,

            user,

            source_mint,

            destination_chain,

            destination_address,

            amount,

            fee,

            status,

            sequence,

            vaa_hash,

            initiated_at,

            completed_at,

            _reserved,
        }
    }
}

/// Custom struct: BuyOrderCreated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct BuyOrderCreated {
    pub buyer: Pubkey,

    pub order_id: Pubkey,

    pub amount: u64,

    pub price_per_kwh: u64,

    pub timestamp: i64,
}

impl BuyOrderCreated {
    pub fn new(
        buyer: Pubkey,

        order_id: Pubkey,

        amount: u64,

        price_per_kwh: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            buyer,

            order_id,

            amount,

            price_per_kwh,

            timestamp,
        }
    }
}

/// Custom struct: CarbonCreditTransferred
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct CarbonCreditTransferred {
    pub sender: Pubkey,

    pub receiver: Pubkey,

    pub amount: u64,

    pub timestamp: i64,
}

impl CarbonCreditTransferred {
    pub fn new(sender: Pubkey, receiver: Pubkey, amount: u64, timestamp: i64) -> Self {
        Self {
            sender,

            receiver,

            amount,

            timestamp,
        }
    }
}

/// Custom struct: CarbonMarketplace
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct CarbonMarketplace {
    pub bump: u8,

    pub authority: Pubkey,

    pub rec_mint: Pubkey,

    pub carbon_mint: Pubkey,

    pub treasury: Pubkey,

    pub minting_fee_bps: u16,

    pub trading_fee_bps: u16,

    pub retirement_fee_bps: u16,

    pub total_minted: u64,

    pub total_retired: u64,

    pub total_carbon_offset: u64,

    pub active_listings: u32,

    pub is_active: bool,

    pub kwh_to_rec_rate: u32,

    pub carbon_intensity: u32,

    pub _reserved: [u8; 32],
}

impl CarbonMarketplace {
    pub fn new(
        bump: u8,

        authority: Pubkey,

        rec_mint: Pubkey,

        carbon_mint: Pubkey,

        treasury: Pubkey,

        minting_fee_bps: u16,

        trading_fee_bps: u16,

        retirement_fee_bps: u16,

        total_minted: u64,

        total_retired: u64,

        total_carbon_offset: u64,

        active_listings: u32,

        is_active: bool,

        kwh_to_rec_rate: u32,

        carbon_intensity: u32,

        _reserved: [u8; 32],
    ) -> Self {
        Self {
            bump,

            authority,

            rec_mint,

            carbon_mint,

            treasury,

            minting_fee_bps,

            trading_fee_bps,

            retirement_fee_bps,

            total_minted,

            total_retired,

            total_carbon_offset,

            active_listings,

            is_active,

            kwh_to_rec_rate,

            carbon_intensity,

            _reserved,
        }
    }
}

/// Custom struct: Commitment
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct Commitment {
    pub point: [u8; 32],
}

impl Commitment {
    pub fn new(point: [u8; 32]) -> Self {
        Self { point }
    }
}

/// Custom struct: ConfidentialBalance
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ConfidentialBalance {
    pub owner: Pubkey,

    pub mint: Pubkey,

    pub encrypted_amount: ElGamalCiphertext,

    pub pending_amount: u64,

    pub last_update_slot: u64,

    pub bump: u8,
}

impl ConfidentialBalance {
    pub fn new(
        owner: Pubkey,

        mint: Pubkey,

        encrypted_amount: ElGamalCiphertext,

        pending_amount: u64,

        last_update_slot: u64,

        bump: u8,
    ) -> Self {
        Self {
            owner,

            mint,

            encrypted_amount,

            pending_amount,

            last_update_slot,

            bump,
        }
    }
}

/// Custom struct: CrossChainOrder
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct CrossChainOrder {
    pub solana_order: Pubkey,

    pub origin_chain: u16,

    pub origin_order_id: [u8; 32],

    pub origin_user: [u8; 32],

    pub energy_amount: u64,

    pub price: u64,

    pub payment_token: [u8; 32],

    pub status: u8,

    pub settlement_vaa: [u8; 32],

    pub created_at: i64,

    pub settled_at: i64,

    pub _reserved: [u8; 32],
}

impl CrossChainOrder {
    pub fn new(
        solana_order: Pubkey,

        origin_chain: u16,

        origin_order_id: [u8; 32],

        origin_user: [u8; 32],

        energy_amount: u64,

        price: u64,

        payment_token: [u8; 32],

        status: u8,

        settlement_vaa: [u8; 32],

        created_at: i64,

        settled_at: i64,

        _reserved: [u8; 32],
    ) -> Self {
        Self {
            solana_order,

            origin_chain,

            origin_order_id,

            origin_user,

            energy_amount,

            price,

            payment_token,

            status,

            settlement_vaa,

            created_at,

            settled_at,

            _reserved,
        }
    }
}

/// Custom enum: CurveType
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub enum CurveType {
    LinearSolar,

    SteepWind,

    FlatBattery,
}

/// Custom struct: ElGamalCiphertext
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ElGamalCiphertext {
    pub r_g: [u8; 32],

    pub c: [u8; 32],
}

impl ElGamalCiphertext {
    pub fn new(r_g: [u8; 32], c: [u8; 32]) -> Self {
        Self { r_g, c }
    }
}

/// Custom struct: EqualityProof
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct EqualityProof {
    pub challenge: [u8; 32],

    pub response: [u8; 32],
}

impl EqualityProof {
    pub fn new(challenge: [u8; 32], response: [u8; 32]) -> Self {
        Self {
            challenge,

            response,
        }
    }
}

/// Custom struct: ErcCertificate
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ErcCertificate {
    pub certificate_id: String,

    pub authority: Pubkey,

    pub owner: Pubkey,

    pub energy_amount: u64,

    pub renewable_source: String,

    pub validation_data: String,

    pub issued_at: i64,

    pub expires_at: Option<i64>,

    pub status: ErcStatus,

    pub validated_for_trading: bool,

    pub trading_validated_at: Option<i64>,

    pub revocation_reason: Option<String>,

    pub revoked_at: Option<i64>,

    pub transfer_count: u8,

    pub last_transferred_at: Option<i64>,
}

impl ErcCertificate {
    pub fn new(
        certificate_id: String,

        authority: Pubkey,

        owner: Pubkey,

        energy_amount: u64,

        renewable_source: String,

        validation_data: String,

        issued_at: i64,

        expires_at: Option<i64>,

        status: ErcStatus,

        validated_for_trading: bool,

        trading_validated_at: Option<i64>,

        revocation_reason: Option<String>,

        revoked_at: Option<i64>,

        transfer_count: u8,

        last_transferred_at: Option<i64>,
    ) -> Self {
        Self {
            certificate_id,

            authority,

            owner,

            energy_amount,

            renewable_source,

            validation_data,

            issued_at,

            expires_at,

            status,

            validated_for_trading,

            trading_validated_at,

            revocation_reason,

            revoked_at,

            transfer_count,

            last_transferred_at,
        }
    }
}

/// Custom enum: ErcStatus
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub enum ErcStatus {
    Valid,

    Expired,

    Revoked,

    Pending,
}

/// Custom struct: ListingCreated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ListingCreated {
    pub listing_id: u64,

    pub seller: Pubkey,

    pub certificate: Pubkey,

    pub amount: u64,

    pub price_per_rec: u64,

    pub timestamp: i64,
}

impl ListingCreated {
    pub fn new(
        listing_id: u64,

        seller: Pubkey,

        certificate: Pubkey,

        amount: u64,

        price_per_rec: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            listing_id,

            seller,

            certificate,

            amount,

            price_per_rec,

            timestamp,
        }
    }
}

/// Custom struct: ListingFilled
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ListingFilled {
    pub listing_id: u64,

    pub buyer: Pubkey,

    pub amount: u64,

    pub total_price: u64,

    pub timestamp: i64,
}

impl ListingFilled {
    pub fn new(
        listing_id: u64,

        buyer: Pubkey,

        amount: u64,

        total_price: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            listing_id,

            buyer,

            amount,

            total_price,

            timestamp,
        }
    }
}

/// Custom struct: Market
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct Market {
    pub authority: Pubkey,

    pub total_volume: u64,

    pub created_at: i64,

    pub last_clearing_price: u64,

    pub volume_weighted_price: u64,

    pub active_orders: u32,

    pub total_trades: u32,

    pub market_fee_bps: u16,

    pub clearing_enabled: u8,

    pub _padding1: [u8; 5],

    pub batch_config: BatchConfig,

    pub current_batch: BatchInfo,

    pub has_current_batch: u8,

    pub _padding_batch: [u8; 7],

    pub buy_side_depth: [PriceLevel; 20],

    pub sell_side_depth: [PriceLevel; 20],

    pub buy_side_depth_count: u8,

    pub sell_side_depth_count: u8,

    pub price_history_count: u8,

    pub _padding_depth: [u8; 5],

    pub price_history: [PricePoint; 24],

    pub _padding_history: [u8; 0],
}

impl Market {
    pub fn new(
        authority: Pubkey,

        total_volume: u64,

        created_at: i64,

        last_clearing_price: u64,

        volume_weighted_price: u64,

        active_orders: u32,

        total_trades: u32,

        market_fee_bps: u16,

        clearing_enabled: u8,

        _padding1: [u8; 5],

        batch_config: BatchConfig,

        current_batch: BatchInfo,

        has_current_batch: u8,

        _padding_batch: [u8; 7],

        buy_side_depth: [PriceLevel; 20],

        sell_side_depth: [PriceLevel; 20],

        buy_side_depth_count: u8,

        sell_side_depth_count: u8,

        price_history_count: u8,

        _padding_depth: [u8; 5],

        price_history: [PricePoint; 24],

        _padding_history: [u8; 0],
    ) -> Self {
        Self {
            authority,

            total_volume,

            created_at,

            last_clearing_price,

            volume_weighted_price,

            active_orders,

            total_trades,

            market_fee_bps,

            clearing_enabled,

            _padding1,

            batch_config,

            current_batch,

            has_current_batch,

            _padding_batch,

            buy_side_depth,

            sell_side_depth,

            buy_side_depth_count,

            sell_side_depth_count,

            price_history_count,

            _padding_depth,

            price_history,

            _padding_history,
        }
    }
}

/// Custom struct: MarketInitialized
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct MarketInitialized {
    pub authority: Pubkey,

    pub timestamp: i64,
}

impl MarketInitialized {
    pub fn new(authority: Pubkey, timestamp: i64) -> Self {
        Self {
            authority,

            timestamp,
        }
    }
}

/// Custom struct: MarketParamsUpdated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct MarketParamsUpdated {
    pub authority: Pubkey,

    pub market_fee_bps: u16,

    pub clearing_enabled: bool,

    pub timestamp: i64,
}

impl MarketParamsUpdated {
    pub fn new(
        authority: Pubkey,

        market_fee_bps: u16,

        clearing_enabled: bool,

        timestamp: i64,
    ) -> Self {
        Self {
            authority,

            market_fee_bps,

            clearing_enabled,

            timestamp,
        }
    }
}

/// Custom struct: MeterCommitment
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct MeterCommitment {
    pub hash: [u8; 32],

    pub timestamp: i64,

    pub previous: [u8; 32],
}

impl MeterCommitment {
    pub fn new(hash: [u8; 32], timestamp: i64, previous: [u8; 32]) -> Self {
        Self {
            hash,

            timestamp,

            previous,
        }
    }
}

/// Custom struct: MeterHistory
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct MeterHistory {
    pub meter: Pubkey,

    pub readings: [u64; 24],

    pub timestamps: [i64; 24],

    pub current_index: u8,

    pub total_readings: u64,

    pub running_average: u64,

    pub std_deviation: u64,

    pub last_anomaly_at: i64,

    pub anomaly_count: u32,

    pub _reserved: [u8; 32],
}

impl MeterHistory {
    pub fn new(
        meter: Pubkey,

        readings: [u64; 24],

        timestamps: [i64; 24],

        current_index: u8,

        total_readings: u64,

        running_average: u64,

        std_deviation: u64,

        last_anomaly_at: i64,

        anomaly_count: u32,

        _reserved: [u8; 32],
    ) -> Self {
        Self {
            meter,

            readings,

            timestamps,

            current_index,

            total_readings,

            running_average,

            std_deviation,

            last_anomaly_at,

            anomaly_count,

            _reserved,
        }
    }
}

/// Custom struct: MeterReadingProof
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct MeterReadingProof {
    pub commitment: MeterCommitment,

    pub range_proof: [u8; 128],

    pub oracle_signature: [u8; 32],

    pub oracle_pubkey: Pubkey,
}

impl MeterReadingProof {
    pub fn new(
        commitment: MeterCommitment,

        range_proof: [u8; 128],

        oracle_signature: [u8; 32],

        oracle_pubkey: Pubkey,
    ) -> Self {
        Self {
            commitment,

            range_proof,

            oracle_signature,

            oracle_pubkey,
        }
    }
}

/// Custom struct: MeterVerificationConfig
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct MeterVerificationConfig {
    pub bump: u8,

    pub authority: Pubkey,

    pub authorized_oracles: [Pubkey; 5],

    pub oracle_count: u8,

    pub min_oracles: u8,

    pub max_delta_per_hour: u64,

    pub max_same_readings: u8,

    pub min_interval: u32,

    pub anomaly_detection_enabled: bool,

    pub _reserved: [u8; 32],
}

impl MeterVerificationConfig {
    pub fn new(
        bump: u8,

        authority: Pubkey,

        authorized_oracles: [Pubkey; 5],

        oracle_count: u8,

        min_oracles: u8,

        max_delta_per_hour: u64,

        max_same_readings: u8,

        min_interval: u32,

        anomaly_detection_enabled: bool,

        _reserved: [u8; 32],
    ) -> Self {
        Self {
            bump,

            authority,

            authorized_oracles,

            oracle_count,

            min_oracles,

            max_delta_per_hour,

            max_same_readings,

            min_interval,

            anomaly_detection_enabled,

            _reserved,
        }
    }
}

/// Custom struct: OracleAuthorized
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OracleAuthorized {
    pub oracle: Pubkey,

    pub authority: Pubkey,

    pub timestamp: i64,
}

impl OracleAuthorized {
    pub fn new(oracle: Pubkey, authority: Pubkey, timestamp: i64) -> Self {
        Self {
            oracle,

            authority,

            timestamp,
        }
    }
}

/// Custom struct: Order
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct Order {
    pub seller: Pubkey,

    pub buyer: Pubkey,

    pub order_id: u64,

    pub amount: u64,

    pub filled_amount: u64,

    pub price_per_kwh: u64,

    pub order_type: u8,

    pub status: u8,

    pub _padding: [u8; 6],

    pub created_at: i64,

    pub expires_at: i64,
}

impl Order {
    pub fn new(
        seller: Pubkey,

        buyer: Pubkey,

        order_id: u64,

        amount: u64,

        filled_amount: u64,

        price_per_kwh: u64,

        order_type: u8,

        status: u8,

        _padding: [u8; 6],

        created_at: i64,

        expires_at: i64,
    ) -> Self {
        Self {
            seller,

            buyer,

            order_id,

            amount,

            filled_amount,

            price_per_kwh,

            order_type,

            status,

            _padding,

            created_at,

            expires_at,
        }
    }
}

/// Custom struct: OrderCancelled
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OrderCancelled {
    pub order_id: Pubkey,

    pub user: Pubkey,

    pub timestamp: i64,
}

impl OrderCancelled {
    pub fn new(order_id: Pubkey, user: Pubkey, timestamp: i64) -> Self {
        Self {
            order_id,

            user,

            timestamp,
        }
    }
}

/// Custom struct: OrderMatched
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OrderMatched {
    pub sell_order: Pubkey,

    pub buy_order: Pubkey,

    pub seller: Pubkey,

    pub buyer: Pubkey,

    pub amount: u64,

    pub price: u64,

    pub total_value: u64,

    pub fee_amount: u64,

    pub timestamp: i64,
}

impl OrderMatched {
    pub fn new(
        sell_order: Pubkey,

        buy_order: Pubkey,

        seller: Pubkey,

        buyer: Pubkey,

        amount: u64,

        price: u64,

        total_value: u64,

        fee_amount: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            sell_order,

            buy_order,

            seller,

            buyer,

            amount,

            price,

            total_value,

            fee_amount,

            timestamp,
        }
    }
}

/// Custom struct: OrderPaymentInfo
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OrderPaymentInfo {
    pub order: Pubkey,

    pub payment_token: u8,

    pub payment_mint: Pubkey,

    pub price_in_payment_token: u64,

    pub exchange_rate: u64,

    pub rate_timestamp: i64,

    pub payment_processed: bool,

    pub _reserved: [u8; 32],
}

impl OrderPaymentInfo {
    pub fn new(
        order: Pubkey,

        payment_token: u8,

        payment_mint: Pubkey,

        price_in_payment_token: u64,

        exchange_rate: u64,

        rate_timestamp: i64,

        payment_processed: bool,

        _reserved: [u8; 32],
    ) -> Self {
        Self {
            order,

            payment_token,

            payment_mint,

            price_in_payment_token,

            exchange_rate,

            rate_timestamp,

            payment_processed,

            _reserved,
        }
    }
}

/// Custom struct: PeakEventDeclared
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct PeakEventDeclared {
    pub market: Pubkey,

    pub multiplier: u16,

    pub start_time: i64,

    pub end_time: i64,
}

impl PeakEventDeclared {
    pub fn new(market: Pubkey, multiplier: u16, start_time: i64, end_time: i64) -> Self {
        Self {
            market,

            multiplier,

            start_time,

            end_time,
        }
    }
}

/// Custom struct: PriceLevel
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct PriceLevel {
    pub price: u64,

    pub total_amount: u64,

    pub order_count: u16,

    pub _padding: [u8; 6],
}

impl PriceLevel {
    pub fn new(price: u64, total_amount: u64, order_count: u16, _padding: [u8; 6]) -> Self {
        Self {
            price,

            total_amount,

            order_count,

            _padding,
        }
    }
}

/// Custom struct: PricePoint
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct PricePoint {
    pub price: u64,

    pub volume: u64,

    pub timestamp: i64,
}

impl PricePoint {
    pub fn new(price: u64, volume: u64, timestamp: i64) -> Self {
        Self {
            price,

            volume,

            timestamp,
        }
    }
}

/// Custom struct: PriceSnapshot
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct PriceSnapshot {
    pub market: Pubkey,

    pub timestamp: i64,

    pub price: u64,

    pub base_component: u64,

    pub tou_multiplier: u16,

    pub seasonal_multiplier: u16,

    pub supply_demand_adjustment: i32,

    pub congestion_adjustment: i32,

    pub supply: u64,

    pub demand: u64,

    pub time_period: u8,

    pub season: u8,

    pub _reserved: [u8; 16],
}

impl PriceSnapshot {
    pub fn new(
        market: Pubkey,

        timestamp: i64,

        price: u64,

        base_component: u64,

        tou_multiplier: u16,

        seasonal_multiplier: u16,

        supply_demand_adjustment: i32,

        congestion_adjustment: i32,

        supply: u64,

        demand: u64,

        time_period: u8,

        season: u8,

        _reserved: [u8; 16],
    ) -> Self {
        Self {
            market,

            timestamp,

            price,

            base_component,

            tou_multiplier,

            seasonal_multiplier,

            supply_demand_adjustment,

            congestion_adjustment,

            supply,

            demand,

            time_period,

            season,

            _reserved,
        }
    }
}

/// Custom struct: PriceTier
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct PriceTier {
    pub base_price: u64,

    pub multiplier: u16,

    pub start_hour: u8,

    pub end_hour: u8,

    pub period: u8,

    pub _padding: [u8; 2],
}

impl PriceTier {
    pub fn new(
        base_price: u64,

        multiplier: u16,

        start_hour: u8,

        end_hour: u8,

        period: u8,

        _padding: [u8; 2],
    ) -> Self {
        Self {
            base_price,

            multiplier,

            start_hour,

            end_hour,

            period,

            _padding,
        }
    }
}

/// Custom struct: PriceUpdated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct PriceUpdated {
    pub market: Pubkey,

    pub old_price: u64,

    pub new_price: u64,

    pub time_period: u8,

    pub supply: u64,

    pub demand: u64,

    pub timestamp: i64,
}

impl PriceUpdated {
    pub fn new(
        market: Pubkey,

        old_price: u64,

        new_price: u64,

        time_period: u8,

        supply: u64,

        demand: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            market,

            old_price,

            new_price,

            time_period,

            supply,

            demand,

            timestamp,
        }
    }
}

/// Custom struct: PricingConfig
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct PricingConfig {
    pub bump: u8,

    pub market: Pubkey,

    pub authority: Pubkey,

    pub enabled: bool,

    pub base_price: u64,

    pub min_price: u64,

    pub max_price: u64,

    pub tou_tiers: [PriceTier; 6],

    pub tou_tier_count: u8,

    pub seasonal_multipliers: [u16; 4],

    pub current_supply: u64,

    pub current_demand: u64,

    pub supply_demand_sensitivity: u16,

    pub congestion_factor: u16,

    pub last_update: i64,

    pub update_interval: u32,

    pub timezone_offset: i16,

    pub _reserved: [u8; 32],
}

impl PricingConfig {
    pub fn new(
        bump: u8,

        market: Pubkey,

        authority: Pubkey,

        enabled: bool,

        base_price: u64,

        min_price: u64,

        max_price: u64,

        tou_tiers: [PriceTier; 6],

        tou_tier_count: u8,

        seasonal_multipliers: [u16; 4],

        current_supply: u64,

        current_demand: u64,

        supply_demand_sensitivity: u16,

        congestion_factor: u16,

        last_update: i64,

        update_interval: u32,

        timezone_offset: i16,

        _reserved: [u8; 32],
    ) -> Self {
        Self {
            bump,

            market,

            authority,

            enabled,

            base_price,

            min_price,

            max_price,

            tou_tiers,

            tou_tier_count,

            seasonal_multipliers,

            current_supply,

            current_demand,

            supply_demand_sensitivity,

            congestion_factor,

            last_update,

            update_interval,

            timezone_offset,

            _reserved,
        }
    }
}

/// Custom struct: PricingConfigured
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct PricingConfigured {
    pub market: Pubkey,

    pub authority: Pubkey,

    pub base_price: u64,

    pub enabled: bool,

    pub timestamp: i64,
}

impl PricingConfigured {
    pub fn new(
        market: Pubkey,

        authority: Pubkey,

        base_price: u64,

        enabled: bool,

        timestamp: i64,
    ) -> Self {
        Self {
            market,

            authority,

            base_price,

            enabled,

            timestamp,
        }
    }
}

/// Custom struct: RangeProof
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct RangeProof {
    pub proof_data: [u8; 64],

    pub commitment: Commitment,
}

impl RangeProof {
    pub fn new(proof_data: [u8; 64], commitment: Commitment) -> Self {
        Self {
            proof_data,

            commitment,
        }
    }
}

/// Custom struct: ReadingVerified
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ReadingVerified {
    pub meter: Pubkey,

    pub reading: u64,

    pub timestamp: i64,

    pub commitment: [u8; 32],

    pub oracle: Pubkey,
}

impl ReadingVerified {
    pub fn new(
        meter: Pubkey,

        reading: u64,

        timestamp: i64,

        commitment: [u8; 32],

        oracle: Pubkey,
    ) -> Self {
        Self {
            meter,

            reading,

            timestamp,

            commitment,

            oracle,
        }
    }
}

/// Custom struct: RecCertificate
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct RecCertificate {
    pub bump: u8,

    pub certificate_id: u64,

    pub owner: Pubkey,

    pub issuer: Pubkey,

    pub rec_type: u8,

    pub energy_amount: u64,

    pub rec_amount: u64,

    pub carbon_offset: u64,

    pub generation_start: i64,

    pub generation_end: i64,

    pub issued_at: i64,

    pub location: [u8; 8],

    pub meter: Pubkey,

    pub verified_by: Pubkey,

    pub is_retired: bool,

    pub retirement_reason: u8,

    pub retired_at: i64,

    pub retired_by: Pubkey,

    pub retirement_beneficiary: [u8; 32],

    pub _reserved: [u8; 32],
}

impl RecCertificate {
    pub fn new(
        bump: u8,

        certificate_id: u64,

        owner: Pubkey,

        issuer: Pubkey,

        rec_type: u8,

        energy_amount: u64,

        rec_amount: u64,

        carbon_offset: u64,

        generation_start: i64,

        generation_end: i64,

        issued_at: i64,

        location: [u8; 8],

        meter: Pubkey,

        verified_by: Pubkey,

        is_retired: bool,

        retirement_reason: u8,

        retired_at: i64,

        retired_by: Pubkey,

        retirement_beneficiary: [u8; 32],

        _reserved: [u8; 32],
    ) -> Self {
        Self {
            bump,

            certificate_id,

            owner,

            issuer,

            rec_type,

            energy_amount,

            rec_amount,

            carbon_offset,

            generation_start,

            generation_end,

            issued_at,

            location,

            meter,

            verified_by,

            is_retired,

            retirement_reason,

            retired_at,

            retired_by,

            retirement_beneficiary,

            _reserved,
        }
    }
}

/// Custom struct: RecMinted
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct RecMinted {
    pub certificate_id: u64,

    pub issuer: Pubkey,

    pub rec_type: u8,

    pub energy_amount: u64,

    pub rec_amount: u64,

    pub carbon_offset: u64,

    pub timestamp: i64,
}

impl RecMinted {
    pub fn new(
        certificate_id: u64,

        issuer: Pubkey,

        rec_type: u8,

        energy_amount: u64,

        rec_amount: u64,

        carbon_offset: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            certificate_id,

            issuer,

            rec_type,

            energy_amount,

            rec_amount,

            carbon_offset,

            timestamp,
        }
    }
}

/// Custom struct: RecRetired
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct RecRetired {
    pub retirement_id: u64,

    pub certificate: Pubkey,

    pub amount: u64,

    pub carbon_offset: u64,

    pub reason: u8,

    pub beneficiary: [u8; 32],

    pub timestamp: i64,
}

impl RecRetired {
    pub fn new(
        retirement_id: u64,

        certificate: Pubkey,

        amount: u64,

        carbon_offset: u64,

        reason: u8,

        beneficiary: [u8; 32],

        timestamp: i64,
    ) -> Self {
        Self {
            retirement_id,

            certificate,

            amount,

            carbon_offset,

            reason,

            beneficiary,

            timestamp,
        }
    }
}

/// Custom struct: RecTransferred
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct RecTransferred {
    pub certificate: Pubkey,

    pub from: Pubkey,

    pub to: Pubkey,

    pub amount: u64,

    pub timestamp: i64,
}

impl RecTransferred {
    pub fn new(certificate: Pubkey, from: Pubkey, to: Pubkey, amount: u64, timestamp: i64) -> Self {
        Self {
            certificate,

            from,

            to,

            amount,

            timestamp,
        }
    }
}

/// Custom struct: SellOrderCreated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct SellOrderCreated {
    pub seller: Pubkey,

    pub order_id: Pubkey,

    pub amount: u64,

    pub price_per_kwh: u64,

    pub timestamp: i64,
}

impl SellOrderCreated {
    pub fn new(
        seller: Pubkey,

        order_id: Pubkey,

        amount: u64,

        price_per_kwh: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            seller,

            order_id,

            amount,

            price_per_kwh,

            timestamp,
        }
    }
}

/// Custom struct: StablecoinOrderCreated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct StablecoinOrderCreated {
    pub order: Pubkey,

    pub owner: Pubkey,

    pub payment_token: u8,

    pub payment_mint: Pubkey,

    pub energy_amount: u64,

    pub price_in_payment: u64,

    pub timestamp: i64,
}

impl StablecoinOrderCreated {
    pub fn new(
        order: Pubkey,

        owner: Pubkey,

        payment_token: u8,

        payment_mint: Pubkey,

        energy_amount: u64,

        price_in_payment: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            order,

            owner,

            payment_token,

            payment_mint,

            energy_amount,

            price_in_payment,

            timestamp,
        }
    }
}

/// Custom struct: StablecoinSettlement
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct StablecoinSettlement {
    pub buy_order: Pubkey,

    pub sell_order: Pubkey,

    pub payment_token: u8,

    pub energy_amount: u64,

    pub payment_amount: u64,

    pub grid_equivalent: u64,

    pub timestamp: i64,
}

impl StablecoinSettlement {
    pub fn new(
        buy_order: Pubkey,

        sell_order: Pubkey,

        payment_token: u8,

        energy_amount: u64,

        payment_amount: u64,

        grid_equivalent: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            buy_order,

            sell_order,

            payment_token,

            energy_amount,

            payment_amount,

            grid_equivalent,

            timestamp,
        }
    }
}

/// Custom struct: TokenConfig
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct TokenConfig {
    pub bump: u8,

    pub market: Pubkey,

    pub token_type: u8,

    pub mint: Pubkey,

    pub decimals: u8,

    pub enabled: bool,

    pub min_order_size: u64,

    pub price_oracle: Option<Pubkey>,

    pub last_price: u64,

    pub last_price_update: i64,

    pub max_price_deviation_bps: u16,

    pub _reserved: [u8; 32],
}

impl TokenConfig {
    pub fn new(
        bump: u8,

        market: Pubkey,

        token_type: u8,

        mint: Pubkey,

        decimals: u8,

        enabled: bool,

        min_order_size: u64,

        price_oracle: Option<Pubkey>,

        last_price: u64,

        last_price_update: i64,

        max_price_deviation_bps: u16,

        _reserved: [u8; 32],
    ) -> Self {
        Self {
            bump,

            market,

            token_type,

            mint,

            decimals,

            enabled,

            min_order_size,

            price_oracle,

            last_price,

            last_price_update,

            max_price_deviation_bps,

            _reserved,
        }
    }
}

/// Custom struct: TokenConfigured
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct TokenConfigured {
    pub market: Pubkey,

    pub token_type: u8,

    pub mint: Pubkey,

    pub enabled: bool,

    pub timestamp: i64,
}

impl TokenConfigured {
    pub fn new(
        market: Pubkey,

        token_type: u8,

        mint: Pubkey,

        enabled: bool,

        timestamp: i64,
    ) -> Self {
        Self {
            market,

            token_type,

            mint,

            enabled,

            timestamp,
        }
    }
}

/// Custom struct: TokenSwapExecuted
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct TokenSwapExecuted {
    pub user: Pubkey,

    pub input_mint: Pubkey,

    pub output_mint: Pubkey,

    pub input_amount: u64,

    pub output_amount: u64,

    pub rate: u64,

    pub timestamp: i64,
}

impl TokenSwapExecuted {
    pub fn new(
        user: Pubkey,

        input_mint: Pubkey,

        output_mint: Pubkey,

        input_amount: u64,

        output_amount: u64,

        rate: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            user,

            input_mint,

            output_mint,

            input_amount,

            output_amount,

            rate,

            timestamp,
        }
    }
}

/// Custom struct: TokensReceived
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct TokensReceived {
    pub user: Pubkey,

    pub source_chain: u16,

    pub wrapped_mint: Pubkey,

    pub amount: u64,

    pub timestamp: i64,
}

impl TokensReceived {
    pub fn new(
        user: Pubkey,

        source_chain: u16,

        wrapped_mint: Pubkey,

        amount: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            user,

            source_chain,

            wrapped_mint,

            amount,

            timestamp,
        }
    }
}

/// Custom struct: TradeRecord
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct TradeRecord {
    pub sell_order: Pubkey,

    pub buy_order: Pubkey,

    pub seller: Pubkey,

    pub buyer: Pubkey,

    pub amount: u64,

    pub price_per_kwh: u64,

    pub total_value: u64,

    pub fee_amount: u64,

    pub executed_at: i64,
}

impl TradeRecord {
    pub fn new(
        sell_order: Pubkey,

        buy_order: Pubkey,

        seller: Pubkey,

        buyer: Pubkey,

        amount: u64,

        price_per_kwh: u64,

        total_value: u64,

        fee_amount: u64,

        executed_at: i64,
    ) -> Self {
        Self {
            sell_order,

            buy_order,

            seller,

            buyer,

            amount,

            price_per_kwh,

            total_value,

            fee_amount,

            executed_at,
        }
    }
}

/// Custom struct: TransferProof
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct TransferProof {
    pub amount_commitment: Commitment,

    pub amount_range_proof: RangeProof,

    pub remaining_range_proof: RangeProof,

    pub balance_proof: EqualityProof,
}

impl TransferProof {
    pub fn new(
        amount_commitment: Commitment,

        amount_range_proof: RangeProof,

        remaining_range_proof: RangeProof,

        balance_proof: EqualityProof,
    ) -> Self {
        Self {
            amount_commitment,

            amount_range_proof,

            remaining_range_proof,

            balance_proof,
        }
    }
}

/// Custom struct: VerifiedReading
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct VerifiedReading {
    pub bump: u8,

    pub meter: Pubkey,

    pub timestamp: i64,

    pub value: u64,

    pub reading_type: u8,

    pub commitment: [u8; 32],

    pub verified_by: Pubkey,

    pub verified_at: i64,

    pub anomaly_flags: u8,

    pub confidence: u8,

    pub _reserved: [u8; 32],
}

impl VerifiedReading {
    pub fn new(
        bump: u8,

        meter: Pubkey,

        timestamp: i64,

        value: u64,

        reading_type: u8,

        commitment: [u8; 32],

        verified_by: Pubkey,

        verified_at: i64,

        anomaly_flags: u8,

        confidence: u8,

        _reserved: [u8; 32],
    ) -> Self {
        Self {
            bump,

            meter,

            timestamp,

            value,

            reading_type,

            commitment,

            verified_by,

            verified_at,

            anomaly_flags,

            confidence,

            _reserved,
        }
    }
}

/// Custom struct: WrappedTokenRecord
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct WrappedTokenRecord {
    pub wrapped_mint: Pubkey,

    pub origin_chain: u16,

    pub origin_address: [u8; 32],

    pub trading_approved: bool,

    pub total_received: u64,

    pub total_unwrapped: u64,

    pub _reserved: [u8; 32],
}

impl WrappedTokenRecord {
    pub fn new(
        wrapped_mint: Pubkey,

        origin_chain: u16,

        origin_address: [u8; 32],

        trading_approved: bool,

        total_received: u64,

        total_unwrapped: u64,

        _reserved: [u8; 32],
    ) -> Self {
        Self {
            wrapped_mint,

            origin_chain,

            origin_address,

            trading_approved,

            total_received,

            total_unwrapped,

            _reserved,
        }
    }
}

/// Custom struct: AuthorityChangeApproved
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AuthorityChangeApproved {
    pub old_authority: Pubkey,

    pub new_authority: Pubkey,

    pub timestamp: i64,
}

impl AuthorityChangeApproved {
    pub fn new(old_authority: Pubkey, new_authority: Pubkey, timestamp: i64) -> Self {
        Self {
            old_authority,

            new_authority,

            timestamp,
        }
    }
}

/// Custom struct: AuthorityChangeCancelled
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AuthorityChangeCancelled {
    pub authority: Pubkey,

    pub cancelled_proposal: Pubkey,

    pub timestamp: i64,
}

impl AuthorityChangeCancelled {
    pub fn new(authority: Pubkey, cancelled_proposal: Pubkey, timestamp: i64) -> Self {
        Self {
            authority,

            cancelled_proposal,

            timestamp,
        }
    }
}

/// Custom struct: AuthorityChangeProposed
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AuthorityChangeProposed {
    pub current_authority: Pubkey,

    pub proposed_authority: Pubkey,

    pub expires_at: i64,

    pub timestamp: i64,
}

impl AuthorityChangeProposed {
    pub fn new(
        current_authority: Pubkey,

        proposed_authority: Pubkey,

        expires_at: i64,

        timestamp: i64,
    ) -> Self {
        Self {
            current_authority,

            proposed_authority,

            expires_at,

            timestamp,
        }
    }
}

/// Custom struct: AuthorityInfoUpdated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AuthorityInfoUpdated {
    pub authority: Pubkey,

    pub old_contact: String,

    pub new_contact: String,

    pub timestamp: i64,
}

impl AuthorityInfoUpdated {
    pub fn new(
        authority: Pubkey,

        old_contact: String,

        new_contact: String,

        timestamp: i64,
    ) -> Self {
        Self {
            authority,

            old_contact,

            new_contact,

            timestamp,
        }
    }
}

/// Custom struct: EmergencyPauseActivated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct EmergencyPauseActivated {
    pub authority: Pubkey,

    pub timestamp: i64,
}

impl EmergencyPauseActivated {
    pub fn new(authority: Pubkey, timestamp: i64) -> Self {
        Self {
            authority,

            timestamp,
        }
    }
}

/// Custom struct: EmergencyPauseDeactivated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct EmergencyPauseDeactivated {
    pub authority: Pubkey,

    pub timestamp: i64,
}

impl EmergencyPauseDeactivated {
    pub fn new(authority: Pubkey, timestamp: i64) -> Self {
        Self {
            authority,

            timestamp,
        }
    }
}

/// Custom struct: ErcCertificate
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ErcCertificate {
    pub certificate_id: String,

    pub authority: Pubkey,

    pub owner: Pubkey,

    pub energy_amount: u64,

    pub renewable_source: String,

    pub validation_data: String,

    pub issued_at: i64,

    pub expires_at: Option<i64>,

    pub status: ErcStatus,

    pub validated_for_trading: bool,

    pub trading_validated_at: Option<i64>,

    pub revocation_reason: Option<String>,

    pub revoked_at: Option<i64>,

    pub transfer_count: u8,

    pub last_transferred_at: Option<i64>,
}

impl ErcCertificate {
    pub fn new(
        certificate_id: String,

        authority: Pubkey,

        owner: Pubkey,

        energy_amount: u64,

        renewable_source: String,

        validation_data: String,

        issued_at: i64,

        expires_at: Option<i64>,

        status: ErcStatus,

        validated_for_trading: bool,

        trading_validated_at: Option<i64>,

        revocation_reason: Option<String>,

        revoked_at: Option<i64>,

        transfer_count: u8,

        last_transferred_at: Option<i64>,
    ) -> Self {
        Self {
            certificate_id,

            authority,

            owner,

            energy_amount,

            renewable_source,

            validation_data,

            issued_at,

            expires_at,

            status,

            validated_for_trading,

            trading_validated_at,

            revocation_reason,

            revoked_at,

            transfer_count,

            last_transferred_at,
        }
    }
}

/// Custom struct: ErcIssued
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ErcIssued {
    pub certificate_id: String,

    pub authority: Pubkey,

    pub energy_amount: u64,

    pub renewable_source: String,

    pub timestamp: i64,
}

impl ErcIssued {
    pub fn new(
        certificate_id: String,

        authority: Pubkey,

        energy_amount: u64,

        renewable_source: String,

        timestamp: i64,
    ) -> Self {
        Self {
            certificate_id,

            authority,

            energy_amount,

            renewable_source,

            timestamp,
        }
    }
}

/// Custom struct: ErcLimitsUpdated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ErcLimitsUpdated {
    pub authority: Pubkey,

    pub old_min: u64,

    pub new_min: u64,

    pub old_max: u64,

    pub new_max: u64,

    pub old_validity: i64,

    pub new_validity: i64,

    pub timestamp: i64,
}

impl ErcLimitsUpdated {
    pub fn new(
        authority: Pubkey,

        old_min: u64,

        new_min: u64,

        old_max: u64,

        new_max: u64,

        old_validity: i64,

        new_validity: i64,

        timestamp: i64,
    ) -> Self {
        Self {
            authority,

            old_min,

            new_min,

            old_max,

            new_max,

            old_validity,

            new_validity,

            timestamp,
        }
    }
}

/// Custom struct: ErcRevoked
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ErcRevoked {
    pub certificate_id: String,

    pub authority: Pubkey,

    pub reason: String,

    pub energy_amount: u64,

    pub timestamp: i64,
}

impl ErcRevoked {
    pub fn new(
        certificate_id: String,

        authority: Pubkey,

        reason: String,

        energy_amount: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            certificate_id,

            authority,

            reason,

            energy_amount,

            timestamp,
        }
    }
}

/// Custom enum: ErcStatus
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub enum ErcStatus {
    Valid,

    Expired,

    Revoked,

    Pending,
}

/// Custom struct: ErcTransferred
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ErcTransferred {
    pub certificate_id: String,

    pub from_owner: Pubkey,

    pub to_owner: Pubkey,

    pub energy_amount: u64,

    pub timestamp: i64,
}

impl ErcTransferred {
    pub fn new(
        certificate_id: String,

        from_owner: Pubkey,

        to_owner: Pubkey,

        energy_amount: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            certificate_id,

            from_owner,

            to_owner,

            energy_amount,

            timestamp,
        }
    }
}

/// Custom struct: ErcValidatedForTrading
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ErcValidatedForTrading {
    pub certificate_id: String,

    pub authority: Pubkey,

    pub timestamp: i64,
}

impl ErcValidatedForTrading {
    pub fn new(certificate_id: String, authority: Pubkey, timestamp: i64) -> Self {
        Self {
            certificate_id,

            authority,

            timestamp,
        }
    }
}

/// Custom struct: GovernanceConfigUpdated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct GovernanceConfigUpdated {
    pub authority: Pubkey,

    pub erc_validation_enabled: bool,

    pub old_enabled: bool,

    pub timestamp: i64,
}

impl GovernanceConfigUpdated {
    pub fn new(
        authority: Pubkey,

        erc_validation_enabled: bool,

        old_enabled: bool,

        timestamp: i64,
    ) -> Self {
        Self {
            authority,

            erc_validation_enabled,

            old_enabled,

            timestamp,
        }
    }
}

/// Custom struct: GovernanceStats
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct GovernanceStats {
    pub total_ercs_issued: u64,

    pub total_ercs_validated: u64,

    pub total_ercs_revoked: u64,

    pub total_energy_certified: u64,

    pub erc_validation_enabled: bool,

    pub emergency_paused: bool,

    pub maintenance_mode: bool,

    pub min_energy_amount: u64,

    pub max_erc_amount: u64,

    pub erc_validity_period: i64,

    pub require_oracle_validation: bool,

    pub allow_certificate_transfers: bool,

    pub delegation_enabled: bool,

    pub created_at: i64,

    pub last_updated: i64,

    pub last_erc_issued_at: Option<i64>,

    pub pending_authority_change: bool,

    pub pending_authority: Option<Pubkey>,

    pub pending_authority_expires_at: Option<i64>,

    pub oracle_authority: Option<Pubkey>,

    pub min_oracle_confidence: u8,
}

impl GovernanceStats {
    pub fn new(
        total_ercs_issued: u64,

        total_ercs_validated: u64,

        total_ercs_revoked: u64,

        total_energy_certified: u64,

        erc_validation_enabled: bool,

        emergency_paused: bool,

        maintenance_mode: bool,

        min_energy_amount: u64,

        max_erc_amount: u64,

        erc_validity_period: i64,

        require_oracle_validation: bool,

        allow_certificate_transfers: bool,

        delegation_enabled: bool,

        created_at: i64,

        last_updated: i64,

        last_erc_issued_at: Option<i64>,

        pending_authority_change: bool,

        pending_authority: Option<Pubkey>,

        pending_authority_expires_at: Option<i64>,

        oracle_authority: Option<Pubkey>,

        min_oracle_confidence: u8,
    ) -> Self {
        Self {
            total_ercs_issued,

            total_ercs_validated,

            total_ercs_revoked,

            total_energy_certified,

            erc_validation_enabled,

            emergency_paused,

            maintenance_mode,

            min_energy_amount,

            max_erc_amount,

            erc_validity_period,

            require_oracle_validation,

            allow_certificate_transfers,

            delegation_enabled,

            created_at,

            last_updated,

            last_erc_issued_at,

            pending_authority_change,

            pending_authority,

            pending_authority_expires_at,

            oracle_authority,

            min_oracle_confidence,
        }
    }
}

/// Custom struct: MaintenanceModeUpdated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct MaintenanceModeUpdated {
    pub authority: Pubkey,

    pub maintenance_enabled: bool,

    pub timestamp: i64,
}

impl MaintenanceModeUpdated {
    pub fn new(authority: Pubkey, maintenance_enabled: bool, timestamp: i64) -> Self {
        Self {
            authority,

            maintenance_enabled,

            timestamp,
        }
    }
}

/// Custom struct: OracleAuthoritySet
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OracleAuthoritySet {
    pub authority: Pubkey,

    pub oracle_authority: Pubkey,

    pub min_confidence: u8,

    pub timestamp: i64,
}

impl OracleAuthoritySet {
    pub fn new(
        authority: Pubkey,

        oracle_authority: Pubkey,

        min_confidence: u8,

        timestamp: i64,
    ) -> Self {
        Self {
            authority,

            oracle_authority,

            min_confidence,

            timestamp,
        }
    }
}

/// Custom struct: PoAConfig
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct PoAConfig {
    pub authority: Pubkey,

    pub authority_name: String,

    pub contact_info: String,

    pub version: u8,

    pub emergency_paused: bool,

    pub emergency_timestamp: Option<i64>,

    pub emergency_reason: Option<String>,

    pub maintenance_mode: bool,

    pub erc_validation_enabled: bool,

    pub min_energy_amount: u64,

    pub max_erc_amount: u64,

    pub erc_validity_period: i64,

    pub auto_revoke_expired: bool,

    pub require_oracle_validation: bool,

    pub delegation_enabled: bool,

    pub oracle_authority: Option<Pubkey>,

    pub min_oracle_confidence: u8,

    pub allow_certificate_transfers: bool,

    pub total_ercs_issued: u64,

    pub total_ercs_validated: u64,

    pub total_ercs_revoked: u64,

    pub total_energy_certified: u64,

    pub created_at: i64,

    pub last_updated: i64,

    pub last_erc_issued_at: Option<i64>,

    pub pending_authority: Option<Pubkey>,

    pub pending_authority_proposed_at: Option<i64>,

    pub pending_authority_expires_at: Option<i64>,
}

impl PoAConfig {
    pub fn new(
        authority: Pubkey,

        authority_name: String,

        contact_info: String,

        version: u8,

        emergency_paused: bool,

        emergency_timestamp: Option<i64>,

        emergency_reason: Option<String>,

        maintenance_mode: bool,

        erc_validation_enabled: bool,

        min_energy_amount: u64,

        max_erc_amount: u64,

        erc_validity_period: i64,

        auto_revoke_expired: bool,

        require_oracle_validation: bool,

        delegation_enabled: bool,

        oracle_authority: Option<Pubkey>,

        min_oracle_confidence: u8,

        allow_certificate_transfers: bool,

        total_ercs_issued: u64,

        total_ercs_validated: u64,

        total_ercs_revoked: u64,

        total_energy_certified: u64,

        created_at: i64,

        last_updated: i64,

        last_erc_issued_at: Option<i64>,

        pending_authority: Option<Pubkey>,

        pending_authority_proposed_at: Option<i64>,

        pending_authority_expires_at: Option<i64>,
    ) -> Self {
        Self {
            authority,

            authority_name,

            contact_info,

            version,

            emergency_paused,

            emergency_timestamp,

            emergency_reason,

            maintenance_mode,

            erc_validation_enabled,

            min_energy_amount,

            max_erc_amount,

            erc_validity_period,

            auto_revoke_expired,

            require_oracle_validation,

            delegation_enabled,

            oracle_authority,

            min_oracle_confidence,

            allow_certificate_transfers,

            total_ercs_issued,

            total_ercs_validated,

            total_ercs_revoked,

            total_energy_certified,

            created_at,

            last_updated,

            last_erc_issued_at,

            pending_authority,

            pending_authority_proposed_at,

            pending_authority_expires_at,
        }
    }
}

/// Custom struct: PoAInitialized
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct PoAInitialized {
    pub authority: Pubkey,

    pub authority_name: String,

    pub timestamp: i64,
}

impl PoAInitialized {
    pub fn new(authority: Pubkey, authority_name: String, timestamp: i64) -> Self {
        Self {
            authority,

            authority_name,

            timestamp,
        }
    }
}

// ============================================================================
// END OF GENERATED FILE
// ============================================================================
